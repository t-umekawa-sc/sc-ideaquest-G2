// SC-40 実績/バッジ（プロトタイプ・スタブ）。実体は features/achievements へ（ゲーム層）。
import { ScreenStub } from "@/components/layout";

export default function AchievementsPage() {
  return (
    <ScreenStub
      code="SC-40"
      title="実績 / バッジ"
      description="収集サマリー（達成率）＋バッジグリッド（ティア銅銀金・進捗・シークレット）。モック移植予定。"
    />
  );
}
