# SC-50 情報インプット（一覧・登録・詳細＋関連情報パネル）（画面設計 / 機能詳細）

> 外部 WEB 情報を**手動貼り付け**で登録し、属性を付け、アイデア/コンセプト/クエストへ**動的に関連づけ**る会社横断の知識レイヤ。**サービスの差別化の芯**。
> 仕様の正＝[情報インプット機能 設計](../../設計ドラフト/情報インプット機能_設計.md)（残論点は §11 で確定）・[API設計 N](../../API設計/N_情報インプット.md)・[データモデル §5.33〜5.37/§3（`info_*` enum）](../../データモデル.md)。通知は [H](../../API設計/H_通知.md)。要件＝[FR-41](../../要件定義/README.md#6-機能要件)。UI 標準＝[デザイン標準 §4.1](../デザイン標準.md)（ダイアログ内コンテンツ標準）・一覧は [フロントエンド実装フロー規約 §4.1](../../規約/フロントエンド実装フロー規約.md)（サーバー委譲）。
> 画面 ID＝**SC-50 一覧／SC-51 登録・編集（モーダル）／SC-52 詳細（モーダル）**（本ファイルに束ねる。SC-13 が掲示板＋ダイアログを1ファイルにする流儀に倣う）。

## 1. 概要・目的

- 参加者**全員**が外部 WEB 情報を**無造作に登録**（低摩擦＝タイトル＋本文＋出典URL）→ **`info_curator`（情報判定権限）が属性付与/判定** → **アイデア/コンセプト/クエストへ動的リンク** → **反証は能動的に“揺さぶる”**（要再評価）。
- **受動的なスクラップ帳でなく、絞り込みの流れを能動的に駆動する知識レイヤ**（ISO56002 §7 知識/インテリジェンス・機会特定の燃料）。
- **取り込みは手動貼付のみ**（サーバは外部URLを取りに行かない＝SSRF 対象外・§7）。

## 2. アクセス条件・権限

- **会社横断の知識プール**＝クエスト門番（`can_access_quest`）ではなく、**会社内の active ユーザーなら一覧/詳細を閲覧可**（テナント内・他社は不可）。グローバルナビ（`.appnav`）業務群に1項目「情報インプット」を追加。
- 書き込みは段階権限（[API N.0](../../API設計/N_情報インプット.md)）:
  - **全ユーザー**＝低摩擦登録（`title`＋任意 `body`/`source_url`＝`status=raw`）／自分視点の**手動リンク追加・棄却**。
  - **`info_curator`**＝属性付与・情報判定（triage）・`status` 遷移（raw→curated→archived）・アーカイブ／自動リンクの**種別変更**（関連↔裏付け↔反証）。
  - **`info_curator` の付与/剥奪**＝会社アカウント管理者/system_admin（導線は SC-90 系に同居・§8）。
- 認可失敗＝403／他テナント＝404／未認証＝401。**属性付与/判定の可否はサーバーが `can` で返し**、フロントは表示/UX 出し分けのみ（コーディング規約 §1）。

## 3. レイアウト構成（SC-50 一覧）

- ヘッダー（画面名「情報インプット」）＋主操作「＋情報を登録」（全ユーザー）＋一覧ツールバー（`.list-toolbar`）。
- **一覧は DataTable サーバーモード委譲**（`GET /info-items`・§1.8.1・フロント実装フロー規約 §4.1）＝横断検索（`q`＝title/body 全文 PGroonga）・分類フィルタ（`status`/`priority`/`source`/`classification`/`scope`/`target_business`/`impact_class`/`impact_level`/`impact_timing`/`triage`／`category` #8 多値）・ソート（`-created_at`/`priority`/`-impact_level`/`due_date`）・番号/カーソルページャ・列設定/密度/CSV。列 `sortable`/`filter` は backend ホワイトリスト一致。
- **ワードクラウド パネル**＝一覧の上部 or サイド（`GET /info-items/word-cloud`）。**現在の絞り込み範囲**のトークン頻度を可視化＝語クリックで `q` に流し込み（絞り込み動線）。
- 既定表示＝`status=archived` を除外（アーカイブは絞り込みで明示表示）。

## 4. 表示項目（一覧カード/行）

- タイトル／**要約プレビュー**（`summary`・§12-3）／`status` バッジ（`raw`＝未判定・`curated`＝判定済み・`archived`）／`priority`（優先度）／`impact_class`（機会/脅威/その他バッジ・色分け）／情報カテゴリ(#8) チップ／情報ソース（#2）／出典URL（リンク・別タブ・`rel="noopener noreferrer"`）／`due_date`（対応/有効期限・切迫度）／登録者（アバター/氏名）／登録日時／💡関連リンク数（`link_count`）／**🧵続報バッジ**（`follow_up_count>0` で数を表示・§12-1）。
- **要約/本文プレビューはサニタイズ済み**（要約は平文 `summary`／本文は `body_html` の許可リストサニタイズ・XSS・§9）。`roots_only` で続報を根に束ねる切替。

## 5. アクション・遷移

| 操作 | 条件 | 遷移/結果 |
| --- | --- | --- |
| ＋情報を登録 | 全ユーザー | **SC-51 登録モーダル**（URL付き・§6） |
| 続報を登録 | 全ユーザー | **SC-51 登録モーダル**（親をプリフィル＋`parent_info_id`・§6a/§12-1） |
| 行/カードクリック | 閲覧可 | **SC-52 詳細モーダル**（URL付き・§7） |
| ワードクラウドの語 | — | 語を `q` に適用して一覧を再クエリ（絞り込み） |
| 分類フィルタ/検索/ソート | — | サーバー委譲で再クエリ（URL 同期・状態復元・§4.12） |

## 6. SC-51 登録・編集（モーダル）

- **標準モーダル**（`.modal.sectioned`・URL付き＝Parallel＋Intercept・[デザイン標準 §4.1](../デザイン標準.md)）。フッター文言＝フォームなので「**キャンセル**」。
- **ダイアログ内コンテンツ標準**（§4.1）＝囲まず**項目ごとに仕切り線**（`.dialog-section`）。
- **入力グループ（項目ごとに区切り）**:
  1. **基本（全ユーザー・低摩擦）**＝タイトル（必須）／内容・説明（**リッチテキスト**＝WEBページを書式込みで貼付・§6a）／出典URL（http/https のみ・422 `invalid_url`）。
  2. **分類（`info_curator` のみ・raw では非表示 or 読取）**＝優先度／情報ソース／情報分類（単一）／大分類／対象事業／**情報カテゴリ（#8・複数可）**。
  3. **影響評価（環境スキャン・curator）**＝影響度／**影響分類（機会/脅威/その他）**／影響発生時期。
  4. **判定（トリアージ・curator）**＝情報判定日／情報判定／判定理由。
  5. **期限日**＝対応/有効期限（任意・#11 影響発生時期とは別軸）。
- **出し分け**＝一般ユーザーは基本のみ（保存で `status=raw`）／`info_curator` は全項目（属性を付けると `status=raw→curated`）。編集時、raw の登録者本人は基本のみ編集可・curated 後は `info_curator` のみ。
- 保存（`POST`/`PATCH /info-items`）＝本文/タイトル変更時に**サニタイズ→`body_text` 派生→`info_tokens` 再生成→抽出要約 `summary` 生成→類似度で auto リンク（既定 `related`）再計算**（同期・[API N.6](../../API設計/N_情報インプット.md)・§12-2/12-3）。§4.7 入力検証（必須=タイトル・URL 形式・上部サマリ＋足元ヒント＋エラースナックバー）。

### 6a. リッチテキスト本文・ワードクラウドプレビュー・続報（2026-09-19 追加・§12）
- **リッチテキスト入力（§12-4）**＝WEBページを**書式（太字/箇条書き/見出し/リンク/表）＋画像込みで貼付**。エディタ候補＝Tiptap(ProseMirror) 等（`shared.css` に馴染ませる・選定は実装フェーズ）。**画像は貼付時に MinIO 再ホスト**（`POST /info-items/images`・paste ハンドラで blob→自社 URL 置換）。保存時 nh3 サニタイズ（§9）。
- **ワードクラウドプレビュー（§12-2）**＝本文入力欄の近くに「この情報の語」プレビュー＝草稿を `POST /info-items/word-cloud-preview` で**同期トークン化**して表示（トリガ＝本文 **blur かボタン**・キー入力毎は不可）。保存後は詳細（SC-52）のミニ・ワードクラウドに引き継ぐ。
- **要約プレビュー**＝保存後に抽出要約 `summary` が付く（選別で使う・SC-52/一覧で表示）。
- **続報登録（§12-1）**＝SC-52 詳細 or 一覧行の「**続報を登録**」から開くと、**親の属性を初期プリフィル**した状態で本モーダルが開く（本文/URL は新規）。保存時に `parent_info_id` を送り、**親の未棄却リンクを `origin=auto` でスナップショット複製**。

## 7. SC-52 詳細（モーダル）＋関連リンク

- **標準モーダル**（参照系＝フッター「**閉じる**」・§4.1）。ダイアログ内コンテンツ標準（囲まず項目ごとに仕切り線）。
- **表示**＝全属性（§4 の項目）＋**要約**（`summary`・先頭に配置＝長文を読まず概要把握・§12-3）＋本文（`body_html` の**サニタイズ済みリッチ表示**＝書式/画像込み・§12-4）＋出典URL＋**ミニ・ワードクラウド**（この情報のトークン上位）。
- **続報スレッド（§12-1）**＝`thread.parent`（続報元があれば「◯◯の続報」導線）＋`thread.follow_ups[]`（この情報への続報を**時系列**で一覧）。**「続報を登録」**アクション（全ユーザー・親をプリフィルして SC-51 を開く）。
- **この情報からクエストを作成（機会特定→行動の動線・2026-09-19 追加）**＝判定の結果「新しく取り組む価値がある」と判断したら、**「＋ この情報からクエストを作成」**（primary）で **SC-11 クエスト作成**を開く。**元情報バナー（`.disclosure--ref`）＋目的・テーマを情報の要約からプリフィル**（編集可）。保存時に **`info_link`（情報→新クエスト・`origin=manual`・`kind=related`）を自動作成**＝最初から関連付いた状態（API＝C クエスト作成の `from_info_id`／§N と突合）。※判定＝`innovation活動`/カイゼン系のとき特に目立たせる。
- **関連リンク（§N.3）**＝この情報が紐づく成果物の一覧（`links[]`）＝相手（アイデア/コンセプト/クエスト/前提）へのリンク＋**種別バッジ**（関連/裏付け/**反証**・色分け）＋由来（auto/manual）＋一致度（score）。
  - **種別変更**（`info_curator`）＝関連→裏付け/反証。**反証（manual）に変えると post-commit で「根底を揺さぶる」＝作成者＋評価者へ通知＋要再評価フラグ**（[コンセプト機能 §3.5](../../設計ドラフト/コンセプト機能_ISO56002_再設計.md)）。確認ダイアログ（変更が波及するため）。
  - **手動リンク追加**（全ユーザー）＝相手を選んで関連付け（`POST /info-links`）。**自動リンクの棄却**（`rejected_at`・パネルから除外）。
- **属性付与/判定/アーカイブ**（`info_curator`）＝「属性を編集」→ SC-51 編集モーダル／「アーカイブ」（`POST /info-items/{id}/archive`・確認ダイアログ）。登録者本人の raw は「削除」（`DELETE`・`status=raw` のみ・確認ダイアログ）。
- フッター順＝閉じる（左）→ 副（アーカイブ/削除）→ 主要（属性を編集）（§4.1）。

## 8. 関連情報パネル（各成果物へ内包＝共有セクション）

- **アイデア詳細（SC-22）／クエスト詳細（SC-12）／コンセプト（新設）** に「**関連情報**」パネルを内包＝`GET /{artifact}/related-info`（一致度上位 N・`rejected` 除外）。
- 表示＝情報タイトル＋**種別バッジ**（関連/裏付け/反証）＋出典＋一致度。カードクリックで **SC-52 詳細モーダル**。
- 操作＝**手動リンク追加**（この成果物に情報を紐付け）／**棄却**（自動リンクを外す）／（curator）**種別変更**。**反証で揺さぶり**＝当該成果物に「要再評価」表示＋通知（受領側の SC-22/コンセプト側で明示）。
- **双方向**＝情報→成果物（提示・揺さぶり）／成果物→情報（前提が証拠として情報を引用＝コンセプト段の「前提1件=1スレッド」に情報を貼る動線・§3.5）。
- 情報判定権限の付与 UI は会社アカウント管理（SC-90 系）に同居（`info_curator` の一覧/付与/剥奪＝[API N.5](../../API設計/N_情報インプット.md)）。

## 9. セキュリティ・ガバナンス（§7 設計に準拠）

- **本文の XSS サニタイズ（リッチテキスト・§12-4）**＝**保存時＋表示時に許可リスト方式**で無害化（backend `nh3`／front も `dangerouslySetInnerHTML` は許可リスト済みのみ・コーディング規約 §2.2④）。on* 属性・script・`javascript:` を全弾き。**貼付画像は MinIO 再ホスト**（外部 img src は除去/再ホスト＝トラッキング/referer 漏れ防止・`POST /info-items/images`）。**出典URLは http/https のみ**（登録/更新で 422）。外部リンクは `rel="noopener noreferrer"` ＋別タブ。
- **SSRF＝対象外**（サーバフェッチ無し）。
- **削除ガード**＝curated 済みの物理削除は不可（アーカイブへ誘導・409）。
- **テナント分離**＝会社DB（他社の情報は見えない・404）。

## 10. モック / 実装フロー

- **モック先行**（フロント実装フロー規約）＝`doc/画面設計/mocks/SC-50_情報インプット.html`（一覧＝DataTable・登録/詳細モーダル・ワードクラウド・関連情報パネル）を作成→受入→実装（`impl/frontend/src/features/info-input`〔新 feature〕）。
- **一覧は最初からサーバー委譲契約**（`GET /info-items` の DataTable クエリ・列 flags=backend ホワイトリスト一致）＝後からの作り直しを避ける（実装フロー規約 §4.1・memory）。
- backend＝新ドメイン `tenant/info`（4層）＝router/application/domain/repository＋`janome` 再利用（トークン/類似度）。

## 11. 未確定・次アクション

1. **SC 番号の確定**（SC-50/51/52 で採番・画面遷移図へ反映済み）。コンセプト画面の SC 番号はコンセプト段で採番。
2. **関連情報パネルの内包先**＝アイデア詳細（SC-22）／クエスト詳細（SC-12）／コンセプト（新設）。各画面のどのタブ/位置に置くか（SC-22 は評価結果の下、SC-12 は概要 or 専用タブ）は各画面のモック作成時に確定。
3. **ワードクラウドの実装**＝MVP はトークン頻度（`info_tokens`）／描画ライブラリ選定（軽量・reduce-motion 尊重）。
4. **FR 起票**（要件定義 README・設計 §10-4）＝MVP 境界（§9 設計）・受入観点。
5. 情報判定権限付与 UI を SC-90 系のどこに置くか（会社アカウント管理の一機能）。
