# ゲーム感 ブラウザ受入チェックリスト

> 目的＝「ゲーム感向上」フェーズの**視覚/操作感**をユーザーがブラウザで確認するための一覧。各項目に**一意ID（`GF-AC-NNN`）**を付ける。
> 使い方＝下表を上から操作して確認し、気になる点は **ID を添えて**指摘する（例：「GF-AC-002 のバー速度が速すぎる」）。自動テスト（純ロジックの vitest）は別途 `doc/テスト/I_ダッシュボード.md` の I-TC-150/151 で担保済み＝ここは**見た目の受入**に限定。
> 状態列＝`未確認`／`OK`／`要修正`（指摘は本人が記入 or 口頭）。

## 事前準備（並行ワークフロー）
- **受入/QA は compose の本番ビルドで**（2026-08-28〜）＝`docker compose -f impl/compose.yaml up -d --build frontend`。**新しいコミットを反映するには毎回 `--build` で再ビルド**（コンテナ内にソースを焼くため）。本番ビルドはハッシュ付き CSS を確定参照するので**F5 リロードでスタイルが剥がれない**（`next dev` のオンデマンド compile 起因の無スタイル現象を回避）。backend 一式も compose 稼働・`/api` は `backend:8000` へプロキシ。
- **コードを弄りながらの素早い検証はホスト**＝`cd impl/frontend && npm run dev`（HMR・`/api` は `localhost:8000` へプロキシ）。※`next dev` はごく稀にリロードで CSS グリッチが起こり得る＝その場合は dev サーバーだけ再起動（コンテナ再起動は不要）。
- ブラウザ `http://localhost:3000` → ログイン `ACME-01` / `user@acme.example` / `Passw0rd!` → **ダッシュボード**。
- OS 設定「視差効果を減らす / reduce motion」を ON/OFF で切り替えて両方確認できると尚良い。

## 増分 #1：数値の演出（SC-01 ヒーロー）

| ID | 対象 | 確認手順 | 期待する見た目/挙動 | 状態 |
|---|---|---|---|---|
| GF-AC-001 | コイン/SP のカウントアップ | ダッシュボードを開く（またはリロード） | ヒーローの「◆ コイン」「✦ SP」の数字が **0 から現在値へ数え上がる**（約0.7秒・終盤で滑らかに減速） | ✅ OK |
| GF-AC-002 | XP バーの充填アニメ | 同上 | XP バーが **左端 0% から現在%へ伸びて充填**される（約0.8秒） | ✅ OK |
| GF-AC-003 | reduce motion 尊重（#1） | OS の「視差効果を減らす」を ON にしてリロード | カウントアップ/バー充填の**演出が無効**＝数字もバーも**即座に最終値**で表示（チラつき無し） | ✅ OK |

## 増分 #2：レベルアップ祝福（SC-01）

> 実際にレベルアップするのは手間なので、**検証用トリック**で発火させられます：ブラウザ devtools → Application → Local Storage → `http://localhost:3000` の `iq:lastSeenLevel:<あなたのaccount_id>` の値を**現在レベルより小さい数**（例 `1`）に書き換えて**リロード**。→ 祝福が出る。確認後は消して構いません（次回リロードで現在値に再記録されます）。

| ID | 対象 | 確認手順 | 期待する見た目/挙動 | 状態 |
|---|---|---|---|---|
| GF-AC-010 | 祝福オーバーレイの表示 | 上記トリックで発火 | 画面中央に **「LEVEL UP!」＋「Lv.N」＋「スキルポイント +1」** のカードがポップイン、周囲に✦の火花・紫のオーラ。約2.6秒で自動的に消える | ✅ OK |
| GF-AC-011 | クリックで即閉じ | 祝福表示中にカード/背景をクリック | すぐ閉じる | ✅ OK |
| GF-AC-012 | 誤発火しない（初回・同値・下降） | localStorage の当該キーを**削除**してリロード／その後もう一度リロード | 1回目（初回観測）＝**祝福は出ない**（記録のみ）。2回目（同値）＝**出ない** | ✅ OK |
| GF-AC-013 | アカウント別スコープ | 別アカウント（例 `mfa@acme2.example`）でログインしてダッシュボード | 前ユーザーのレベル記録に引きずられて**誤って祝福が出ない**（キーは account_id 別） | OK |
| GF-AC-014 | reduce motion 尊重（#2） | 「視差効果を減らす」ON で発火 | カード/火花/オーラの**アニメが無効**＝静的にカード表示（火花は非表示）・自動消滅は機能 | OK |

## 増分 #3：登場アニメ（SC-01 パネル/カード）

| ID | 対象 | 確認手順 | 期待する見た目/挙動 | 状態 |
|---|---|---|---|---|
| GF-AC-020 | パネル/カードの登場フェード（framer・load 時のみ） | ダッシュボードをリロード | 各セクション/パネルが**フェードイン**（軽く時間差スタッガ・約0.3秒）。**投票やフォロー解除などの操作では登場アニメは再生されない**（load 時のみ）。※旧 CSS の「下から持ち上がり」は framer 一本化で opacity フェードに変更（未投票/フォローカード個別は従来どおり下から少し持ち上がる） | 未確認 |
| GF-AC-021 | reduce motion 尊重（#3） | 「視差効果を減らす」ON でリロード | 登場アニメは**無効**＝各要素が**即表示**（隠れたまま/チラつきが無い） | 未確認 |

## 増分 #4：XP バーのシャイン（SC-01 ヒーロー）

| ID | 対象 | 確認手順 | 期待する見た目/挙動 | 状態 |
|---|---|---|---|---|
| GF-AC-030 | XP バーの光沢走査 | ダッシュボードのヒーロー XP バーを眺める | 充填部を**周期的に白い光が左→右へ走る**（約2.6秒間隔）。光はバーの充填幅内に収まる（はみ出さない） | OK |
| GF-AC-031 | reduce motion 尊重（#4） | 「視差効果を減らす」ON | シャイン走査が**無効**（静的なバー） | OK |

## 増分 #5：クイック投票の押下バースト（SC-01 未投票カード）

| ID | 対象 | 確認手順 | 期待する見た目/挙動 | 状態 |
|---|---|---|---|---|
| GF-AC-040 | 投票クリックの火花＋カードのふわっと退場 | ダッシュボードの「未投票」カードで **▲賛成 / ▼反対** をクリック（最後の1枚も試す） | クリック位置から **✦の火花が6方向へ ~0.6秒**飛ぶ。**投票したカードは即消えず ~0.6秒かけてフェード（少し縮む）**して退場＝火花が常にそのカード上に重なり、**繰り上がりに火花が乗ったり空スペースに残ったりしない**（framer-motion `AnimatePresence popLayout`） | 未確認 |
| GF-AC-042 | 残りカードの繰り上がりが滑らか（framer-motion layout） | 複数ある「未投票」で先頭〜中間のカードに投票 | 投票カードがフェード退場する**のと同時に、残りの未投票カードが新しい位置へスッと移動して詰まる**（カクッと瞬間移動しない）。登場も下から少し持ち上がりつつ順にフェードイン | 未確認 |
| GF-AC-041 | reduce motion 尊重（#5） | 「視差効果を減らす」ON でクリック | 火花は**出ず**、退場フェード・繰り上がりの移動アニメも**なく即座に入れ替わる**（投票自体は正常に動く） | 未確認 |

> 補足＝火花は**見た目だけ**で投票の成否に影響しません（失敗時はカードが戻ります）。未投票カードが無い場合は、別クエストで公開アイデアに未投票の状態を用意すると確認できます。

## 増分 #6：実績アンロック祝福（SC-40 実績/バッジ）

> 対象＝`/achievements`（実績画面）。前回この画面で観測した「獲得済み実績 id 群」を localStorage（`iq:seenAch:<accountId>`・アカウント別）に保存し、次回**新規に増えた獲得実績**を中央で祝福する（純ロジック＝`features/achievements/celebrate.ts`・G-TC-150）。#2 レベルアップと同型。
> **発火トリック**＝実際に新実績を獲得しなくても確認可＝DevTools Console で `iq:seenAch:<accountId>` の配列から**獲得済み id を1つ削除**（例 `localStorage.setItem("iq:seenAch:"+ACCOUNT_ID, JSON.stringify(["適当に1つ減らした配列"]))`）→ `/achievements` を再読込。※実績が1つも獲得済みでない状態では発火しない（新規解放が無いため）。

| ID | 対象 | 確認手順 | 期待する見た目/挙動 | 状態 |
|---|---|---|---|---|
| GF-AC-050 | 祝福オーバーレイの表示 | 上記トリックで発火（獲得済み id を1つ減らして再読込） | 画面中央に **「ACHIEVEMENT UNLOCKED!」** カードがポップイン、解放された実績の**アイコン＋名称**（複数なら列挙）＋「実績を獲得しました」。✦の火花・金色（コイン色）のオーラ。約3.2秒で自動消滅 | OK |
| GF-AC-051 | クリックで即閉じ | 祝福表示中にカード/背景をクリック | すぐ閉じる | OK |
| GF-AC-052 | 誤発火しない（初回・変化なし） | localStorage の当該キーを**削除**して `/achievements` を再読込／その後もう一度再読込 | 1回目（初回観測）＝**祝福は出ない**（現在の獲得を一斉祝福しない・記録のみ）。2回目（差分なし）＝**出ない** | OK |
| GF-AC-053 | reduce motion 尊重（#6） | 「視差効果を減らす」ON で発火 | カード/火花/オーラの**アニメが無効**＝静的にカード表示（火花は非表示）・自動消滅は機能 | OK |

> 補足＝アカウント別スコープ（キーに account_id）は #2（GF-AC-013）と同機構。祝福は**見た目だけ**で獲得状態や残高に影響しません（表示は `GET /achievements` の実データ）。

## 増分 #7：ヘッダーの微演出（全画面・SC 共通ヘッダー）

> 対象＝認証後の全画面で共用する共通ヘッダー（`components/layout/AppHeader.tsx`）。純視覚＝データ属性駆動の CSS（`design-system.css` の `.bell__icon` / `.pixel-stat.coin[data-bump]`）。純ロジック無し（#3/#4/#5 と同じく GF-AC のみ）。未読数は既存の realtime state、コイン残高は既存の `balance.coin` を利用。

| ID | 対象 | 確認手順 | 期待する見た目/挙動 | 状態 |
|---|---|---|---|---|
| GF-AC-060 | 未読ありでベルが揺れる | 未読通知がある状態（`unread>0`＝ベルに赤バッジ）でヘッダーを眺める | 🔔 の絵文字だけが **周期的に軽く振れる**（約2.8秒間隔でチリンと一振り）。**赤バッジは揺れず固定**。未読0では揺れない | 未確認 |
| GF-AC-061 | コイン変化時のパルス | コインが増減する操作（例：ショップで購入／実績・投稿等で獲得）後にヘッダーのコインチップ `◆ N` を見る | 残高が変わった**瞬間だけ**チップが一度 **ぷるっと拡大＋金色のリング**（約0.7秒）。その後は静止。ページ再読込直後（初回描画）は光らない | 未確認 |
| GF-AC-062 | 狭幅（メニュー内）でも効く | ウィンドウを狭めてアバターメニューを開き、コインが変化する操作を行う | メニュー内のコインチップも同様にパルスする | 未確認 |
| GF-AC-063 | reduce motion 尊重（#7） | 「視差効果を減らす」ON | ベルの揺れ／コインのパルスが**無効**（バッジ・残高表示自体は正常） | 未確認 |

> 補足＝いずれも**見た目だけ**でデータ・遷移に影響しません（ベルは通知一覧へのリンク・コインはショップへのリンクのまま）。

## 増分 #8：投票の獲得フィードバック「+5 XP」（SC-01・段階ハイブリッド step1）

> 対象＝ダッシュボードの**クイック投票**（「未投票のアイデア」カードの ▲賛成 / ▼反対）。投票応答 `xp_awarded` が真（＝サーバーが実際に XP を付与＝初回・日次上限5/日内）のときだけ、クリック地点に「+5 XP」を出し、ヒーローの **XP バーを +5 分だけ楽観的に前進＋一瞬グロー**（連動）。純ロジック＝`features/dashboard/xpAward.ts`（`bumpedXpPct`/`VOTE_XP`・I-TC-152）。金額 +5 は**当面 frontend 定数**（暫定・将来 backend の `xp_delta` へ寄せる＝step2）。backend 変更なし・純加算的な視覚。
> **注意点**＝XP は**楽観表示**（server 権威の正値は次のダッシュボード読込で整合）。上限超過や2回目以降の同一アイデア投票は `xp_awarded=false` になり**フロートもバー前進も出ない**（正しくゲート）。前進はレベルアップを詐称せず現レベルバー内で最大 100% にクランプ。

| ID | 対象 | 確認手順 | 期待する見た目/挙動 | 状態 |
|---|---|---|---|---|
| GF-AC-070 | 「+5 XP」フロート | 「未投票のアイデア」で ▲賛成 / ▼反対 をクリック（初回・上限内） | クリック位置から **「+5 XP」が上へふわっと上昇して ~1秒で消える**（青系・#5 の火花と同時に出る） | 未確認 |
| GF-AC-071 | XP バー連動（前進＋グロー） | 同上の直後にヒーローの XP バーを見る | バーが **+5 分だけ右へ伸び**、伸びた瞬間に**白いグローが一瞬**走る。NEXT/ツールチップの獲得 XP も +5 反映 | 未確認 |
| GF-AC-072 | 付与されない時は出ない | 同一アイデアに2回目投票（切替）／日次上限（5票/日）超過後に投票 | **フロートもバー前進も出ない**（`xp_awarded=false`＝サーバーが付与しないため）。投票の切替自体は正常 | 未確認 |
| GF-AC-073 | 上限クランプ（レベルアップ詐称なし） | XP バーが満杯付近の状態で複数回付与 | バーは **100% を超えない**（レベルアップ演出は勝手に出ない＝#2 は実レベル変化時のみ） | 未確認 |
| GF-AC-074 | reduce motion 尊重（#8） | 「視差効果を減らす」ON で投票 | フロート／バーのグローが**無効**（投票・楽観リスト除外は正常。バー幅の更新は即時） | 未確認 |

> 補足＝金額はサーバー応答 `xp_delta` を表示（step2 で backend delta に一本化＝frontend の暫定定数 `VOTE_XP` は撤去済み）。

### 増分 #8 step2：投稿公開・評価確定の獲得フィードバック（backend delta・SC-21/22・SC-25）

> step2＝投票/投稿公開/評価確定の応答に**実際に付与した XP（`xp_delta`）**を載せ、frontend はその実額を表示する（金額の正はサーバー＝D 台帳 vote=+5/idea_post=+50・F evaluation=+30）。投稿公開・評価確定は各画面の既存の**報酬スナックバー**を実額で駆動（従来のハードコード「＋50/＋30」を撤去）。投票（ダッシュボード）は #8 step1 のフロート＋バー連動を実額で駆動（GF-AC-070..074・見た目は不変）。付与先が別人の選定(+200)/評価連動コインは対象外（本人の獲得のみ）。

| ID | 対象 | 確認手順 | 期待する見た目/挙動 | 状態 |
|---|---|---|---|---|
| GF-AC-075 | 投稿公開の報酬（実額） | アイデアを新規作成して「投稿する」（初回公開） | 「アイデアを投稿しました」の**報酬スナックバーに「＋50 XP」**（サーバーの実付与額）。以前のハードコードでなく応答 `xp_delta` 由来 | 未確認 |
| GF-AC-076 | 評価確定の報酬（実額＋冪等） | 評価を全観点＋総評で「確定」→ 同じ評価をもう一度「確定」 | 初回確定は「評価を確定しました」＋**「＋30 XP」**／2回目（再確定・冪等）は**XP を出さず**「評価を更新しました」（従来は再確定でも誤って「＋30 XP」と表示していた＝解消） | 未確認 |
| GF-AC-077 | 投票の実額（step1 と連続） | ダッシュボードのクイック投票（初回／上限超過後） | 「+N XP」は**サーバー実額**（初回 +5・上限超過や2回目は付与なしで**出ない**）。step1 の見た目のまま金額源だけ server に一本化（暫定定数を撤去） | 未確認 |

> 補足＝表示額はすべてサーバーの実付与額です。ズレて見えたら指摘してください（金額のルールは backend が単一の正）。購入（コイン消費）の「−N ◆」演出は本 step のスコープ外（獲得＝gain のフィードバックに限定）。

## 増分 #9：クリック要素の押下マイクロ演出（全画面・純CSS）

> 対象＝**リンク/ボタンになっているカード**と**ダッシュボードのショートカットタイル**。既存の押下（ボタンの沈み等）に加え、カーソルを乗せると軽く**浮き上がる**立体感を横断で付与（`design-system.css` の `a.card`/`button.card`・`dashboard.css` の `.tile`）。純CSS・純ロジック無し＝GF-AC のみ（#3/#4/#5/#7 と同型）。静的パネル（`.card` の `div`/`article`＝未投票/フォロー中カードなど、カード自体はクリック対象でないもの）は**浮かない**（クリック可能なものだけ反応）。

| ID | 対象 | 確認手順 | 期待する見た目/挙動 | 状態 |
|---|---|---|---|---|
| GF-AC-080 | クリック可能カードの hover リフト（全画面） | ダッシュボードの「下書き」「参加中クエスト」カード／クエスト一覧のカード／魔法（SC-32）のカードにカーソルを乗せる→クリック | ホバーで**数px 上に浮き＋影が濃くなる**（約0.12秒）。クリックで**沈む**。未投票/フォロー中の“操作ボタン内包カード”は浮かない（クリック対象でないため） | 未確認 |
| GF-AC-081 | ショートカットタイルの hover リフト＋絵文字バウンス | ダッシュボード下段のショートカット（クエスト一覧/ショップ/…）にカーソルを乗せる | タイルが浮き上がり、**絵文字がぷるっと拡大（バウンス）**。クリックで沈む | 未確認 |
| GF-AC-082 | reduce motion 尊重（#9） | 「視差効果を減らす」ON | 浮き上がり/バウンスが**無効**（背景・影の色変化は残り、遷移は成立） | 未確認 |

> 補足＝「もっと浮かせて/控えめに」等の強さの調整、対象カードの追加希望（例：通知カード・アイデア一覧行）も歓迎＝次の微調整で拾います。

## 増分 #10：チャット魔法「発動の瞬間」の演出（SC-24）

> 対象＝アイデアチャット（SC-24）で自分が**魔法を発動**した瞬間（リアクションの魔法＝`castSpell` 成功時）。対象メッセージ矩形に**エフェクト種別ごとの one-shot 演出**を重ね（`SpellCastFx`・座標固定オーバーレイ）、そのまま従来の**永続エフェクト**（`spell-fx--*`）に着地する。純CSS＋座標オーバーレイ・純ロジック無し＝GF-AC のみ。**自分の発動のみ**（他ユーザーの魔法は従来どおり永続エフェクトのみ・realtime バーストは後回し）。あわせて `FX` マップを修正し **rainbow/aura が各々の見た目**になる（従来は sparkle/ice に畳まれていた）。

| ID | 対象 | 確認手順 | 期待する見た目/挙動 | 状態 |
|---|---|---|---|---|
| GF-AC-090 | 発動の瞬間演出（炎） | チャットで解放済みの**炎系**魔法をメッセージに発動 | 対象メッセージから**🔥が噴き上がり・暖色フラッシュ・リング**が一撃（約1秒）→そのまま枠が燃える永続エフェクトに着地 | 未確認 |
| GF-AC-091 | 種別ごとに見た目が違う | 雷/氷/きらめき/虹/オーラ系をそれぞれ発動 | 雷＝黄のストロボ＋⚡／氷＝青＋❄／きらめき＝紫✨／虹＝🌈／オーラ＝🔮＋広がるリング…と**種別ごとに色・動きが変わる**（虹・オーラも各々の見た目＝以前の畳み込み解消） | 未確認 |
| GF-AC-092 | 永続エフェクトへの着地 | 発動後にそのメッセージを見る | 一撃演出が消えた後は**従来の永続 `spell-fx`**（枠の燃え/グロー等）が残る（発動演出は重複で出ない・二度押しは 409 で発動しない） | 未確認 |
| GF-AC-093 | reduce motion 尊重（#10） | 「視差効果を減らす」ON で発動 | 発動の瞬間演出は**出ない**（魔法の付与・永続エフェクトの静的色味は従来どおり） | 未確認 |

> 補足＝発動演出は**見た目だけ**で、魔法の付与/取消・集計には影響しません（サーバー権威）。他ユーザーの発動も光らせたい（realtime バースト）等の要望は次増分で検討します。

## 設定：アニメーション演出の ON/OFF（プロフィール・デザイン標準 §4.9）

> ユーザー別にアニメ演出を抑制できる設定（`accounts.reduce_motion`）。**実効 = OS「視差効果を減らす」 OR この設定**（OS が最優先の下限＝OS が ON なら設定に関係なく常に抑制・設定では ON に戻せない）。プロフィール画面（`/profile`）の「アニメーション演出」チェックボックス（＝動きを減らす）で切替→保存。純ロジックは vitest（K-TC-021）、backend は K-TC-020 で担保。

| ID | 対象 | 確認手順 | 期待する見た目/挙動 | 状態 |
|---|---|---|---|---|
| GF-AC-100 | 設定で全演出 OFF | `/profile` で「動きを減らす」を**チェック→保存**→ダッシュボード等を操作 | カウントアップ・XPバー充填・登場アニメ・祝福・バースト・魔法発動・ヘッダー演出・カードのリフト…**#1〜#10 のすべてが止まる**（情報＝数値/バー/バッジは残る） | 未確認 |
| GF-AC-101 | 設定で全演出 ON（既定） | 「動きを減らす」を**外して保存** | 演出が**元どおり有効**に戻る（OS が reduce の場合は OS 優先で止まったまま） | 未確認 |
| GF-AC-102 | OS が最優先（設定で上書き不可） | OS の「視差効果を減らす」を **ON** にした状態で、設定の「動きを減らす」を**外す**（＝ON にしようとする） | 演出は**出ない**（OS が最優先の下限＝設定では ON に戻せない） | 未確認 |
| GF-AC-103 | 設定の永続と全画面反映 | 保存後にリロード／別画面へ遷移 | 設定が保持され、**全画面**（ヘッダー含む）で一貫して抑制/有効が反映される | 未確認 |

## 増分 #11：魔法解放の祝福（SC-32 魔法/スキル）

> 対象＝`/spells`（魔法/スキル画面）。SP を消費して魔法を**解放した瞬間**に、解放したカードへ**その魔法の signature エフェクト**を one-shot で弾く（#10 と共通の `SpellCastFx`＝`@/components/ui`・種別テーマ）。SP 残高は #1 の `CountUp` で数え上げ/減少。純CSS＋座標オーバーレイ・純ロジック無し＝GF-AC のみ。**自分の解放時のみ**。SP 消費の手応えは既存の報酬スナックバー「✦ -N SP」＋SP 数値の減少で担保。

| ID | 対象 | 確認手順 | 期待する見た目/挙動 | 状態 |
|---|---|---|---|---|
| GF-AC-110 | 解放の瞬間演出（種別テーマ） | `/spells` で解放可能な魔法（SP 充足・前提解放済み）を「解放する」 | 解放したカードから**その魔法のエフェクトが一撃**（炎=🔥噴き上げ／雷=⚡ストロボ／氷=❄／きらめき=✨／虹=🌈／オーラ=🔮＋リング…約1秒）。カードは「✓ 解放済み」に変わる | 未確認 |
| GF-AC-111 | SP のカウント演出 | 画面を開く／魔法を解放した直後に「✦ N」の SP を見る | 開いた時 0→現在値へ数え上がり、解放時は**消費分だけ数字が減る**（`CountUp`） | 未確認 |
| GF-AC-112 | reduce motion 尊重（#11） | 「視差効果を減らす」ON（または設定でアニメ OFF）で解放 | 解放の瞬間演出は**出ない**／SP のカウントは即最終値（解放・SP 消費・スナックバーは正常） | 未確認 |

> 補足＝演出は**見た目だけ**で解放の成否・SP 消費に影響しません（サーバー権威）。解放は恒久（取り消し不可）。前提未解放/SP 不足はボタンが無効表示。

## 増分 #12：ショップ購入の演出（SC-30 ショップ）

> 対象＝`/shop`。装備を**購入した瞬間**に、そのカードへ**アイテム入手のポップ祝福**（アイコンが弾ける＋きらめき＋リング＋「◆ -価格」の消費フロート）を one-shot で出す（`ItemGetFx`）。コイン残高は #1 の `CountUp` で減少表示。ヘッダーのコイン pulse（#7）も変化で効く。純CSS＋座標オーバーレイ・純ロジック無し＝GF-AC のみ。既存の `.just-bought`（軽いカードポップ）＋報酬スナックバー「◆ -N」も継続。

| ID | 対象 | 確認手順 | 期待する見た目/挙動 | 状態 |
|---|---|---|---|---|
| GF-AC-120 | 入手のポップ祝福 | `/shop` で購入可能な装備を「購入する」→確認で「はい」 | 購入したカードから**アイテムのアイコンが大きくポップ**し、金色のきらめき/リングが弾け、「**◆ -価格**」が上へ流れて消える（約1秒）。カードは「✓ 所有済み」に変わる | 未確認 |
| GF-AC-121 | コイン残高の減少演出 | 購入直後にヒーローの「◆ N」を見る／ヘッダーのコインを見る | ヒーローのコインが**消費分だけ数字が減る**（`CountUp`）／ヘッダーのコインチップも変化で pulse（#7） | 未確認 |
| GF-AC-122 | reduce motion 尊重（#12） | 「視差効果を減らす」ON（または設定でアニメ OFF）で購入 | 入手ポップ演出は**出ない**／コインは即最終値（購入・所有反映・スナックバーは正常） | 未確認 |

> 補足＝演出は**見た目だけ**で購入の成否・コイン消費に影響しません（サーバー権威）。残高不足/所有済みはサーバーが 409・ボタンは残高不足で無効表示。

## 増分 #13：週間ランキングの演出（SC-41 ランキング）

> 対象＝`/ranking`。表彰台のせり上がり登場・メダルのきらめき・自分の順位カード/行の登場グロー・スコアのカウントアップ。SC-41 固有に限定（`.rank-panel.full` 配下）でダッシュボードのミニランキングには波及しない。期間タブ切替でも登場演出を再生（`key=period`）。純CSS＋#1 の `CountUp`・純ロジック無し＝GF-AC のみ。

| ID | 対象 | 確認手順 | 期待する見た目/挙動 | 状態 |
|---|---|---|---|---|
| GF-AC-130 | 表彰台の登場＋メダルきらめき | `/ranking` を開く／期間タブ（今週/先週/今月/通算）を切替 | TOP3 の表彰台が**下からせり上がって登場**（中央=1位が先・左右が少し遅れて）。🥇🥈🥉 と一覧の上位メダルが**周期的にポンと輝く** | 未確認 |
| GF-AC-131 | あなたの順位カード/行の登場グロー | 同上（自分がランクインしている期間） | 「あなたの順位」カードが**淡い青グローで登場**。一覧の**自分の行**が登場時に**ハイライト**して落ち着く。「▼ 自分の順位へ」で自分の行へスクロール＋一瞬光る | 未確認 |
| GF-AC-132 | スコアのカウントアップ | 開いた時／期間切替時に表彰台と「あなたの順位」のスコアを見る | スコアが **0→値へ数え上がる**（`CountUp`）。期間切替で新しい値へ数え直す | 未確認 |
| GF-AC-133 | reduce motion 尊重（#13） | 「視差効果を減らす」ON（または設定でアニメ OFF） | 登場せり上がり・メダルきらめき・グロー・カウントアップが**無効**＝即最終状態で表示（順位・数値・タブ切替は正常） | 未確認 |

> 補足＝演出は**見た目だけ**で順位/スコア（サーバー集計）に影響しません。ダッシュボードのミニ週間ランキングには本演出は掛かりません（SC-41 限定）。

## 増分 #14：アバター装備の演出（SC-31 アバター/着せ替え）

> 対象＝`/avatar`。所有装備をクリックして**装着した瞬間**に、アバタープレビューの該当スロット（頭/顔/手持ち）で**装備アイコンがポップ＋sparkle リング**（内側の keyed 要素）、さらに**クリックした装備カードが一瞬フラッシュ**。純CSS・純ロジック無し＝GF-AC のみ。※体はアバター画像そのもの・背景はスロット位置が無いためポップは頭/顔/手持ちに限定。

| ID | 対象 | 確認手順 | 期待する見た目/挙動 | 状態 |
|---|---|---|---|---|
| GF-AC-140 | 装着のポップ演出 | `/avatar` で頭/顔/手持ちの**所有装備**をクリック（着せ替え） | アバタープレビューの該当位置で**アイコンがぷるっとポップ＋白い sparkle リング**が弾ける（約0.6秒）。クリックした**装備カードが一瞬フラッシュ**して「装備中」に | 未確認 |
| GF-AC-141 | 外す/切替の妥当性 | 「外す」をクリック／別装備に切替 | 「外す」では**ポップは出ない**（アイコンが消えるだけ・誤爆なし）。別装備への切替は新しいアイコンでポップする | 未確認 |
| GF-AC-142 | reduce motion 尊重（#14） | 「視差効果を減らす」ON（または設定でアニメ OFF）で着せ替え | ポップ/リング/カードフラッシュが**無効**＝即座に装備反映（着せ替え自体は正常） | 未確認 |

> 補足＝演出は**見た目だけ**で装備状態（サーバー権威・各スロット1点）に影響しません。未所有はショップ導線（🔒）で装着対象外。

## 増分 #15：通知の新着 pop（全画面・共通ヘッダー・リアルタイム）

> 対象＝認証後 全画面のヘッダーのベル。**新着通知が届いた瞬間**（realtime の `notification.created` で**未読数が増えた**時）に、ベルが**ポンと跳ねる**（バウンス＋赤いグローの明滅＋バッジのポップ）。#7 の「未読>0 で常時わずかに揺れる」とは別の**一撃演出**。純CSS＋データ属性駆動（`data-arrived`）・純ロジック無し＝GF-AC のみ。**未読が増えた時だけ**（既読で減った時は跳ねない・初回描画も跳ねない）。
> **発火条件＝リアルタイムの新着**：自分では起こしづらいので、**別ユーザー/別セッション**が「あなた宛の通知」を発生させる操作（あなたのアイデアへコメント/投票/評価、フォロー中アイデアの更新 等）で確認するのが確実。

| ID | 対象 | 確認手順 | 期待する見た目/挙動 | 状態 |
|---|---|---|---|---|
| GF-AC-150 | 新着でベルが跳ねる | 別ユーザー/別セッションから、あなた宛の通知が発生する操作を行う（未読数が増える） | ヘッダーのベルが**一度ポンと跳ねる**（約0.6秒・バウンス＋赤いグロー＋バッジがポップ）。未読バッジの数字も増える | 未確認 |
| GF-AC-151 | 既読/初回は跳ねない | 通知一覧を開いて既読にする（未読が減る）／画面を普通にリロード | 減少時・初回描画では**跳ねない**（#7 の常時わずかな揺れは未読>0 の間だけ継続） | 未確認 |
| GF-AC-152 | reduce motion 尊重（#15） | 「視差効果を減らす」ON（または設定でアニメ OFF）で新着 | ベルの跳ね/グロー/バッジポップが**無効**（未読数の更新・遷移は正常） | 未確認 |

> 補足＝演出は**見た目だけ**で通知の受信/既読（サーバー権威）に影響しません。未読数は realtime（WS）で更新されます。

## 増分 #16：クエスト選定の祝福（SC-22 アイデア詳細）

> 対象＝アイデア詳細（`/ideas/{id}`）。**選定権限（owner/quest_admin）**を持つ人が「☆ このアイデアを選定」を押して**選定が成立した瞬間**に、中央へ祝福オーバーレイ（👑＋「SELECTED!」＋アイデア名＋「投稿者へ ✦+200 XP」）を約2.8秒表示（クリックで即閉じ）。純CSS＋座標なし中央オーバーレイ・純ロジック無し＝GF-AC のみ。**選定成立時のみ**（解除では出さない）。純装飾のため **reduce-motion 時は出さず**、従来の成功スナックバーで通知（情報は残す）。
> **確認の前提**＝選定権限のあるユーザー（クエスト owner／quest_admin）で、公開済みアイデアの詳細を開く（選定ボタンが出る）。

| ID | 対象 | 確認手順 | 期待する見た目/挙動 | 状態 |
|---|---|---|---|---|
| GF-AC-160 | 選定成立の祝福 | 選定権限のあるアイデア詳細で「☆ このアイデアを選定」を押す | 中央に **👑＋「SELECTED!」＋アイデア名＋「投稿者へ ✦+200 XP」** のカードがポップイン（金色オーラ＋✦火花）。約2.8秒で自動消滅・クリックで即閉じ。ボタンは「★ 選定済み（解除）」に | 未確認 |
| GF-AC-161 | 解除では出ない | 「★ 選定済み（解除）」を押して選定を解除する | 祝福は**出ない**（「選定を解除しました」のスナックバーのみ）。再度選定すると再び祝福 | 未確認 |
| GF-AC-162 | reduce motion 尊重（#16） | 「視差効果を減らす」ON（または設定でアニメ OFF）で選定 | 祝福オーバーレイは**出ない**（成功スナックバー「アイデアを選定しました。投稿者にコイン・XP を付与しました。」で通知・選定自体は正常） | 未確認 |

> 補足＝演出は**見た目だけ**で選定状態・XP/コイン付与（サーバー権威）に影響しません。完了クエストや権限なしはサーバーが 409/403。

## 増分 #17：チャットの手触り（SC-24 アイデアチャット）

> 対象＝アイデアチャット（`/ideas/{id}/chat` 等）。**新着メッセージが下からフェードで登場**・**リアクション（通常/魔法）の追加時にポップ**。メッセージは `key=id` なので**新着だけが登場アニメ**（既存メッセージ/カウント更新は再生しない）。純CSS・純ロジック無し＝GF-AC のみ。#10 の魔法発動演出と併用。

| ID | 対象 | 確認手順 | 期待する見た目/挙動 | 状態 |
|---|---|---|---|---|
| GF-AC-170 | 新着メッセージの登場 | チャットでメッセージを送信する／別ユーザーの投稿を受信する（realtime） | 新しいメッセージが**下から少し持ち上がりつつフェードイン**（約0.28秒）。**既存メッセージは再アニメしない**（初回読込は各メッセージが一度だけ軽くフェード） | 未確認 |
| GF-AC-171 | リアクション追加のポップ | メッセージに絵文字リアクション/魔法を付ける | 付けたリアクションが**ぷるっとポップして現れる**（約0.3秒）。既存リアクションのカウント更新ではポップしない | 未確認 |
| GF-AC-172 | reduce motion 尊重（#17） | 「視差効果を減らす」ON（または設定でアニメ OFF） | メッセージ登場/リアクションのポップが**無効**＝即表示（送信・受信・リアクション自体は正常） | 未確認 |

> 補足＝演出は**見た目だけ**でメッセージ/リアクション（サーバー権威）に影響しません。押下 `:active` の沈み込みは維持（リアクションのポップは fill を付けず干渉させない）。

## 増分 #18：取得中のゲーム化（ローディング表示）

> 対象＝**ゲーム層＋主要コンテンツ画面の「読み込み中…」表示**を、素の文字から**ブランドのコインスピナー**（`Spinner`＝◆が回る＋ラベル・デザイン標準 §13）へ統一。対象画面＝ショップ(SC-30)／魔法(SC-32)／アバター(SC-31)／実績(SC-40)／ランキング(SC-41)／アイデア詳細(SC-22)／チャット(SC-24)／評価(SC-25)。業務層の管理画面（アカウント/会社/クエスト管理 等）は §13 の「業務層はクリーン」に従い**据え置き**。純CSS/既存部品・純ロジック無し＝GF-AC のみ。

| ID | 対象 | 確認手順 | 期待する見た目/挙動 | 状態 |
|---|---|---|---|---|
| GF-AC-180 | コインスピナー表示 | 上記の各画面を開いた直後（取得中）を見る（低速回線やリロード直後が分かりやすい） | 素の「読み込み中…」ではなく、**◆コインが回るスピナー＋「読み込み中…」**が出る（ブランド統一） | 未確認 |
| GF-AC-181 | reduce motion 尊重（#18） | 「視差効果を減らす」ON（または設定でアニメ OFF）で取得中を見る | コインの**回転が止まる**（ラベルは表示・取得完了で通常表示に切替） | 未確認 |

> 補足＝表示だけの変更で取得処理には影響しません。管理系（業務層）の一覧は従来のクリーンな文字表示のままです（意図的・§13）。

## 増分 #19：空状態の演出（EmptyState）

> 対象＝一覧/パネルが空のときの表示を、素の文字から**共通の `EmptyState`（絵文字アイコンがゆるく上下に揺れる＋一言）**へ。寂しさを解消。適用＝チャット（コメントなし）／ランキング（期間データなし）／通知（絞り込み該当なし）／チームアクティビティ（活動なし）／ショップ・実績（絞り込み該当なし）。純CSS（`mascot-bob` 再利用）・純ロジック無し＝GF-AC のみ。情報は文言に残す。

| ID | 対象 | 確認手順 | 期待する見た目/挙動 | 状態 |
|---|---|---|---|---|
| GF-AC-190 | 空状態のマスコット＋一言 | 上記のいずれかを空にする（例＝コメントの無いアイデアのチャット／該当0件になる絞り込み／新規アカウントで通知なし 等） | 素の文字ではなく、**絵文字アイコン（ゆるく上下に揺れる）＋見出し（＋補足）**が中央に出る（例＝💬「まだコメントはありません」/📊「この期間のランキングデータがありません」/🛒「条件に合う装備がありません」） | 未確認 |
| GF-AC-191 | reduce motion 尊重（#19） | 「視差効果を減らす」ON（または設定でアニメ OFF） | アイコンの**上下の揺れが止まる**（アイコン・文言は表示） | 未確認 |

> 補足＝表示だけで取得結果には影響しません。管理系（業務層）の空表示は据え置き（意図的・§13）。

## 増分 #20：3Dアバターのマスコット追従（SC-01 ダッシュボード限定）

> 対象＝ダッシュボード（`/`）。**ホバーしたカードの右上へ、小さな3Dアバターがスプリングで飛んで追従**する（§4 マスコットの3D版＝今どこでも使われていない3Dアバターに役割を持たせる）。中身は `AvatarViewer3D`（現状はプレースホルダのプリミティブ／将来 VRM に差し替え＝`doc/フェーズ毎ルール/3Dアバター.md`）。ユーザーの `avatar_base`（男女）を反映。**ダッシュボード限定**（他画面には出さない）・`pointer-events:none` でクリックを妨げない。純視覚・純ロジック無し＝GF-AC のみ。**WebGL 非対応/アニメ抑制（OS reduce OR ユーザー設定）では出さない**（純装飾）。

| ID | 対象 | 確認手順 | 期待する見た目/挙動 | 状態 |
|---|---|---|---|---|
| GF-AC-200 | カードへの3D追従 | ダッシュボードで各カード（下書き/参加中クエスト/ショートカット等）にカーソルを乗せ、別カードへ移す | ホバーしたカードの**右上に小さな3Dアバター**が現れ（回転）、別カードへ移すと**スプリングで飛んで追従**。ダッシュボード外へ出ると消える | 未確認 |
| GF-AC-201 | 範囲限定・非妨害 | 他画面（一覧/詳細等）を開く／カード上のリンク・ボタンをクリック | 3Dアバターは**ダッシュボードだけ**に出る（他画面では出ない）。カードの**クリックは妨げない**（pointer-events:none） | 未確認 |
| GF-AC-202 | WebGL/reduce motion 尊重（#20） | WebGL 非対応環境／「視差効果を減らす」ON（または設定でアニメ OFF） | 3D追従は**出ない**（ダッシュボードの他機能は正常）。＝純装飾のため非表示 | 未確認 |

> 補足＝3Dアバターは**現状プレースホルダ**（球＋カプセルの見立て）。実 VRM への差し替えは別途（`doc/フェーズ毎ルール/3Dアバター.md`）。差し替え後は本追従にもそのまま反映される。演出は見た目だけでデータに影響しません。

## 増分 #21：レベル称号＆オーラ（SC-01 ヒーロー）

> 対象＝ダッシュボードのヒーロー。レベルに応じた**称号**（見習い→駆け出し→一人前→熟練→達人→英雄→伝説）と、レベル帯（ティア）に応じた**ヒーローの色オーラ**＋称号チップの色。純ロジック＝`features/dashboard/levelTitle.ts`（`levelRank`・I-TC-153）。しきい値/名称は初期値（後で調整可）。純CSS＋vitest・GF-AC は見た目のみ。

| ID | 対象 | 確認手順 | 期待する見た目/挙動 | 状態 |
|---|---|---|---|---|
| GF-AC-210 | 称号の表示 | ダッシュボードのヒーロー（Lv 表示の隣）を見る | レベルに応じた**称号チップ**が出る（例＝Lv.60→「英雄」／Lv.5→「駆け出し」／Lv.1→「見習い」）。しきい値＝1/5/10/20/35/50/70 | 未確認 |
| GF-AC-211 | レベルオーラ（色） | 同上（アバター枠と称号チップの色） | レベル帯（ティア）で**アバター枠に色オーラ**が灯り、称号チップも同色（駆け出し=緑/一人前=青/熟練=紫/達人=金/英雄=赤/伝説=黄）。オーラはゆっくり脈打つ | 未確認 |
| GF-AC-212 | reduce motion 尊重（#21） | 「視差効果を減らす」ON（または設定でアニメ OFF） | オーラの**脈動が止まる**（色・称号は表示） | 未確認 |

> 補足＝称号/色は表示だけでレベル・XP（サーバー権威）に影響しません。しきい値・名称は初期値なので「刻みを変えたい/名称を変えたい」等は歓迎（次の微調整で拾います）。

## 増分 #22：評価の星採点の手触り（SC-25 評価画面）

> 対象＝評価画面（`/ideas/{id}/eval`・SC-22 からのモーダル）の5観点×5点スター採点。**ホバーした星の拡大＋点灯星の金グロー**・**確定時のフィルスイープ・ポップ**（点灯星が左から順に弾ける）。純CSS＋データ属性（`data-pop`）・純ロジック無し＝GF-AC のみ。キーボード操作/ホバープレビューは従来どおり維持。

| ID | 対象 | 確認手順 | 期待する見た目/挙動 | 状態 |
|---|---|---|---|---|
| GF-AC-220 | ホバー拡大＋点灯グロー | 評価画面で観点の星にカーソルを乗せる | ホバー中の星が**ふわっと拡大**し、点灯（採点済み）の星は**金色に淡く光る**。ホバー星より右は拡大しない（プレビューの境界が明快） | 未確認 |
| GF-AC-221 | 確定のフィルスイープ・ポップ | 星をクリック/キーボードで点数を選ぶ | 選んだ点数まで**点灯星が左から順にポン**と弾ける（約0.4秒のスイープ） | 未確認 |
| GF-AC-222 | reduce motion 尊重（#22） | 「視差効果を減らす」ON（または設定でアニメ OFF）で採点 | 拡大/ポップが**無効**＝即座に点灯（採点・キーボード操作は正常） | 未確認 |

> 補足＝演出は**見た目だけ**で採点値（サーバー権威）に影響しません。総評必須・全5観点必須の検証も従来どおり。

## 増分 #23：投票結果の賛否バー（SC-22 アイデア詳細）

> 対象＝アイデア詳細の投票セクション。今までの「▲賛成N ▼反対M」に加え、**賛成(緑)/反対(赤)の比率バー**を可視化＝一目で形勢が分かる。マウント時 0→比率へ伸び、投票の変化で滑らかに伸縮。純ロジック＝`features/ideas/voting.ts`（`votePercents`・D-TC-220）。純CSS＋#1 のバー流儀。

| ID | 対象 | 確認手順 | 期待する見た目/挙動 | 状態 |
|---|---|---|---|---|
| GF-AC-230 | 賛否バーの表示・登場 | 投票のあるアイデア詳細を開く | 賛成/反対数の下に**緑＝賛成・赤＝反対の比率バー**が出て、開いた時に **0→比率へ伸びる**（票が無いアイデアはバー非表示） | 未確認 |
| GF-AC-231 | 投票で伸縮 | 賛成/反対を押す・切り替える | バーの**緑/赤の比率が滑らかに伸縮**して更新（数値も更新） | 未確認 |
| GF-AC-232 | reduce motion 尊重（#23） | 「視差効果を減らす」ON（または設定でアニメ OFF） | バーの伸縮アニメが**無効**＝即座に比率表示（投票自体は正常） | 未確認 |

> 補足＝バーは表示だけで集計（サーバー権威）に影響しません。匿名モードでも賛成/反対の集計数のみ＝バーも集計数ベース（投票者の身元は出しません）。

## 増分 #24：締切の切迫演出（クエスト・SC-01/10/11/12）

> 対象＝クエストの「⏳ 締切」表示（ダッシュボードの参加中クエスト／クエスト一覧・カード／クエスト詳細）。締切までの残日で**段階的に色づき・切迫時は脈打つ**＋**残り日数**を併記。純ロジック＝`lib/deadline.ts`（`deadlineUrgency`/`deadlineCountdown`・C-TC-210）。段階＝safe（8日以上=通常）／soon（3〜7日=橙）／urgent（当日〜2日=赤＋脈動）／over（超過=淡い赤）。

| ID | 対象 | 確認手順 | 期待する見た目/挙動 | 状態 |
|---|---|---|---|---|
| GF-AC-240 | 切迫度の色分け＋残日 | 締切が異なるクエストの ⏳ 締切表示を見る（ダッシュボード/一覧/詳細） | 8日以上=通常色／3〜7日=**橙**／当日〜2日=**赤**／超過=**淡い赤**。safe 以外は「⏳ 日付 ・残りN日／今日締切／締切超過」を併記 | 未確認 |
| GF-AC-241 | 切迫（urgent）の脈動 | 締切が2日以内 or 当日のクエスト | 赤い締切表示が**ゆっくり点滅（脈動）**して注意を引く | 未確認 |
| GF-AC-242 | reduce motion 尊重（#24） | 「視差効果を減らす」ON（または設定でアニメ OFF） | 脈動が**止まる**（色分け・残日表示は維持） | 未確認 |

> 補足＝表示だけで締切/投票締切（サーバー権威）に影響しません（投票の締切後無効化は従来どおり別判定）。日数の境界は初期値（後で調整可）。

## 増分 #25：進捗バーの伸びる演出（実績/評価）

> 対象＝進捗バーが**表示時に 0→現在値へ左から伸びる**（#1 のヒーロー XP バー流儀を各所へ）。適用＝実績（SC-40）のカード進捗バー＋収集サマリーの達成率バー／アイデア詳細（SC-22）の評価結果・観点別平均バー。純CSS（`scaleX(0→1)`・共通 `@keyframes bar-grow`）・純ロジック無し＝GF-AC のみ。データ取得後にバーが出る時に一度再生。

| ID | 対象 | 確認手順 | 期待する見た目/挙動 | 状態 |
|---|---|---|---|---|
| GF-AC-250 | 進捗バーの伸び | 実績（`/achievements`）を開く／アイデア詳細の評価結果を見る | 実績カードの**進捗バー**・収集サマリーの**達成率バー**・評価の**観点別平均バー**が、表示時に**0→現在値へ左から伸びる**（約0.8秒） | 未確認 |
| GF-AC-251 | reduce motion 尊重（#25） | 「視差効果を減らす」ON（または設定でアニメ OFF） | バーの伸びが**無効**＝即座に現在値の長さで表示 | 未確認 |

> 補足＝表示だけで進捗値（サーバー権威）に影響しません。

## 増分 #26：プロフィールのショーケース演出（SC-03）

> 対象＝プロフィール画面（`/profile`）のヒーロー。**登場演出**（アバター・名前・ステータス・アクションが下からフェードで順に現れる）＋**レベル称号チップ＋アバターのレベルオーラ**（#21 の `levelRank` を共通化＝`lib/levelTitle.ts`）＋**コイン/SP のカウントアップ**（#1）＋**XPバーの伸び**（#25）。純CSS＋既存部品・純ロジックは既存（I-TC-153）流用＝GF-AC は見た目のみ。

| ID | 対象 | 確認手順 | 期待する見た目/挙動 | 状態 |
|---|---|---|---|---|
| GF-AC-260 | ヒーローの登場演出 | `/profile` を開く（またはリロード） | アバター→名前→ステータス→アクションが**下から少し持ち上がりつつ順にフェードイン**（スタッガ） | 未確認 |
| GF-AC-261 | 称号/オーラ/カウント/XP伸び | 同上のヒーローを見る | 名前の隣に**レベル称号チップ**（例 Lv.60→英雄）・アバター枠に**レベル色のオーラ**（脈動）・コイン/SP が**0→現在値へカウントアップ**・XPバーが**0→現在%へ伸びる** | 未確認 |
| GF-AC-262 | reduce motion 尊重（#26） | 「視差効果を減らす」ON（または設定でアニメ OFF） | 登場・オーラ脈動・カウント・XP伸びが**無効**＝即座に最終表示（称号・色は表示） | 未確認 |

> 補足＝表示だけでレベル/残高（サーバー権威）に影響しません。称号は #21 と同じ刻み（`lib/levelTitle.ts`）。

## 増分 #27：レアリティの輝き演出（SC-30/31/32）

> 対象＝ショップ（装備）・アバター（ワードローブ）・魔法（スキル）のカード。**レアのカード枠が紫グローで脈打つ**（収集意欲）。標準=青枠・コモン=グレー枠（既存パレット・無地/控えめ）。レア色はデザイン標準の rarity パレット（レア=パープル `--rarity-rare`）に準拠。純CSS＋データ属性（`.card.rarity-*`）・純ロジック無し＝GF-AC のみ。

| ID | 対象 | 確認手順 | 期待する見た目/挙動 | 状態 |
|---|---|---|---|---|
| GF-AC-270 | レアの輝き | ショップ/アバター/魔法でレア（パープル表記）の装備・魔法カードを見る | **レアのカード枠が紫に脈打って光る**（ゆっくり明滅）。標準は青枠・コモンはグレー枠で静か（レアだけ際立つ） | 未確認 |
| GF-AC-271 | reduce motion 尊重（#27） | 「視差効果を減らす」ON（または設定でアニメ OFF） | レアの脈動が**止まる**（レアの紫枠自体は表示） | 未確認 |

> 補足＝表示だけで所有/購入/解放（サーバー権威）に影響しません。レア色は既存の rarity パレット（レア=パープル）。「金色にしたい」等の色変更希望は歓迎（次の微調整で拾います）。

## 増分 #28：ページ遷移の演出（全画面・(app)/template）

> 対象＝認証後の全画面。ページを移動するたびに**本体が軽くフェードインして現れる**（App Router の `template.tsx`＝ナビ毎に再マウント）。ヘッダー等の chrome は残る（遷移しない）。**opacity のみ**＝transform は各種オーバーレイ（バースト/フロート/マスコット追従）の座標を壊すため使わない。純CSS・純ロジック無し＝GF-AC のみ。

| ID | 対象 | 確認手順 | 期待する見た目/挙動 | 状態 |
|---|---|---|---|---|
| GF-AC-280 | ページ遷移フェード | メニューやリンクで画面を移動する（ダッシュボード↔一覧↔詳細 等） | 遷移先のページ本体が**すっとフェードイン**（約0.28秒）。ヘッダーは動かず据え置き | 未確認 |
| GF-AC-281 | reduce motion 尊重（#28） | 「視差効果を減らす」ON（または設定でアニメ OFF） | フェードが**無効**＝即座に表示（遷移自体は正常） | 未確認 |

> 補足＝表示だけで遷移/データに影響しません。opacity のみのため、遷移直後にオーバーレイ演出が出ても座標はずれません。

## 増分 #29：ヘッダーのレベルリング（全画面・共通ヘッダー）

> 対象＝ヘッダー右上のユーザーアバター。**現レベル内の XP 進捗を円環（リング）で表示**（全画面で常に進行感）。レベルアップ間近（進捗 90% 以上）はリングが**脈動**して「あと少し」を示唆。純CSS（conic-gradient）・純ロジック無し＝GF-AC のみ。XP 進捗は `GET /me` 残高から算出（`headerBalance.xpPct`）。Lv バッジ（アバター下）は従来どおり。

| ID | 対象 | 確認手順 | 期待する見た目/挙動 | 状態 |
|---|---|---|---|---|
| GF-AC-290 | レベルリング表示 | ヘッダー右上のアバターを見る（ホバーでツールチップ「次のレベルまで N%」） | アバターの周りに**現レベル内 XP 進捗の円環**（XP色の弧＝進捗ぶん・残りは薄色）。進捗が増えると弧が伸びる | 未確認 |
| GF-AC-291 | 間近の脈動＋reduce motion | 進捗が 90% 以上のとき／「視差効果を減らす」ON | 90% 以上でリングが**脈動**（レベルアップ間近）。reduce-motion（または設定 OFF）では**脈動が止まる**（リング自体は表示） | 未確認 |

> 補足＝表示だけでレベル/XP（サーバー権威）に影響しません。進捗は現レベル内の割合＝ダッシュボード/プロフィールの XP バーと同じ算出。

## 増分 #30：ミニ週間ランキングの演出（SC-01 ダッシュボード）

> 対象＝ダッシュボード右上の**ミニ週間ランキング**（TOP3）。#13（SC-41 フル画面）の**メダルのきらめき＋自分の行の登場ハイライト**をミニ版にも展開（keyframe は design-system.css で共用）。純CSS・純ロジック無し＝GF-AC のみ。SC-41 フルの演出はそのまま。

| ID | 対象 | 確認手順 | 期待する見た目/挙動 | 状態 |
|---|---|---|---|---|
| GF-AC-300 | ミニランキングの演出 | ダッシュボードのミニ週間ランキング（🥇🥈🥉）を見る（自分がTOP3内の週なら自分の行も） | 上位メダルが**周期的にポンと輝く**・**自分の行**が登場時に**ハイライト**して落ち着く（#13 と同じ手触り） | 未確認 |
| GF-AC-301 | reduce motion 尊重（#30） | 「視差効果を減らす」ON（または設定でアニメ OFF） | メダルきらめき・行ハイライトが**無効**（順位・数値は表示） | 未確認 |

> 補足＝表示だけで順位（サーバー集計）に影響しません。

## 増分 #31：時間帯の挨拶（SC-01 ダッシュボード・カテゴリ E 時間/環境の演出）

> 対象＝ダッシュボード最上部に**時間帯で変わる挨拶＋今日の日付**を表示（朝＝おはようございます／昼＝こんにちは／夜＝こんばんは）。挨拶の判定は純ロジック（`lib/greeting.ts`＝I-TC-154）。表示はマウント後に算出（SSR/クライアントの時刻差によるハイドレーション不一致回避）＝差し込み時に**フェードイン**。カテゴリ E（時間・環境の演出）の第1弾。

| ID | 対象 | 確認手順 | 期待する見た目/挙動 | 状態 |
|---|---|---|---|---|
| GF-AC-310 | 時間帯の挨拶 | ダッシュボードを開く（朝5-10時／昼11-17時／夜18-4時で見比べ・端末時刻を変えても可） | 最上部に**時間帯に応じた挨拶＋名前＋今日の日付**が表示され、読み込み時に**すっとフェードイン**する | OK |
| GF-AC-311 | reduce motion 尊重（#31） | 「視差効果を減らす」ON（または設定でアニメ OFF） | フェードインが**無効**（挨拶・日付そのものは表示） | OK |

> 補足＝挨拶は端末のローカル時刻ベースの表示だけで、XP/残高（サーバー権威）に影響しません。

## 増分 #32：プロフィール保存の通知統一（SC-03 プロフィール）

> 対象＝プロフィール編集の「保存する」＋**プロフィール画像（アイコン）の更新/削除**。従来のインライン「保存しました。」を廃止し、**他の更新系（アカウント/会社/評価/アイデア）と同じ共通トースト**（`useSnackbar`・下中央に積む）に統一。成功＝「プロフィールを更新しました」／「プロフィール画像を更新しました」／「プロフィール画像を削除しました」（type=success）。

| ID | 対象 | 確認手順 | 期待する見た目/挙動 | 状態 |
|---|---|---|---|---|
| GF-AC-320 | 保存成功の共通トースト | プロフィール編集で表示名/言語/アニメ設定を変えて「保存する」 | 画面下中央に**✅「プロフィールを更新しました」**のトーストが出る（他の更新系と同じ見た目・数秒で消える）。旧インライン文言は出ない | OK |
| GF-AC-321 | 画像更新の共通トースト | プロフィール画像（アイコン）で「画像を選ぶ」→画像を選択 | ✅「プロフィール画像を更新しました」トーストが出て、アイコンが差し替わる | OK |
| GF-AC-322 | 画像削除の共通トースト | アイコンありの状態で「削除（既定に戻す）」 | ✅「プロフィール画像を削除しました（既定に戻しました）」トーストが出て、頭文字表示に戻る | OK |

> 補足＝失敗時（入力エラー/保存失敗/画像形式・サイズ NG）は従来どおりフォーム内のエラー表示。トーストは成功時のみ。セキュリティ（パスワード＝成功後は再ログインへ遷移／メール変更＝確認メール案内は残す必要があるため常設表示）は対象外。

## 増分 #33：アイデア詳細の投票フィードバック（SC-22・ダッシュボードと共通）

> 対象＝アイデア詳細（SC-22）の**投票**。投票成功時に**更新系と同じ成功トースト**を出し、ダッシュボードのクイック投票と**共通の演出**（クリック位置の火花バースト＋「+N XP」フロート＋ヘッダーのレベルリング/コイン更新）を追加。火花/フロートは `components/ui`（`SparkBurst`/`XpFloat`）へ移設して SC-01/SC-22 で共用（CSS は design-system.css）。XP 額は server 権威（`xp_delta`＝初回/日次上限内で +5・上限後は 0）。

| ID | 対象 | 確認手順 | 期待する見た目/挙動 | 状態 |
|---|---|---|---|---|
| GF-AC-330 | 投票成功トースト | アイデア詳細で **▲賛成/▼反対** を押す | ✅「投票しました」の共通トーストが出る（他の更新系と同じ）。取り消し（同じ選択肢を再クリック）は ℹ️「投票を取り消しました」 | 未確認 |
| GF-AC-331 | 押下の火花＋「+N XP」＋ヘッダー更新 | 未投票のアイデアで初めて賛成/反対を押す | クリック位置から**✦の火花が飛び**、`+5 XP` が**ふわっと上昇**。ヘッダーの**レベルリング（と必要ならコイン）が更新**（日次上限後は XP フロート無し＝トーストのみ） | 未確認 |
| GF-AC-332 | reduce motion 尊重（#33） | 「視差効果を減らす」ON（または設定でアニメ OFF） | 火花・XP フロートは**出ない**（トースト/集計更新は正常） | 未確認 |

> 補足＝火花/フロートは表示だけで、票・XP（サーバー権威）に影響しません。切替（賛成⇄反対）も新規投票と同じ演出。取り消しは演出なし（ℹ️トーストのみ）。

## 増分 #34：ダッシュボード全体を framer-motion の layout で滑らかに再配置（SC-01）

> 対象＝ダッシュボードの主要フローコンテナ（挨拶／上部2カラム／各セクション／下段／管理導線）を `LayoutGroup`＋`motion.* layout` 化。投票やフォロー解除でリストの高さが変わっても、**下のコンテンツが瞬間的に詰まらず全体がスッと移動**して詰まる（GF-AC-040 の退場と連動したチラつきを解消）。あわせてフォロー中カードも投票カードと同様に fade 退場＋繰り上がり。登場アニメ（旧 CSS `dash-enter`＝GF-AC-020）は**廃止し framer の load 時のみ opacity フェードに一本化**（CSS `animation ... both` の transform 固定が framer の layout と競合しチラつく原因だったため）。

| ID | 対象 | 確認手順 | 期待する見た目/挙動 | 状態 |
|---|---|---|---|---|
| GF-AC-340 | 全体の滑らかな再配置（チラつき解消） | 未投票が複数ある状態で投票し、下にクエスト/フォロー/通知等がある画面で見る | 投票カードが退場する**のと同時に、下のセクション全体がガクッと跳ねずスッと上へ詰まる**（上下のチラつきが無い） | 未確認 |
| GF-AC-341 | フォロー解除の退場＋繰り上がり | 「フォロー中のアイデア」で★を押して解除 | 対象カードが**フェード退場**し、残りのフォローカードと下のセクションが**滑らかに繰り上がる** | 未確認 |
| GF-AC-342 | 登場は load 時のみ（操作で再生しない） | 投票やフォロー解除の操作を行う | 操作時に**セクションの登場フェードが再生されない**（load 時だけ）。リロード時のみ登場フェードが出る | 未確認 |
| GF-AC-343 | reduce motion 尊重（#34） | 「視差効果を減らす」ON（または設定でアニメ OFF） | layout の移動・登場フェード・退場が**すべて無効**＝即時に配置が確定（機能は正常） | 未確認 |

> 補足＝表示だけで、票・フォロー状態（サーバー権威）に影響しません。

## 指摘の入れ方
- ID を添えて具体的に（速すぎる/遅すぎる・色・サイズ・位置・タイミング・出過ぎ/出なさすぎ 等）。
- 「対象を増やしたい」（例：ヘッダーのコイン表示・Lv・獲得XP総量にもカウントアップ）も歓迎＝次増分で拾う。
