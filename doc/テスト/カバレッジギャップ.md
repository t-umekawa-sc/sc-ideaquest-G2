# テストカバレッジ・ギャップ（全機能 再レビュー・2026-09-16）

> 設計（要件FR／API設計／画面SC／データモデル）に対し、**テストが無い/観点不足の挙動**を全ドメイン横断で洗い出した棚卸し。5並列監査（A/B・C・D/F・E/H/L・G/K/I/J）の統合。**担保済みは除外**（誤検知回避のため実ファイルで確認済み）。
>
> 区分＝**［A］純テスト不足**（実装済み・テストを足せば埋まる）／**［B］実装ギャップ**（設計にあるが未実装＝テスト前に実装/方針判断が要る）／**［C］設計・実装の乖離**（要判断）。優先度＝High/Med/Low。
>
> 消化ルール＝［A］は red→green で順次テスト追加。［B]/[C] はユーザー判断→実装/仕様修正の上でテスト。埋めた項目は ✅ と TC-ID を追記。

## ［A］純テスト不足（テストを足せば埋まる）

### 通知の生成（発火）— ユーザー要望の起点・最優先 ✅ 完了（2026-09-16）
- [x] **High** `idea_updated` 生成（D→H）＝版追加で**投票者∪フォロワー−編集者**に生成・`params.revision`・`ref_idea_revision_id`。→ **D-TC-223** ✅
- [x] **High** `follow_evaluation`／`follow_selection` 生成（F→H）＝確定/選定で**フォロワー−行為者**に生成。→ **F-TC-205/206** ✅
- [x] **High** `magic_reaction` 生成（E→H）＝他人メッセージへ魔法付与で**投稿者**に生成・`params.spell_id` 凍結／自己付与は非通知。→ **E-TC-224** ✅
- [x] **High** `idea_comment`／`follow_comment` の**正の生成＋投稿者除外**（E 投稿経路）。→ **E-TC-225/226** ✅

### 認可・権限境界・セキュリティ
- [x] **High** ログイン 429 レート制限（IP+login_id・上限超過） → **A-TC-050** ✅
- [x] **High** owner 付与の作成者限定を増分EP経路でも担保 → **C-TC-249** ✅。
- [x] **High** evaluator 付与＝有効パーティー員限定（非メンバー404/メンバー200） → **C-TC-250** ✅
- [x] **Med** 各 permission の実効可否（`vote`のみ会員が評価不可＝403 等の境界）を D/E/F 側で明示。→ **既存担保を確認**（再レビューが見落とし）＝評価は **F-TC-107**（vote 権限のみ→403）／コメントは **E-TC-104**／アイデア作成は **D-TC-103**／投票は **D-TC-122** が権限不足 403 境界を明示済み ✅
- [x] **Med** アカウント発行の Idempotency 二重送信抑止 → **B-TC-035** ✅（旧 B-TC-025 は disable と ID 重複のため 035 へ改番・2026-09-16）
- [x] **Med** クエスト作成 POST /quests の Idempotency → **C-TC-253** ✅
- [x] **Med** 会社アカウント管理者ルート（`/admin/accounts`）の**編集/disable/enable/password-reset 正常系＋他社 IDOR 404＋identity 重複409＋email変更でverifiedリセット**。B-TC-040〜044 は一覧/発行/authz のみだった。→ セルフ経路のギャップを **B-TC-045**（編集＋email変更で verified リセット）／**B-TC-046**（disable/enable）／**B-TC-047**（password-reset）／**B-TC-048**（他社 IDOR 404＝セッション会社固定の実効境界・本経路固有）／**B-TC-049**（identity 重複409）で担保 ✅（変更系ロジックは system_admin 経路 B-TC-025〜034 と service 共有）
- [x] **Med** メール確認リンク**再送で旧トークン失効**（送信→再送→旧トークンconfirm 410・新のみ200）。→ **既存担保を確認**（台帳の「password_setup と非対称」は**事実誤認**＝実装は対称・`invalidate_email_verify_challenges` で再送時に未使用チャレンジ DELETE）＝再送でチャレンジ1件のみ **B-TC-165/166**／旧・無効トークンは 410 **A-TC-104**／email 変更で verified リセット **B-TC-167** が担保済み ✅
- [x] **Med** 添付DL 非パーティー404（IDOR） → **D-TC-227** ✅
- [x] **Med** 選定の非パーティー404（POST/DELETE select・IDOR） → **F-TC-208** ✅
- [x] **Low** `email-verify/confirm` の不正 Origin 拒否（A.7.1）。→ **A-TC-110**（不正 Origin で 403・token 未消費＝Origin 検証が token 判定に先行）✅

### 状態機械・データ整合
- [x] **High** 状態遷移の後退→再前進で副作用が二重化しない → **C-TC-248** ✅
- [x] **High** 添付 1ファイル20MB超過（`too_large`） → **D-TC-224** ✅
- [x] **High** 再投票で stale 解消（voted_revision→current 追随） → **D-TC-225** ✅
- [x] **Med** quest_members のAPI経路トゥームストーン再利用 → **C-TC-255** ✅
- [x] **Med** 公開中クエスト PATCH の strict（categories:[]→422） → **C-TC-254** ✅
- [x] **Low** `PUT /party` 一括差分の owner 検証＋原子性（不正差分で403かつ全ロールバック）／completed で `PUT /party` 409。→ owner 検証403 は既済（**C-TC-141/249**）／completed 409（PUT 経路）= **C-TC-145**／原子性（末尾候補外で422＋先頭有効も未適用・検証先行）= **C-TC-146** ✅
- [x] **Low** 複数フィールド同時変更の changed_fields 複数 → **D-TC-228** ✅

### 一覧クエリ契約・検索・集計
- [x] **High** ランキング scope=company（全社集計） → **G-TC-406** ✅
- [x] **High** 台帳 grant の reason 網羅（selection/evaluation/evaluation_coin/achievement_reward） → **G-TC-109** ✅
- [x] **High** 全文検索のトゥームストーンchat除外（is_deleted=true 非表示） → **J-TC-123** ✅
- [x] **Med** `GET /achievements` の state/category フィルタ → **G-TC-513** ✅
- [ ] **Med→[B]（将来 seam・機能ギャップではない）** `GET /items` の slot/rarity/owned/affordable/sort/limit/cursor＝**backend 未実装だが frontend は client 側で充足**（2026-09-17 仕様確認）。G.1 は当該クエリを規定（正）。ただし現行は `get_items` が**全アイテムを返す**（`{data, coin_balance}`・page_info なし）＋**`ShopView.tsx` が DataTable の client モード**で絞込/並び替え/検索/CSV/ピン/ページャを実施＝**SC-10 sort と同型**（モック一致・DoD 達成）。装備マスタは小さい固定カタログのため client 完結で十分。→ **サーバー委譲は将来 seam**（カタログが client ウィンドウを超えたら backend に `slot/rarity/owned/affordable/sort` ＋カーソルを実装）。実装するなら [B]② sort と同じ要領（router→application→repository＋TC）。
- [x] **Med** 装備 PUT の冪等（同一 item 再PUT no-op） → **G-TC-307** ✅
- [x] **Med** `GET /quests` の q/group_id フィルタ → **C-TC-251** ✅
- [x] **Med** GET /quests/{id}/members 専用EP の in_scope/group_ids → **C-TC-252** ✅
- [x] **Med** `GET /me` の画像**署名URL**解決（生パス非露出・K.1）。→ **既存担保を確認**（台帳の「K側未担保」は誤り）＝`tests/me/test_me_images.py::test_k_tc_avatar_put_sets_signed_url` が **`GET /me` の avatar_image_url が署名URL（`https://minio.test/...`）で返り生パス非露出**を明示アサート（idea_icon/background も同ファイルで担保）✅
- [x] **Med** 評価集計の空表示（可視0件） → **F-TC-209** ✅
- [x] **Low** ランキングのタイブレーク（XP→コイン→先着）と `this_month`/`all` 期間（G.5）。→ タイブレーク（同スコア→XP→先着）= **G-TC-407**／期間 this_month/all = **G-TC-408** ✅（注: score=XP+コインのため「同スコア∧同XP→コイン差」は原理的に生じず、実効タイブレークは XP→先着）
- [x] **Low** 引用元削除で `quotes[].excerpt` がトゥームストーン文言（E.1）。→ **E-TC-227**（引用元 delete で excerpt が「このメッセージは削除されました」に置換）✅
- [x] **Low→設計修正** `chat_preview`（E.1）＝実態に合わせ設計を**「現状未実装・将来対応」**と明示（API設計 D.1／E.1【現状未実装】・2026-09-16）。現行実装は idea 詳細に内包せず＝純テスト対象外。実装着手時に E.1 の形へ準拠（[B] 将来）。

## ［B］実装ギャップ（設計にあるが未実装＝要 実装/方針判断）

- [x] **High** **認証イベントの監査ログ**（A.9-⑥）＝**2026-09-17 実装済み**。精査結果＝旧記述「1箇所のみ」は誤りで `auth.login.new_device`/`auth.password_changed`（PW設定/変更）・管理者 `account.password_reset`・ロール変更 `account.edit` は既存。**欠落分を追加**＝`auth.login.success`（MFA有無）/`auth.login.failure`/`auth.account_locked`/`auth.mfa.issued`（login＋resend）/`auth.mfa.verify`（success/failure）/`auth.logout`/`auth.logout_all`/`auth.password_setup.request`（自己サービス・origin で管理者起点と区別）。actor/IP/UA は `AuditContextMiddleware` 自動付与・機密（PW/生OTP/トークン）は不記録。テスト＝**A-TC-111〜115**（`tests/auth/test_auth_audit.py`）。
- [x] **High** `GET /quests` の **`sort`**（`-created_at`/`deadline`/`-idea_count`/`-member_count`・複数キー・keyset）。根拠 C.1/§1.8.1。→ **実装済み（2026-09-17）**＝router に `sort` 追加／application で whitelist 検証(422 `field=sort`)・sort 解析・カーソルをソートタプル内包形式へ再設計／repository で集計列（相関サブクエリ）注釈＋mixed-direction keyset（`deadline` は NULLS LAST）。テスト＝**C-TC-010/011/012**(int: 集計列ソート+keyset・deadline NULLS LAST・複数キーtiebreak)・**C-TC-106/107/108**(api: idea_count降順・未知キー422・ソート指定時のカーソル安定) ✅
- [x] **High** ダッシュボード populated 系＝**実装は既済で純テスト不足だった**（`get_dashboard` は全パネル合成済み）。**2026-09-17 テスト追加**＝**I-TC-107**(週間ランキング data≤3＋me)/**I-TC-108**(通知 data≤5＋unread_count)/**I-TC-122**(IDOR＝自スコープのみ)＝`tests/dashboard/test_api.py`・**I-TC-131**(部分失敗 best-effort＝1パネル例外で当該 null・他正常)/**I-TC-141〜143**(D/F 横断 read＝下書きアイデア/未投票/下書き評価)＝`tests/dashboard/test_cross_domain.py`。production 無変更 ✅
- [ ] **High** リアルタイム **L-TC-103**（notify は post-commit のみ配信＝rollback 非配信）・**L-TC-131**（WS ハンドシェイクの Origin 検証）。台帳にIDあるが**未実装**。
- [x] **Low→設計修正** `GET /me/spells`（G.3）＝設計を実態に合わせ**不採用化**（API設計 G.3・`GET /spells` が `unlocked`/`can_unlock` 同梱でピッカーも賄える＝重複 EP のため設けない・2026-09-16）。純テスト対象外＝クローズ。

## ［C］設計・実装の乖離（要判断）

- [x] **High** **無変更保存＝版なし（1保存1版）**：frontend(`IdeaForm.persist`)のみ抑制だった。→ **判断＝サーバーでも無変更は版/通知を作らない**（コーディング規約 §1 サーバー権威・通知スパム防止・frontend と対称）で **2026-09-17 実装済み**＝`update_idea` が `_apply_content` の前後で `_content_snapshot` を比較し差分ゼロなら `_record_revision`（版＋`idea_updated` 通知）をスキップ。設計反映＝API設計 D「編集と版」/D.4「版の生成」。テスト＝**D-TC-229**（同一内容 PATCH＝版据え置き+通知0／実変更は版2+通知1＝過抑制しない）✅
- [x] **Med** メンション差し替え時の通知整合（E.2「PATCH で増減した対象へ通知整合」）：`_notify_message_updated` が no-op だった。→ **判断＝決定A（追加分のみ通知）**で **2026-09-17 実装済み**＝編集で**新規に追加された被メンションのみ** `mention` 通知（不変は再通知せず・外した分は通知も取消もしない＝at-most-once・H に取消プリミティブなし）。設計反映＝API設計 E.2/E.6。テスト＝**E-TC-228**（追加U2に1件・不変U1据え置き・除外しても取消なし）✅

## 済み（今セッションで追加・参考）
- 実績の解除タイミング全条件タイプ＝G-TC-502/503/505/509/510/511/512。
- ランキング積み上げ＝G-TC-401/402。
- スクロール位置復元 §4.12＝M-TC-012/013/014。既読focus＝H-TC-211。通知日時＝I-TC-155/156。引用非通知＝E-TC-223。
