# テストカバレッジ・ギャップ（全機能 再レビュー・2026-09-16）

> 設計（要件FR／API設計／画面SC／データモデル）に対し、**テストが無い/観点不足の挙動**を全ドメイン横断で洗い出した棚卸し。5並列監査（A/B・C・D/F・E/H/L・G/K/I/J）の統合。**担保済みは除外**（誤検知回避のため実ファイルで確認済み）。
>
> 区分＝**［A］純テスト不足**（実装済み・テストを足せば埋まる）／**［B］実装ギャップ**（設計にあるが未実装＝テスト前に実装/方針判断が要る）／**［C］設計・実装の乖離**（要判断）。優先度＝High/Med/Low。
>
> 消化ルール＝［A］は red→green で順次テスト追加。［B]/[C] はユーザー判断→実装/仕様修正の上でテスト。埋めた項目は ✅ と TC-ID を追記。

## ［A］純テスト不足（テストを足せば埋まる）

### 通知の生成（発火）— ユーザー要望の起点・最優先 ✅ 完了（2026-09-16）
- [x] **High** `idea_updated` 生成（D→H）＝版追加で**投票者∪フォロワー−編集者**に生成・`params.revision`・`ref_idea_revision_id`。→ **D-TC-223** ✅
- [x] **High** `follow_evaluation`／`follow_selection` 生成（F→H）＝確定/選定で**フォロワー−行為者**に生成。→ **F-TC-205/206** ✅
- [x] **High** `magic_reaction` 生成（E→H）＝他人メッセージへ魔法付与で**投稿者**に生成・`params.spell_id` 凍結／自己付与は非通知。→ **E-TC-224** ✅
- [x] **High** `idea_comment`／`follow_comment` の**正の生成＋投稿者除外**（E 投稿経路）。→ **E-TC-225/226** ✅

### 認可・権限境界・セキュリティ
- [ ] **High** ログイン **429 レート制限**（`check_login_rate_limit`・IP+login_id）＝実装稼働中だがテスト0（A-TC-050 は予約のまま）。ロック層（第二層）と別。
- [ ] **High** `owner` 付与の作成者限定を**増分EP経路**でも担保＝`POST /quests/{id}/members {permissions:[owner]}`・`PUT .../permissions {permissions:[owner]}` を非作成者(quest_admin)が叩き 403。PATCH 経路(C-TC-122)のみ担保。
- [ ] **High** `evaluator` 付与＝**有効パーティー員限定**（非メンバーへ付与は404・メンバーは反映）。根拠 C.0。
- [ ] **Med** 各 permission の実効可否（`vote`のみ会員が評価不可＝403 等の境界）を D/E/F 側で明示（要 red 確認）。
- [ ] **Med** アカウント発行の **Idempotency 二重送信抑止**（同一キー再送で accounts/mail_outbox/account_sync_outbox いずれも1件）。SEC-TC-041〜044 はアイデア作成のみ。
- [ ] **Med** クエスト作成 `POST /quests` の Idempotency 二重送信抑止。
- [ ] **Med** 会社アカウント管理者ルート（`/admin/accounts`）の**編集/disable/enable/password-reset 正常系＋他社 IDOR 404＋identity 重複409＋email変更でverifiedリセット**。B-TC-040〜044 は一覧/発行/authz のみ。
- [ ] **Med** メール確認リンク**再送で旧トークン失効**（送信→再送→旧トークンconfirm 410・新のみ200）。password_setup(A-TC-040)と非対称。
- [ ] **Med** 添付ダウンロードの**非パーティー404（IDOR）**（D.3）。D-TC-136 は happy path のみ。
- [ ] **Med** 選定/評価の非パーティー404＝`POST/DELETE select` の門番（F.0）。F-TC-116 は権限403のみ。
- [ ] **Low** `email-verify/confirm` の不正 Origin 拒否（A.7.1）。

### 状態機械・データ整合
- [ ] **High** 状態遷移の**後退→再前進で副作用が二重化しない**（`completed→evaluating→completed` で `quest_result_ready`通知・`quest_completed`活動・`evaluation_coin` が各1件のまま）。根拠 C.5。
- [ ] **High** 添付 **1ファイル20MB超過（`too_large`/413）**（D.3/§5.12）。`too_many`/`mime`は担保・`too_large`はコード/台帳とも0。
- [ ] **High** 再投票で **stale 解消**＝押し直しで `voted_revision`→`current_revision` 追随・`vote.stale=false` に戻る（D.5）。D-TC-144 は true 方向のみ。
- [ ] **Med** `quest_members` 部分ユニークの**API経路トゥームストーン再利用**（DELETE→POST 再追加で行が増えない・joined_at 更新・既定権限復活）。C-TC-006 は repo 直のみ。
- [ ] **Med** 公開中クエスト PATCH の strict（recruiting に `categories:[]`→422）。C-TC-113 は作成時のみ。
- [ ] **Low** `PUT /party` 一括差分の owner 検証＋原子性（不正差分で403かつ全ロールバック）／completed で `PUT /party` 409。
- [ ] **Low** 複数フィールド同時変更の `changed_fields`＝複数（D.4）。D-TC-138 は1版1フィールド。

### 一覧クエリ契約・検索・集計
- [ ] **High** ランキング `scope=company`（全社集計・テナント分離）の api（G.5）。既存 G-TC-401〜405 は quest スコープのみ。
- [ ] **High** 台帳 grant の reason 網羅＝**選定XP=200・evaluation・evaluation_coin・achievement_reward** の `(kind,reason,amount,ref_type)` を G.6 対応表と突合（ledger 単体）。test_ledger は idea_post/shop_purchase/login のみ。
- [ ] **High** 全文検索の**トゥームストーンchat除外（J-TC-123）**＝`is_deleted=true` チャットが結果に出ない（漏洩観点）。
- [ ] **Med** `GET /achievements` の `state`/`category` フィルタ（G.4）。
- [ ] **Med** `GET /items` の `slot`/`rarity`/`owned`/`affordable`/`sort`（G.1・`affordable` は不正入手防止観点）。
- [ ] **Med** 装備 PUT の冪等（同一 item 再PUTは no-op・is_equipped 行1点）。G.2。
- [ ] **Med** `GET /quests` の `q`/`group_id` フィルタ（C.1）。status=bogus 422 のみ担保。
- [ ] **Med** `GET /quests/{id}/members`（専用EP）の `in_scope`/`group_ids`（C.1）。詳細EP(C-TC-233/235)のみ。
- [ ] **Med** `GET /me` の画像**署名URL**解決（生パス非露出・K.1）。K側で未担保（dashboard I-TC-102 のみ）。
- [ ] **Med** 評価集計の**空表示**（可視0件＝evaluator_count=0/overall_avg=null/coin0）を `GET /ideas/{id}/evaluation` 直叩き（F.1）。
- [ ] **Low** ランキングのタイブレーク（XP→コイン→先着）と `this_month`/`all` 期間（G.5）。
- [ ] **Low** 引用元削除で `quotes[].excerpt` がトゥームストーン文言（E.1）。
- [ ] **Low** `chat_preview`（直近3件・切詰め・削除トゥームストーン・total_count）を api で（E.1）。

## ［B］実装ギャップ（設計にあるが未実装＝要 実装/方針判断）

- [ ] **High** **認証イベントの監査ログ**（ログイン成功/失敗・logout・logout-all・MFA発行/検証・ロック・PW再設定リクエスト）。根拠 A.9-⑥。`audit.record` は `email.verify.confirm` の1箇所のみ＝**実装欠落**。侵害調査の土台。→ 実装＋テスト。
- [ ] **High** `GET /quests` の **`sort`**（`-created_at`/`deadline`/`-idea_count`/`-member_count`・複数キー・keyset）。根拠 C.1/§1.8.1。router に `sort` 引数が無い＝**実装欠落**。
- [ ] **High** ダッシュボード **I-TC-107/108**（週間ランキング populated・通知 populated＋unread_count）＋ **I-TC-122**(IDOR)/**I-TC-131**(部分失敗best-effort)/**I-TC-141〜143**(D/F横断read)。台帳にIDあるが**未実装**（空ダッシュボードのみ検証）。
- [ ] **High** リアルタイム **L-TC-103**（notify は post-commit のみ配信＝rollback 非配信）・**L-TC-131**（WS ハンドシェイクの Origin 検証）。台帳にIDあるが**未実装**。
- [ ] **Low** `GET /me/spells`（G.3）＝設計EP だがチャットは E 経由で代替済み＝**デッドスペック**。→ 設計側を「E代替・G.3不要」に正すか実装。

## ［C］設計・実装の乖離（要判断）

- [ ] **High** **無変更保存＝版なし（1保存1版）**：frontend(`IdeaForm.persist`)のみ抑制。**backend `update_idea` は差分に関わらず publish 中 PATCH で毎回 `_record_revision`＋`idea_updated` 通知を発火**（サーバーガード無し・テスト無し）。→ 「サーバーでも無変更は版/通知を作らない」を担保するか、frontend専任と割り切るか判断（判断後にテスト）。
- [ ] **Med** メンション差し替え時の通知整合（E.2「PATCH で増減した対象へ通知整合」）：実装 `_notify_message_updated` は **no-op**。設計は整合を要求＝乖離。→ 仕様確定（PATCH で新規メンションに通知するか）後にテスト。

## 済み（今セッションで追加・参考）
- 実績の解除タイミング全条件タイプ＝G-TC-502/503/505/509/510/511/512。
- ランキング積み上げ＝G-TC-401/402。
- スクロール位置復元 §4.12＝M-TC-012/013/014。既読focus＝H-TC-211。通知日時＝I-TC-155/156。引用非通知＝E-TC-223。
