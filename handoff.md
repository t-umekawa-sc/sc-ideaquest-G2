# 作業引き継ぎ (handoff)

> 次回セッション開始時に **このファイルだけ読めば作業を再開できる** ことを目的とした引き継ぎメモ。
> 詳細な仕様は必ず `doc/要件定義/README.md`（唯一の要件定義書）を参照すること。

- 最終更新: 2026-07-06
- プロジェクト: ideaquest（これから作る WEB アプリ。**現在は要件定義フェーズ。コード実装は未着手**）

## 0. リポジトリ状態

- 作業ディレクトリ: `/home/t-umekawa/sc-ideaquest-G2`
- git 管理下。remote `origin` = https://github.com/t-umekawa-sc/sc-ideaquest-G2.git（ブランチ `main`）
- 直近コミット（新しい順）:
  - `d1dcac3` Define voting spec (self-vote, named, avatar display, XP)
  - `c7371ef` Define quest permission model and evaluator scope
  - `54b837f` Add quest-centric spec to requirements (quest, voting, chat, evaluation)
  - `65d2655` Add initial ideaquest requirements docs and handoff notes
- 運用ルール: 要件を1つ決めるごとに `doc/要件定義/README.md`＋この handoff を更新し、ユーザー承認のうえコミット＆プッシュしてきた。
- 要件定義ドキュメントは日本語で記述。

## 1. 現在のファイル構成

```
sc-ideaquest-G2/
├── doc/
│   └── 要件定義/
│       └── README.md   ← 要件定義書（実質的な仕様の本体。かなり書き込み済み）
└── handoff.md          ← このファイル
```

## 2. アプリのコンセプト（確定）

- **ideaquest = ゲーム感覚で楽しくアイデアを出し・管理できる WEB アプリ**。テーマはゲーミフィケーション。
- **ターゲット = 企業**（社内でアイデアを集めたい組織）。マルチテナント（企業単位でデータ分離）、ロールあり。
- 中心概念:「**クエスト**」= アイデア出しのテーマ単位。カテゴリー・目的/テーマ・期限日・参加メンバー（パーティー）を設定して作成。
- 全体フロー: クエスト作成 → メンバーがアイデア投稿 → 賛成/反対の投票＋アイデア別チャットで議論 → 評価者が5観点で採点・コメント → 集計・選定。
- ゲーミフィケーション: 行動で **XP**（→レベルアップ）と **コイン**（→ショップで装備購入）を獲得し、**3Dアバター**を着せ替え。

## 3. これまでに決定した要件（確定事項）

すべて `doc/要件定義/README.md` に反映済み。

- **ターゲット = 企業**（マルチテナント、ロール: 一般/管理者、管理者機能、SSOは案）。
- **クエスト**の入力項目: カテゴリー / 目的・テーマ / 期限日 / 参加メンバー（パーティー）。
- **アイデア**の入力項目: 件名 / タイムリミット / 利害関係者 / アイデア本文 / 価値 / 備考・特記事項 / 関連資料ファイル添付。
- **アイデアごとにチャットグループ**を自動作成し、メンバーが議論。
- **評価**: 評価者がアイデアを5観点（新規性・影響度・実現度・適合性・コスト）で各5点満点採点＋観点別コメント。
- **評価者の定義**: クエスト作成者＋作成者が評価者権限を付与した参加メンバー（クエスト単位の権限、複数可）。付与対象は参加メンバー限定。
- **クエスト内の権限モデル（6権限）**: 所有者 / クエスト管理 / 評価者 / アイデア投票 / アイデア作成 / コメント作成。
  - 所有者 = 全操作可。**作成者は既定で所有者・剥奪不可**。所有者権限の他者への付与は**作成者のみ**。
  - クエスト管理権限 = クエスト編集（参加メンバー調整・権限付与/剥奪含む）。所有者/管理権限者がその他権限を付与/剥奪できる。
  - 権限を持てるのは参加メンバーのみ。
  - **新規参加メンバーの既定権限 = 投票＋アイデア作成＋コメント**（他は手動付与）。
  - **パーティーから外すと権限は失うが、それまでの入力（アイデア/投票/評価/コメント）は削除せず表示継続**。
- **投票の仕様**: 賛成/反対、1人1票・締切まで変更可。**自分のアイデアにも投票可**。**記名**。賛成/反対したユーザーの**アバターを並べて表示**。**投票者にXP付与**（量はTBD）。**評価スコアには非影響**（投票と評価は独立）。

## 4. まだ決まっていない主な TBD（次に検討する候補）

要件定義書 第12節に一覧あり。優先度の高そうなもの:

- 「コスト」観点の採点方向（低コスト＝高得点か）＋5観点の重み付けの有無。
- クエストのカテゴリー: 事前定義リスト or 自由入力。
- アイデア項目の必須/任意（タイムリミット・利害関係者・価値 など）。
- チャットの参加範囲（パーティー全員か限定か）・ファイル添付・メンション・通知。
- **XP・コインの具体的な付与量とレベル設計**（投票/評価/チャットで付与するかも含む）。
- 添付ファイルの上限サイズ・形式・保管先。
- 提供形態（SaaS or 個別導入）、認証方式（独自 or SSO）、アイデアの公開範囲。
- 装備アイテムの種類・入手方法、3Dアバターのモデル形式（glTF/VRM）・着せ替え方式。
- 技術スタックの確定（フロント3Dは Three.js / React Three Fiber を想定中）、マネタイズの有無。

## 5. 次にやること

1. 上記 TBD をユーザーと1つずつ確認して確定 → その都度 README と handoff を更新しコミット＆プッシュ。
2. ある程度固まったら、画面設計・データモデルを別ファイル（`画面設計.md` / `データモデル.md`）に分割して詳細化。
3. 技術スタックを確定し、実装フェーズへ。

## 6. データモデル案（現時点、詳細は README 第9節）

Company / User / Quest（owner_id 保持）/ QuestMember（パーティー）/ QuestMemberPermission（権限）/ Idea / Attachment / Vote / ChatGroup / ChatMessage / Evaluation / EvaluationScore / Item(Equipment) / UserItem / Transaction(Activity)。
