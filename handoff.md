# 作業引き継ぎ (handoff)

> 次回セッション開始時に **このファイルだけ読めば作業を再開できる** ことを目的とした引き継ぎメモ。
> 詳細な仕様は必ず `doc/要件定義/README.md`（唯一の要件定義書）を参照すること。

- 最終更新: 2026-07-19
- プロジェクト: ideaquest（これから作る WEB アプリ）
- **現在のフェーズ = データモデル詳細フェーズをレビュー中（画面設計フェーズ＝完了・SC-00〜SC-92）**。**`doc/データモデル.md` を新規作成**（管理DB6＋会社DB27テーブルの ER 図・カラム/型/制約/インデックス・Enum一覧・全文検索索引・集計方針・TBD集約）。**FK命名規則 §2.1 を新設して全FKカラム（約60箇所）を統一・検証済み**（詳細は第5節末尾）。要件定義は主要項目が確定済み（残TBDは第4節＋データモデル.md §8）。技術スタックも確定。**アプリのコード実装・compose 等のインフラ定義はまだ未着手**（リポジトリは doc と handoff のみ）。**次＝データモデル.md §8 の配置系TBDをユーザー確認→API設計→実装**（第5節末尾参照）。

## 0. リポジトリ状態

- 作業ディレクトリ: `/home/t-umekawa/sc-ideaquest-G2`
- git 管理下。remote `origin` = https://github.com/t-umekawa-sc/sc-ideaquest-G2.git（ブランチ `main`）
- 直近コミット（新しい順・2026-07-19 時点。全履歴は `git log`）:
  - `(未取得)` データモデル.md 全テーブル定義・ER図から共通監査カラム(created_at/updated_at)行を削除し §2.1 の共通装備へ完全省略統一（意味的タイムスタンプ last_login_at/expires_at 等とindex参照は保持）
  - `cf0a986` データモデル.md §2 に共通監査カラム §2.1 を新設（全テーブルに登録3点 created_at/created_by_id/created_program＋更新3点 updated_at/updated_by_id/updated_program を共通装備。各表・ER図では列挙省略）。FK規則は §2.1→§2.2 に繰り下げ・参照修正
  - `54124fe` データモデル.md §4 管理DBのER図に論理名を併記（テーブル=mermaidエイリアス `name["物理名（論理名）"]`／全カラム=属性コメント。会社DB §5.1/5.2 は関係のみ形式のため未対応）
  - `4fd7599` データモデル.md に FK命名規則（現§2.2）を新設＋既存4カラムを規則に統一（granted_by→granted_by_id / uploaded_by→uploaded_by_id / reply_to_id→reply_to_message_id / ref_revision_id→ref_idea_revision_id）
  - `c594075` データモデル.md を物理テーブル名=複数形snake_caseに統一＋テーブル/カラムに日本語論理名を併記
  - `b81694a` データモデル詳細 doc/データモデル.md を新規作成（管理DB6＋会社DB27テーブルのER図・定義・Enum・全文検索・集計方針・TBD）
  - `a7cbaf7` ベル(<a>)のホバー時リンク下線を除去（.bell / .bell:hover text-decoration:none）
  - `a53ca70` handoff を最新化（初期スコープ全画面SC-00〜92完成 ほか）
  - `a417658` SC-92 文脈バナーをヘッダー直下で固定しz-indexを下げてメニュー操作を確保
  - `0b0660c` ログインIDとメールアドレスを別項目に分離（Account login_id/email・SC-00/90/92）
  - `e210f23` style-guide のカラー見本を .tokens/.token へ改名し .swatch 名前衝突を解消（レイアウト崩れ修正）
  - `b864f64` 会社にアバター/カラーを追加＋SC-91を会社一覧に再設計しSC-92会社詳細へ分割
  - `f0a2113` SC-90/91 管理画面を追加＋トグルスイッチ.switchを新設（初期スコープ全画面が完了）
  - `810cefe` SC-02 通知画面を追加（業務層・種別/未読フィルタ、全ページのベルをSC-02へ接続）
  - `2da48d6` SC-40 実績/バッジ（実績報酬＝ティア連動コイン 銅20/銀50/金150）／`5a36b0a` SC-41 ランキング／`f5dced1` SC-30 ショップ／`0b45a16` SC-31 アバター着せ替え
  - `8b528e1` `34ad62b` `8977fa7` SC-32 魔法/スキル画面（SPで解放）＋スキルツリー（同系統下位が前提）＋1メッセージ1魔法
  - `47d15a7` ダッシュボードの下書きに評価下書きも表示
  - `7672519` 背景画像の設定をヘッダーのユーザーメニューへ移動（全画面共通・shared.js集約）
  - `dc84fcf` SC-22 に議論アクティビティグラフ＋変更履歴/差分＋投票見直しを追加
  - `973c483` `b0df02b` 評価に総評（全体コメント・必須）を追加＝SC-22 評価結果に表示
  - `e3cc89a` クエストカラー設定（プリセットパレット `.swatches`）を新設＋SC-11
  - `896ec97` 複数選択コンボボックス `.multiselect` を新設し SC-11/SC-21 へ反映
  - `3a838f2` SC-11/SC-21 作成・編集モーダルを追加＋クエスト件名を必須化・既存導線を接続
  - （以前: 共通ヘッダー統一 / SC-10 クエスト一覧 / SC-25 評価 / SC-24 チャット / SC-22 アイデア詳細 / SC-12 クエスト詳細 / SC-01 ダッシュボード / SC-00 ログイン / デザイン標準・MFA・要件確定 等）
- **直近（2026-07-19）で初期スコープの全画面（SC-00〜SC-92）が完成**。ゲーム層（SC-30/31/32/40/41）＋通知（SC-02）＋管理系（SC-90/91/92）を追加、トグルスイッチ `.switch` 新設、実績報酬をティア連動コインに変更、システム管理を会社一覧(SC-91)＋会社詳細(SC-92)に分割、会社にアバター/カラー追加、ログインIDとメールを別項目に分離。加えて style-guide の `.swatch` 名前衝突と SC-92 バナー固定位置の不具合を修正。**作業ツリーはクリーン。次フェーズ＝データモデル詳細→API設計→実装（第5節末尾）。**
- **画面の進捗（画面設計フェーズ）＝完了: SC-00 ログイン / SC-01 ダッシュボード / SC-10 クエスト一覧 / SC-11 クエスト作成編集 / SC-12 クエスト詳細 / SC-21 アイデア登録編集 / SC-22 アイデア詳細 / SC-24 アイデアチャット / SC-25 評価 / SC-02 通知 / SC-30 ショップ / SC-31 アバター着せ替え / SC-32 魔法スキル / SC-40 実績バッジ / SC-41 ランキング / SC-90 クエストグループ管理 / SC-91 システム管理（会社一覧）/ SC-92 会社詳細・設定。**＝初期スコープの全画面（SC-00〜SC-92）が完了**。**（各画面 = `mocks/SC-xx_*.html` ＋ `screens/SC-xx_*.md`）
- 運用ルール: 要件を1つ決めるごとに `doc/要件定義/README.md`＋この handoff を更新し、**ユーザー承認のうえコミット＆プッシュ**する。README が唯一の要件定義書。
- 要件定義ドキュメントは日本語で記述。

## 1. 現在のファイル構成

```
sc-ideaquest-G2/
├── doc/
│   ├── データモデル.md      ← **データモデル詳細**（ER図・テーブル定義・型/制約/index・Enum・全文検索・集計方針・TBD。README第9節の実装展開版）
│   ├── 要件定義/
│   │   └── README.md      ← 要件定義書（唯一の仕様本体。かなり書き込み済み）
│   └── 画面設計/
│       ├── 画面遷移図.md   ← 画面遷移図＋画面一覧（**遷移レベルは合意済み**。設計フェーズの起点）
│       ├── デザイン標準.md ← **デザイン標準（確定）**。トーン/カラー/タイポ/コンポーネント規約
│       ├── mocks/
│       │   ├── shared.css      ← 共通CSS（デザイントークン=CSS変数）。全モックが参照
│       │   ├── shared.js       ← モック共通JS（`[data-combobox]`/`[data-multiselect]` 初期化・`.usermenu` 開閉・背景画像設定 `[data-bg-set]`/`[data-bg-reset]` 集約）
│       │   ├── style-guide.html ← 目視確認用スタイルガイド（ブラウザで直接開く）
│       │   ├── SC-00_ログイン.html ← SC-00 ログイン画面モック（背景画像＋2状態）
│       │   ├── SC-01_ダッシュボード.html ← SC-01 ダッシュボードモック（上部2カラム=左ヒーロー＋右週間ランキング／下段）
│       │   ├── SC-02_通知一覧.html ← SC-02 通知（業務層。一覧標準の状態/種別絞り込み＋すべて既読＋日付グループ〔今日/昨日/それ以前〕の通知リスト。クリックで参照先〔SC-24/22/40〕へ・既読トグル・ヘッダーベルの遷移先）
│       │   ├── SC-12_クエスト詳細.html ← SC-12 クエスト詳細モック（ヘッダー＋クエスト内週間ランキング／タブ〔💡アイデア/👥パーティー/🔍全文検索/📋概要〕・アイデア一覧テーブル）
│       │   ├── SC-22_アイデア詳細.html ← SC-22 アイデア詳細モック（ヘッダー／メイン〔価値・本文・添付・チャットプレビュー〕＋右レール〔投票・評価結果・情報〕）
│       │   ├── SC-24_アイデアチャット.html ← SC-24 アイデアチャットモック（文脈バー／フラットリスト〔日付・未読セパレータ・添付・メンション〕／入力欄〔@メンション候補・添付・Enter送信〕）
│       │   ├── SC-25_評価画面.html ← SC-25 評価モーダル（5観点★採点＋観点別コメント＋平均プレビュー＋公開範囲指定・下書き・ドラッグ移動・アイデア折りたたみ）
│       │   ├── SC-10_クエスト一覧.html ← SC-10 クエスト一覧（一覧標準＋カード/リスト切替・クエストアイコン/色アクセント）
│       │   ├── SC-11_クエスト作成編集.html ← SC-11 クエスト作成・編集モーダル（アイコン/件名/カテゴリ〔事前定義＋自由入力〕/期限/グループ/パーティー＋6権限トグル・ドラッグ移動）
│       │   ├── SC-21_アイデア登録編集.html ← SC-21 アイデア登録・編集モーダル（必須3項目＋任意折りたたみ＋添付D&D・必須充足まで投稿無効・ドラッグ移動）
│       │   ├── SC-30_ショップ.html ← SC-30 ショップ（ゲーム層。コイン残高ヒーロー〔CRTガラス〕＋一覧標準の絞り込み/並び替え＋装備グリッド〔レアリティ色・購入=コイン消費・所有済みはSC-31導線〕。SC-31と同一装備セット）
│       │   ├── SC-31_アバター着せ替え.html ← SC-31 アバター/着せ替え（ゲーム層。左=3Dアバタービューア〔CRTガラス〕＋右=ワードローブ〔5スロット×装備・所有はクリック着替え・未所有はSC-30誘導〕）
│       │   ├── SC-32_魔法スキル.html ← SC-32 魔法/スキル（ゲーム層。SPステータス＝CRTガラス＋魔法カタログ6種〔効果プレビュー付き〕・SP消費で解放）
│       │   ├── SC-40_実績バッジ.html ← SC-40 実績/バッジ（ゲーム層。収集サマリー〔CRTガラス＋達成率バー〕＋一覧標準の絞り込み＋バッジグリッド〔ティア銅銀金・進捗バー・シークレット伏せ〕。報酬なし=収集/表示のみ）
│       │   ├── SC-41_ランキング.html ← SC-41 ランキング（ゲーム層。全社・期間切替〔今週/先週/今月/通算〕＋表彰台TOP3＋全件リスト〔.rank-panel流用〕＋自分の順位サマリー。スコア＝XP＋コイン）
│       │   ├── SC-90_クエストグループ管理.html ← SC-90 クエストグループ管理（業務層。QG管理者が自グループ内アカウントを発行/編集/論理削除〔無効化⇄再有効化〕/パスワード再設定。管理グループ切替＋一覧標準＋発行/編集モーダル）
│       │   ├── SC-91_システム管理.html ← SC-91 システム管理（会社一覧）（業務層・運営。会社〔テナント〕一覧＋検索/状態フィルタ＋会社作成モーダル。各行「管理する→」で SC-92 へ）
│       │   ├── SC-92_会社詳細.html ← SC-92 会社詳細/設定（業務層・運営。上部に「メンテ中の会社」固定文脈バナー＋会社設定〔.switch×3〕＋その会社のアカウント/所属フル操作。?company=id で会社特定）
│       │   └── assets/
│       │       ├── logo-ideaquest.png ← 正式ロゴ（紋章＋ワードマーク・**白背景を透過加工済み**）
│       │       ├── mascot-hero.png    ← マスコット（青マントの剣士ドット絵・背景透過済み）
│       │       └── login-bg.jpg       ← ログイン背景（ドット絵の夕焼け帆船）
│       └── screens/
│           ├── SC-00_ログイン.md ← SC-00 画面設計（機能詳細）
│           ├── SC-01_ダッシュボード.md ← SC-01 画面設計（機能詳細）
│           ├── SC-02_通知一覧.md ← SC-02 画面設計（機能詳細）
│           ├── SC-12_クエスト詳細.md ← SC-12 画面設計（機能詳細）
│           ├── SC-22_アイデア詳細.md ← SC-22 画面設計（機能詳細）
│           ├── SC-24_アイデアチャット.md ← SC-24 画面設計（機能詳細）
│           ├── SC-25_評価画面.md ← SC-25 画面設計（機能詳細）
│           ├── SC-10_クエスト一覧.md ← SC-10 画面設計（機能詳細）
│           ├── SC-11_クエスト作成編集.md ← SC-11 画面設計（機能詳細）
│           ├── SC-21_アイデア登録編集.md ← SC-21 画面設計（機能詳細）
│           ├── SC-30_ショップ.md ← SC-30 画面設計（機能詳細）
│           ├── SC-31_アバター着せ替え.md ← SC-31 画面設計（機能詳細）
│           ├── SC-32_魔法スキル.md ← SC-32 画面設計（機能詳細）
│           ├── SC-40_実績バッジ.md ← SC-40 画面設計（機能詳細）
│           ├── SC-41_ランキング.md ← SC-41 画面設計（機能詳細）
│           ├── SC-90_クエストグループ管理.md ← SC-90 画面設計（機能詳細）
│           ├── SC-91_システム管理.md ← SC-91 画面設計（機能詳細・会社一覧）
│           └── SC-92_会社詳細.md ← SC-92 画面設計（機能詳細・会社詳細/設定）
│       （今後: mocks/SC-xx_*.html〔各画面モック〕・screens/SC-xx_*.md〔画面設計〕を追加予定）
└── handoff.md             ← このファイル
```

## 2. アプリのコンセプト（確定）

- **ideaquest = ゲーム感覚で楽しくアイデアを出し・管理できる WEB アプリ**。テーマはゲーミフィケーション。
- **ターゲット = 企業**（社内でアイデアを集めたい組織）。Docker で動く簡易マルチテナント（会社単位でDB分離）、ロールあり。
- 中心概念:「**クエスト**」= アイデア出しのテーマ単位。カテゴリー・目的/テーマ・期限日・参加メンバー（パーティー）を設定して作成。
- 全体フロー: クエスト作成 → メンバーがアイデア投稿 → 賛成/反対の投票＋アイデア別チャットで議論 → 評価者が5観点で採点・コメント → 集計・選定。
- ゲーミフィケーション: 行動で **XP**（→レベルアップ）と **コイン**（→ショップで装備購入）を獲得し、**3Dアバター**を着せ替え。

## 3. これまでに決定した要件（確定事項）

すべて `doc/要件定義/README.md` に反映済み。

- **ターゲット = 企業**（簡易マルチテナント。ロール: 一般 / クエストグループ管理者 / システム管理者）。
- **クエスト**の入力項目: カテゴリー / 目的・テーマ / 期限日 / 参加メンバー（パーティー）。
- **アイデア**の入力項目: 件名 / タイムリミット / 利害関係者 / アイデア本文 / 価値 / 備考・特記事項 / 関連資料ファイル添付。**必須は 件名・アイデア本文・価値**（他は任意）。
- **アイデアごとにチャットグループ**を自動作成し、メンバーが議論。
- **評価**: 評価者がアイデアを5観点（新規性・影響度・実現度・適合性・コスト）で各5点満点採点＋観点別コメント。
- **評価者の定義**: クエスト作成者＋作成者が評価者権限を付与した参加メンバー（クエスト単位の権限、複数可）。付与対象は参加メンバー限定。
- **クエスト内の権限モデル（6権限）**: 所有者 / クエスト管理 / 評価者 / アイデア投票 / アイデア作成 / コメント作成。
  - 所有者 = 全操作可。**作成者は既定で所有者・剥奪不可**。所有者権限の他者への付与は**作成者のみ**。
  - クエスト管理権限 = クエスト編集（参加メンバー調整・権限付与/剥奪含む）。所有者/管理権限者がその他権限を付与/剥奪できる。
  - 権限を持てるのは参加メンバーのみ。
  - **新規参加メンバーの既定権限 = 投票＋アイデア作成＋コメント**（他は手動付与）。
  - **パーティーから外すと権限は失うが、それまでの入力（アイデア/投票/評価/コメント）は削除せず表示継続**。
- **投票の仕様**: 賛成/反対、1人1票・締切まで変更可。**自分のアイデアにも投票可**。**投票者にXP付与（初期値5・日次上限対象）**。**評価スコアには非影響**（投票と評価は独立）。
- **投票の匿名化（会社ごとON/OFF・既定ON）**: 投票者を公開する記名モードと、集計数のみ表示する匿名モードを**会社（テナント）単位**で切替。**既定=ON（匿名）**。記名モード=賛成/反対のアバターを並べて表示、匿名モード=賛成数/反対数のみ。**投票自体は内部保持**（本人の変更・XP付与に使用、匿名化は表示のみ制御）。
  - **匿名モード時の所有者/管理者への開示**も会社単位で設定（`hide_voters_from_managers`・**既定ON=管理者にも隠す**）。OFFにすると所有者/クエスト管理権限者だけは投票者を確認可（一般には非表示のまま）。
  - 設定は**システム管理者が会社管理の一環で実施**。データ配置: 管理DBの**Companyに `vote_anonymized`（既定ON）／`hide_voters_from_managers`（既定ON）の2フラグ**を持たせ、ログイン時の会社判定と併せて参照。
- **アイデア入力の必須/任意**: **件名・アイデア本文・価値を必須**。タイムリミット・利害関係者・備考・添付は任意。
- **チャット参加範囲**: **パーティーメンバー全員が閲覧・投稿可**（既定でコメント作成権限）。**添付・`@`メンション・通知すべて対応**（通知はMVPアプリ内通知、外部通知は将来）。
- **アイデアの公開範囲**: **そのクエストのパーティー内のみ**（会社全体・グループ全体には非公開。管理者は管理機能の範囲で閲覧）。
- **添付ファイル（アイデア／チャット共通）**: 実体は **S3互換オブジェクトストレージ MinIO（Dockerコンテナ）** に保管し会社ごとにバケット/プレフィックスで論理分離。**物理ファイル名はハッシュ化**（元名で保存しない・パストラバーサル等対策）。**DBはファイルパス＋元アップロードファイル名**（＋サイズ/MIME/アップロード者/日時）を保持。上限サイズ・許可形式の具体値はTBD（初期値設定）。ダウンロードは権限検証＋署名付きURL想定。**compose定義は未作成＝実装フェーズで作成**。
- **ゲーミフィケーション（XP/コイン/レベル）**（数値は初期値・調整可）:
  - **XP付与（成果重視カーブ）**: 自アイデア選定=200 / アイデア投稿=50 / 評価実施=30 / ログイン=10 / 投票=5 / チャット=5。**軽い行動（投票/チャット/ログイン）は1日の付与上限で乱獲対策**。
  - **コイン**: 行動では付与せず、**アイデアの評価点連動でのみ付与（希少）**。全評価者×全5観点の**平均点連動** = `round(平均点×10)`（最大50/アイデア）。投稿者に付与。
  - **レベル**: **総量式の線形上昇**。Lv n→n+1 必要XP = `100+(n-1)×50`（Lv1→2=100、以降+50ずつ）。レベルアップ報酬なし・上限当面なし。
- **5観点の重み付け**: **均等（重みなし）で単純平均**（決定）。コイン付与の平均スコアも同様に均等平均。将来必要なら拡張検討。
- **装備アイテム**（数値は初期値）: **5スロット構成**（頭/顔/体/手持ち/背景・各1点装備）。**入手はコイン購入のみ**（レベル解放・実績報酬なし）。**レアリティ3段階の幅広い価格帯**＝コモン10〜30 / 標準50〜150 / レア300〜800コイン。購入時はサーバー側で残高・価格を検証。
- **3Dアバター**: モデル形式は **VRM**（three-vrm で R3F 連携）。着せ替えは **パーツ差し替え方式**（ベースアバターに5スロットごとの装備メッシュを取り付け・差し替え）。パーツ制作ガイドライン・スキニング詳細はTBD。
- **技術スタック（決定）**: フロント=**Next.js（React）＋Three.js/R3F＋three-vrm** ／ バックエンド=**Python・FastAPI**（ログイン時に管理DBで所属会社判定→会社DBへ動的ルーティング。SQLAlchemy等で接続切替）／ DB=**PostgreSQL**（管理DB1＋会社DB N・全てDockerコンテナ。将来k8s＋CloudNativePGと整合）。
- **提供形態 = Docker コンテナで動く簡易マルチテナント（簡易 SaaS）**。1 デプロイに複数の会社（テナント）が同居。
- **1 デプロイの構成** = Web サーバー(リバプロ)1 ＋ フロント1 ＋ バックエンド1 ＋ 管理DB1 ＋ 会社DB N ＋ **MinIO(S3互換オブジェクトストレージ)1**。バックエンドは1つで、**ログイン時に管理DBで所属会社を判定し、その会社DBへ動的にルーティング**。
- **DB 構成 = 2 層**。①**管理 DB（システム共通・1つ）**＝認証/アカウント/所属会社/クエストグループ所属/会社DB接続情報を一元管理。②**会社 DB（会社ごとに物理分離・別コンテナ/インスタンス）**＝クエスト・アイデア等の機密データを隔離。
- **クエストグループ（会社DB内の中間グルーピング）** = ユーザーは複数所属可。クエストは1つのクエストグループに属す。**表示されるのは「所属グループ内で作られ、かつ自分がパーティー参加中」のクエストのみ**。クエスト作成時に選べるパーティー候補は同一クエストグループ所属者に限定。
- **管理画面 2 階層** = ①システム管理者（運営: 会社作成・DBプロビジョニング・**全アカウント／クエストグループ所属をフル操作**）②クエストグループ管理者（自グループ内アカウントの登録・編集・論理削除のみ）。
- **認証方式 = 独自アカウントで開始**、SSO（SAML/OIDC）連携は将来対応（Could）。**アカウントは管理者発行のみ（自己新規登録なし）／初回ログイン時にパスワード設定**。
- **画面設計の進め方（決定）**: ①`doc/画面設計/画面遷移図.md` で全体フロー合意 → ②画面ごとにワイヤーフレーム → ③**HTML/CSSモック**（`doc/画面設計/mocks/SC-xx_*.html`）→ ④画面設計＝機能詳細（`doc/画面設計/screens/SC-xx_*.md`）。画面IDは SC-00〜SC-91 を採番済み（遷移図参照）。
- **クエストのカテゴリー = 事前定義リストからの選択＋自由入力の併用**。
- **評価「コスト」観点 = 低コストほど高得点**（5点＝非常に低コスト）。5観点の重み付けは**均等（重みなし）で確定**。

## 4. まだ決まっていない主な TBD（次に検討する候補）

要件定義書 第12節に一覧あり。優先度の高そうなもの:

- **XP日次上限の具体値**・コイン付与の**確定タイミング**（評価締切一括 or 都度再計算）・レベル上限の要否。
- 添付ファイルの**上限サイズ・許可形式の具体値**（保管先＝MinIO・命名＝ハッシュ・DB保持情報は決定済み）。
- テナント（会社DBコンテナ）数の上限・リソース設計。
- **管理DB↔会社DBのUser／クエストグループ所属の同期・配置**。
- 装備アイテムの具体ラインナップ、VRMパーツ制作ガイドライン・スキニング詳細。
- マネタイズの有無（コイン課金など）。

（済）提供形態=Docker簡易SaaS / 構成=Web1+フロント1+バックエンド1+管理DB1+会社DB N（バックエンドがログイン時に会社DBへ動的ルーティング）/ DB=会社ごと物理分離＋管理DBでアカウント一元管理 / クエストグループ=会社DB内グルーピング（複数所属可・パーティー候補は同一グループ・表示は参加クエストのみ）/ 管理画面=システム管理者＋クエストグループ管理者の2階層 / プロビジョニング=compose単一ファイル手編集＋.env管理（MVP）/ 認証=独自＋将来SSO / カテゴリー=事前定義＋自由入力 / コスト観点=低コストほど高得点。

## 5. 次にやること

- **進め方（運用ルール）**: TBD をユーザーと1つずつ確認 → 確定したら README（第12節のチェックも）＋この handoff を更新 → **ユーザー承認を得てからコミット＆プッシュ**。まとめて質問すると効率的（`AskUserQuestion` を活用）。
- **現在: 画面設計フェーズ**。`doc/画面設計/画面遷移図.md` の全体フローは**合意済み**（遷移レベルの未決なし）。
  - 確定した画面フロー: **アイデア一覧はクエスト詳細(SC-12)に内包**（旧SC-20廃止）／**ダッシュボードから参加中クエストへ直行ショートカット**／**実績(SC-40)・ランキング(SC-41)を初期スコープに追加**。画面IDは SC-00〜SC-91。
  - **デザイン標準を確定（2026-07-15）**: トーン=**バランス型**（クリーンな業務SaaS＋ゲーム要素だけ**レトロ・ドット絵**の二層構成）／カラー=**ブルー基調**／モック方式=**共通CSS＋デザイントークン**。成果物は `doc/画面設計/デザイン標準.md`・`mocks/shared.css`・`mocks/style-guide.html`（ブラウザで目視確認可）。以降のモックは必ず shared.css を参照する。
  - **RPG風メッセージ演出を部品化**: XP獲得/レベルアップ通知を**文字送り（タイプライター）**で表示する `.pixel-dialog`（＋`.pixel-caret`／`▼`続き表示）を shared.css に用意。実装ではJSで1文字ずつ表示。**トリガー・表示位置（例: 画面下部にトースト的に／モーダル／クリック送り or 自動送り）・送り速度・効果音は画面設計（SC-01等）で確定する未決事項**。
  - **マスコット演出**: `.mascot`（`mascot-hero.png`）＝**カードにホバーすると右上に出現し、カード間を飛んで移動**。モックは vanilla JS 簡易版。**本番のカード間なめらか移動は framer-motion 等の共有要素アニメーション（layoutId）で実装＝画面作成フェーズに持ち越し**（ユーザー合意済み）。
  - **フォーム部品の標準を追加（2026-07-15）**: 「クリック可能な要素は指ポインター」で統一。
    - `.checkbox`（ネイティブinputを装飾・ラベル/ボックス両方 pointer・focus-visibleリング・indeterminate/disabled対応）。
    - `.select` 本体を pointer 化。**候補(option)はOS描画で cursor 不可** → 候補まで作り込むなら `.combobox`。
    - `.combobox`（`<button>+listbox` のカスタムコンボボックス。**候補ホバーで指ポインター＋ハイライト**、キーボード対応）。挙動は **`mocks/shared.js`**（`[data-combobox]` を初期化。shared.css と対のモック共通JS）。
    - **トグルスイッチ `.switch` は新設済み（2026-07-19）**（管理画面のON/OFF設定〔MFA必須・投票匿名化 等〕向け＝SC-91 で使用）。
  - **ワイヤーフレームの方針（合意）**: 全画面必須にはしない。単純画面（SC-00等）はいきなりモック、情報密度が高い画面（SC-12/SC-22等）だけモック前に軽いテキストワイヤーで要素を洗い出す。
  - 次アクション: **1画面ずつ（必要なら軽ワイヤー→）HTML/CSSモック→画面設計(機能詳細)** の順で詰める。
  - **SC-00 ログイン = 完了**（モック `mocks/SC-00_ログイン.html` ＋画面設計 `screens/SC-00_ログイン.md`）。背景=login-bg.jpg、カードはフロステッドガラス（半透明rgba .66＋blur14px）、**ログイン／認証コード(MFA)／初回パスワード設定の3状態**、パスワード表示切替を実装。ロゴ白背景は透過加工済み。
  - **MFA を確定（2026-07-15）**: ID＋パスワード後に**メールOTP（6桁・10分・単回）**。**「このデバイスを信頼する」ONで次回以降その端末はOTPスキップ（有効期限30日）／別端末は常にOTP**。**会社ごとにON/OFF（既定ON）＝Companyに `mfa_required`**。セッション保持は**デバイス信頼に一本化**（「ログイン状態を保持する」は廃止）。管理DBに **TrustedDevice / OTPチャレンジ** を追加。README(FR-03b・データモデル・セキュリティ・第12節)と `screens/SC-00_ログイン.md` に反映済み。残TBD（メール送信基盤・信頼端末管理画面・セッション期間の具体値・閾値・TOTP将来）は画面設計md 第10節。
  - **業務層ボタンの押下(:active)デザインを確定（2026-07-16）**: 業務層 `.btn` 系は **1px 沈み込み＋内側の影 `inset`＋塗りを一段濃く**のクリーンな挙動（ゲーム層 `.btn-pixel` のハードシャドウ・バウンドとは意図的に差別化）。バリアントごとに active 色を定義（primary=`--color-primary-active` / danger=`--color-danger-active`〔新規トークン追加〕 / outline=`--color-border` / 標準=`--color-border-strong`）。併せて **`.btn-danger` の hover も追加**（従来なし）。`prefers-reduced-motion` 時は沈みを止め色変化のみ。shared.css・style-guide.html（3節にhint追記）・デザイン標準.md（3・4節）に反映済み。
  - **ゲーム層マテリアル＝「CRTガラス」を確定＋「1レイヤー1マテリアル」方針を追加（2026-07-18）**: ゲーム層（`.pixel-panel`/`.pixel-dialog`）を**半透明の暗ガラス＋走査線＋上端反射（＝古いゲーム機の画面ガラス）**に一新（`--pixel-glass-*` トークン・`backdrop-filter`・ドット感〔角丸なし/ハード枠・影/ピクセルフォント〕は維持）。背景（`.app-bg`）を透かして映える。**方針＝1レイヤー1マテリアル**: このガラスは**ゲーム層のみ**、**業務層はクリーン不透明のまま**（読ませる本文パネルの可読性＋背景画像スクリムとの整合を守る）。**業務パネルの区別は材質でなく色アクセントで**（下書き=グレー/未投票=青/フォロー=水色/参加中=クエスト色）。ガラス案の比較検討で A(フロステッド)/B(クリスタル)/C(CRT)/D(ネオン) を試作し **C を採用**。shared.css（`:root` トークン・`.pixel-panel`・`.pixel-dialog`）／style-guide.html（6節に背景付きで実演＋方針明記）／デザイン標準.md（0節の二層表・新設「1レイヤー1マテリアル」節・4節）に反映。**残: `.pixel-stat` ピルをガラスに合わせるか。**
  - **業務パネルの用途別カラーアクセントを実装（2026-07-18）**: 「材質でなく色で区別」の方針を具体化。共通部品 `.card-accent`（左端の色帯・擬似要素なのでホバーの影/枠変化に不干渉・色は `--accent`）を shared.css に追加。ダッシュボード（SC-01）で **下書き=グレー／未投票=プライマリ青／フォロー中=インフォ水色／参加中クエスト=クエスト色** を適用。**`.quest-icon__char`（頭文字タイル）も `--accent` 駆動**にし、参加中クエストカードでは帯とアイコン色が連動（per-quest カラー。3枚に個別色をインライン指定）。style-guide（5節にアクセント見本）／デザイン標準（4節カード・クエストアイコン）に反映。
  - **SC-01 ダッシュボード = 完了（2026-07-16）**（モック `mocks/SC-01_ダッシュボード.html` ＋画面設計 `screens/SC-01_ダッシュボード.md`）。**上部2カラム（ゲーム層）＝左ヒーロー（アバター/Lv/XPバー/コイン/SHOP・きせかえ）＋右 週間ランキングTOP3** ／ **下書きパネル（最上段・クリックで作成/編集モーダル）** ／ **未投票アイデアパネル（投票ショートカット・カード内賛成/反対＋クリックで投票モーダル）** ／ **参加中クエスト→SC-12直行カード** ／ **フォロー中アイデアのパネル（クリックで拡大モーダル）** ／ **下段（最近の通知→SC-02／ショートカットタイル／ロール条件付き管理導線SC-90/91）**。ヘッダーに通知ベル（未読バッジ）＋アバターメニュー。**ログインボーナス演出＝画面下部トースト・自動送り（.pixel-dialog土台・数秒でフェードアウト・reduced-motion対応）で確定**。**週間ランキング＝今週の獲得XP＋獲得コインの単純合算スコア（初期値・調整可）で会社内順位・週次リセット**、TOP3＋自分の順位を表示、全件は SC-41。残TBD（レベルアップ演出・通知ドロップダウン要否・参加中クエストの件数/並び・API集約or分割・ランキングのXP/コイン重み・週起点/同点処理・フォロー詳細・3Dアバター表示方法）は画面設計md 第10節。
  - **下書き（ドラフト）機能を確定（2026-07-16）**: クエスト・アイデアを**下書き保存**可。**作成者本人のみ表示**（公開/投稿までパーティー非公開）。**XP・コインは公開/投稿時に付与**（下書きでは付与しない）。**ダッシュボード最上段（参加中クエストの上）に下書きパネル**、**クリックでフォロー同様の拡大アニメーション（FLIP／本番は framer-motion `layoutId`）→ 作成/編集モーダル（SC-11/SC-21 のクエスト/アイデア編集フォーム）を開いて続きを編集**。下書き0件ならセクション非表示。README（FR-29・Quest/Idea の status に「下書き」追加・第12節）／画面遷移図／SC-01 に反映。残TBD（自動保存・保持期間/上限・下書きクエストのパーティー未確定の扱い）。
  - **コンテンツ背景画像のパーソナライズを確定（2026-07-16）**: 認証済みユーザーが**あいさつ行右上の「🖼️ 背景画像」ボタン**からコンテンツ領域の背景画像をアップロード設定でき、**その利用者の全認証画面に反映**（ユーザー個人設定・実体は MinIO・`User.background_image_path`）。可読性のため薄い明色スクリム（`.app-bg` を shared相当でSC-01に実装／デザイン標準に記載）。モックは localStorage で全画面反映を簡易再現。FR-30・README第8/12節・デザイン標準・SC-01（4.11）に反映。残TBD（上限サイズ/形式・既定背景・会社による禁止/固定設定の要否）。
  - **未投票アイデアの投票ショートカットを確定（2026-07-16）**: 参加クエストで**自分が未投票のアイデア**を**下書きの下**にパネル表示。**カード内の賛成/反対でクイック投票**、**パネルクリックでFLIP拡大→投票モーダル**（アイデア要約＋賛成/反対）。投票=SC-22内アクションと同一（1人1票・締切まで変更可・XP付与・匿名化は表示のみ）。投票済みは未投票リストから外れ、0件でセクション非表示。FR-23に追記／画面遷移図／SC-01（4.5）に反映。残TBD（件数/並び・投票取消の扱い）。**未投票・フォロー中パネル（＋各モーダル）に投稿者（`.poster`＝アバター＋氏名）を表示（2026-07-16追加。`.poster` を shared.css に昇格）**。
  - **アイデアフォロー機能を確定（2026-07-16）**: 参加中クエストのアイデアを**フォロー＝ウォッチ**（新規コメント・評価・選定 等を通知）。**ダッシュボードにフォローパネル**（参加中クエストの直下）を置き、**パネルクリックで拡大アニメーション→SC-22アイデア詳細をモーダル表示**。README（FR-28・データモデル Follow・第12節）／画面遷移図／SC-01 に反映。
  - **UI標準: 登録・編集・（一覧/ダッシュボードから開く）詳細はモーダルダイアログを確定（2026-07-16）**: SC-11・SC-21 は個別画面でなく**モーダル**。本番は **Next.js Parallel Routes（@modal）＋ Intercept Routes（(.)）** で URL を持つモーダル（リロード/直アクセスはフルページ）。パネル/カード→モーダルの拡大は **framer-motion `layoutId`**（共有要素アニメーション）。デザイン標準.md に `.modal` 標準を追記。モックは vanilla JS の FLIP 簡易版で実演。
  - **クエストアイコンを確定（2026-07-16）**: クエストごとに**アイコン画像を任意設定**でき、**未設定時は「件名の頭文字タイル＋所有者アバターを右下に重ねた」フォールバック**を自動描画。共通コンポーネント `.quest-icon`（`.sm/.lg`・`__img`/`__char`/`__owner`）を shared.css に追加。設定は作成/編集モーダル（SC-11）。表示は SC-12ヘッダー・SC-01参加中クエストカード（＋今後のクエスト一覧）。実体は MinIO・`Quest.icon_image_path`。README（FR-20・Quest）／デザイン標準／SC-12・SC-01 に反映。
  - **全文検索タブ（クエスト内・日本語）を追加（2026-07-16）**: SC-12 に **`🔍 全文検索` タブ**。**アイデアの件名/本文/価値/備考＋チャット本文**を日本語全文検索し、**種別バッジ＋所属アイデア＋ハイライト付きスニペット**で表示、クリックで SC-22/SC-24 へ。対象フィルタ（すべて/アイデア/チャット）・件数・空表示あり。**エンジンは PGroonga 推奨（分かち書き不要・DB内完結・要ユーザー確認）／代替 pg_bigm／将来 Meilisearch・OpenSearch**。モックはクライアント側の部分一致＋ハイライトで再現。FR-31・技術スタック・第12節／画面遷移図／SC-12（4.2b・API）に反映。**残：エンジン確定・索引/更新方式・添付名を対象に含めるか**。
  - **SC-12 クエスト詳細 = 完了（2026-07-16）**（モック `mocks/SC-12_クエスト詳細.html` ＋画面設計 `screens/SC-12_クエスト詳細.md`）。**上部2カラム＝左クエストヘッダー（カテゴリー/ステータス/件名/目的テーマ/メタ＋アクション）＋右クエスト内週間ランキング**（`.rank-panel` を shared.css に昇格し全社ランキングと共用。スコア＝当該クエストの活動で得たXP＋コインの合算・週次・Activity.quest_id で集計）。**ヘッダーにクエストアイコン（未設定=件名頭文字＋所有者アバター）・所有者名の左にアバター**。下に**タブ切替〔💡アイデア/👥パーティー/🔍全文検索/📋概要〕（各タブ左にアイコン）**。**アイデア一覧に一覧標準（検索＋フィルター〔状態/評価〕＋並び替え＋件数/空表示）を実装**（`.list-toolbar`/`.list-count`/`.list-empty` を shared.css に昇格＝全一覧の標準）。**アイデア一覧＝テーブル（行リスト：件名/投稿者/賛成反対/コメント/評価/自分の状態）**、行クリックで SC-22。**アイデア追加＝SC-21モーダル／クエスト編集・パーティー権限編集＝SC-11モーダル**（登録編集はモーダル標準）。テーブル標準 `.table`/`.table-wrap` を shared.css に追加（タブ `.tabs` はSC-12ローカル）。背景画像(.app-bg)も反映。残TBD（ページング/絞り込み・タブURL保持・SC-22モーダル差し込み範囲・締切後の可否ルール）は画面設計md 第9節。
  - **SC-22 アイデア詳細 = 完了（2026-07-18）**（モック `mocks/SC-22_アイデア詳細.html` ＋画面設計 `screens/SC-22_アイデア詳細.md`）。**ヘッダー（カテゴリ/ステータス/件名/投稿者/フォロー★/編集/メタ）＋メイン＋右レールの2カラム**（狭い/モーダルは1カラム縦積み）。**メイン＝アイデア内容〔価値・本文・利害関係者〕／関連資料〔添付〕／チャット〔導線＋直近プレビュー・SC-24へ〕**。**右レール＝投票〔賛成/反対インライン・1人1票締切まで変更可・再クリック取消・匿名/記名対応・+5XP〕／評価結果〔5観点バー＋均等平均＋コイン`round(平均×10)`＋観点別コメント＋公開範囲バッジ・評価者はSC-25へ〕／情報メタ**。**一覧/ダッシュボードから開く際はモーダル差し込み（Intercept・URL直はフルページ）**。**新要件＝評価結果の公開範囲を評価者が指定（既定=パーティー全員／限定=投稿者＋評価者＋所有者/管理）→ README第6節・データモデル `Evaluation.visibility`・第12節に反映**。残TBD（限定時の範囲外への見せ方／記名モードUI／チャットプレビュー件数／締切後の可否／モーダル時の右レール畳み方）は画面設計md 第9節。
  - **SC-24 アイデアチャット = 完了（2026-07-18）**（モック `mocks/SC-24_アイデアチャット.html` ＋画面設計 `screens/SC-24_アイデアチャット.md`）。**フラットリスト（Slack/Teams風）**で決定。**上部＝コンパクト文脈バー（クエスト名/件名/参加人数・SC-22へ戻る）／中央＝メッセージスレッド（日付セパレータ・未読セパレータ・アバター＋氏名＋時刻＋本文＋添付チップ・`@`メンション強調〔自分宛は警告色〕）／下部＝入力欄（sticky・自動リサイズ・📎添付チップ・@メンション候補ポップアップ〔↑↓/Enter/Tab/Esc〕・Enter送信/Shift+Enter改行・+5XP注記）**。コメント権限＝既定付与、閲覧＝パーティー全員。モックはJSで送信/メンション候補/添付を実演。
    - **SC-24 追加設計（2026-07-18）**: ①**メッセージのホバーアクション**（🙂リアクション/💬引用返信/自分の投稿は✏️編集〔インライン・「編集済み」〕・🗑削除〔トゥームストーン〕）②**添付のリッチ表示**（画像インラインサムネイル＋クリックでライトボックス拡大）③**リアクション詳細**（ホバーで誰が押したか＝モックは title・本番はポップオーバー）④**入力欄の最小化**（右上「⌄ 最小化」で折りたたみ、スリムバーで再展開）。SC-24 mock/doc に反映。
    - **SC-24 コメント入力UIを刷新（2026-07-18）**: 「全幅テキストエリア＋下部ツールバー（左＝📎添付/@メンション挿入/😀絵文字/太字/コード/🔗リンク、右＝送信）」のリッチコンポーザーに変更（📎と最小化の重なり解消も兼ねる）。**簡易書式（Markdownライト: `**`太字/`` ` ``コード/`[label](url)`リンク/`@`メンション）を本文で装飾表示**（renderText拡張）。**空メッセージは送信不可（送信ボタン無効化）**。絵文字はカーソル位置へ挿入。SC-24 mock/doc（4.3）に反映。残TBD（編集/削除の期限・履歴・管理者削除／スレッド返信／リアルタイム配信／締切後可否／絵文字セット・リアクション上限）は画面設計md 第9節。
  - **チャットリアクション＋スキルポイント/魔法を新設（2026-07-18・要件追加）**: ①**チャットに2層リアクション**＝通常（絵文字・無料・複数可）＋**魔法**（SP解放・エフェクト・**各魔法1チャット1回・取消/付け替え可**）。②**レベルアップで「スキルポイント(SP)」を獲得**（新通貨・初期値+1/Lv）。③**SPで魔法を解放**（新画面 **SC-32 魔法/スキル**）。④魔法は**チャットの魔法リアクション**として発動し、対象メッセージに**永続エフェクト**（例: 🔥炎＝枠が燃える／氷/雷/キラキラ）。装飾/社交演出のみ（XP/評価/投票に非影響）・`prefers-reduced-motion`尊重。**反映済**: README（FR-32/33・第6節「スキルポイント&魔法」・データモデル `User.skill_point_balance`/`Spell`/`UserSpell`/`Reaction`・第12節）／画面遷移図（SC-32追加・SC-24リアクション）／デザイン標準（`.reaction`系・`.spell-fx--*`・`--pixel-skill`・"業務チャットへの遊びのクロスオーバー例外"明記）／shared.css（`.reaction-bar`/`.reaction`/`.reaction-add`/`.spell-fx--fire`ほか・`.pixel-stat.skill`）／**SC-24 mock＋doc**（リアクションピッカー〔通常絵文字＋魔法〕・炎エフェクトのデモをJSで実装）。
    - **魔法の使用ルールを変更（2026-07-18）**: 当初の「クールダウン制」→**「各魔法は1アイデアチャットにつき1回のみ・通常リアクション同様に取消して別メッセージへ付け替え可」**に変更（炎・氷・雷…をそれぞれ1回ずつ。「これだ」と思うメッセージに付ける体験）。README（FR-33・第6節・データモデル Reaction のユニーク制約`chat_group×user×spell`・第12節）／SC-24 mock（`spellPlacement`で魔法ごと1回制御・魔法チップクリックで取消）＋doc に反映。
    - **1メッセージ1魔法に制限（2026-07-19）**: 上記に加え**1つのメッセージに付けられる魔法は1個まで（種類問わず）＝他ユーザが既に魔法を付けていると付けられない（早い者勝ち・エフェクト重なり防止）**。通常リアクション（絵文字）は対象外（複数・複数人可）。SC-24 mock（`messageMagicChip` で占有判定・`castSpell` にガード・ピッカーは占有時「既に魔法が付いています」を表示・他ユーザ魔法を1件シードして体験可＝`seedForeignSpell`）＋doc、README（FR-33・第6節「1メッセージ1魔法」・Reaction を `message_id` ユニークにも制約・チェックリスト）に反映。
    - **残TBD**: SP付与量/Lv・魔法カタログ（種類/エフェクト/SPコスト）・通常リアクションの絵文字セット・1メッセージのリアクション上限・エフェクトの他画面表示可否。
  - **SC-25 評価画面 = 完了（2026-07-18）**（モック `mocks/SC-25_評価画面.html` ＋画面設計 `screens/SC-25_評価画面.md`）。**モーダル標準**（SC-22「評価する」から差し込み・URL直/リロードはフルページ）。**5観点（新規性/影響度/実現度/適合性/コスト）を★スター採点**（コスト＝低コストほど高得点をラベル明示）＋**観点別コメント**。**集計プレビュー**（本評価の平均＝均等平均・採点数）。**公開範囲＝ラジオ（🔓パーティー全員〔既定〕/🔒限定）**。確定時に全観点必須を検証（デモ）。評価者権限のみ。JSで★採点/ホバー/平均計算/公開範囲ハイライトを実装。
    - **SC-25 追加（2026-07-18）**: ①**評価の下書き保存**（`Evaluation.status`＝draft/submitted・本人のみ・全観点不要・XP/コインは確定時のみ付与）②**モーダルをドラッグ移動可**（ヘッダーつかんで移動＝背後SC-22を確認）③**「アイデアを確認」折りたたみ**（価値/本文/添付を `<details>` で埋め込み・移動不要で確認可）。README（FR-25・第6節・データモデル Evaluation.status・第12節）／SC-25 mock＋doc に反映。残TBD（限定時の範囲外見せ方・コイン確定タイミング・締切後可否・コメント必須/上限・評価取下げ・★半点）は画面設計md 第9節。
  - **SC-10 クエスト一覧 = 完了（2026-07-18）**（モック `mocks/SC-10_クエスト一覧.html` ＋画面設計 `screens/SC-10_クエスト一覧.md`）。**一覧標準（検索＋状態/グループ絞り込み＋並び替え＋件数/空/クリア）＋カード/リスト表示切替**（`.viewtoggle`・同一データからJSでカードとテーブルを描画）。**クエストアイコン＋クエスト色アクセント**（`--accent`）。カード/行→SC-12、＋作成→SC-11（モーダル）。表示対象＝所属グループ×参加中（FR-15・サーバー強制）。残TBD（ページング・URL保持・下書きを一覧に出すか・既定表示の記憶・グループ横断のグルーピング）は画面設計md 第9節。
  - **共通ヘッダーを全画面で統一（2026-07-18）**: 認証後の全画面ヘッダーを**ダッシュボード(SC-01)と同一**に統一＝`.header-actions`（Lv/コインの `.pixel-stat`＋**通知ベル `.bell`〔未読バッジ〕**＋**アバターメニュー `.usermenu`**〔プロフィール/アバター(SC-31)/設定/ログアウト〕）。**ヘッダーCSSを shared.css へ、`.usermenu` 開閉JSを shared.js へ昇格**（SC-01 のローカル定義は削除）。SC-10/12/22/24/25 の簡素ヘッダー（Lv/コイン＋「通知」ボタン）を置換。**SP（スキルポイント）を全ヘッダーに表示**（ダッシュボード含む・Lv/コインの隣・`a.pixel-stat.skill`＝SC-32へリンク・SC-32未作成の前方リンク）。デザイン標準4節に共通ヘッダー（Lv/コイン/SP＋ベル＋アバターメニュー）を明記。SC-00ログインは対象外。
  - **SC-11 クエスト作成・編集 ＝ 完了（2026-07-18）**（モック `mocks/SC-11_クエスト作成編集.html` ＋画面設計 `screens/SC-11_クエスト作成編集.md`）。**モーダル標準**（SC-10「＋作成」/SC-12「編集」/SC-01下書き から差し込み）。**クエストアイコン（アップロード/未設定フォールバック・件名頭文字と連動）／件名／カテゴリー〔combobox 事前定義＋自由入力〕／期限日／目的・テーマ／クエストグループ〔パーティー候補を規定〕／パーティー・権限**。**パーティー＝6権限トグルチップ**（所有者/管理/評価者/投票/作成/コメント。作成者＝所有者固定・剥奪/削除不可、候補〔同一グループ〕から追加時に既定権限〔投票/作成/コメント〕自動付与、所有者付与は作成者のみ）。ドラッグ移動・必須チェック。**新規要件を発見→確定: クエストに「件名（必須）」を追加**（要件表に無かったがアイコン頭文字・一覧表示に不可欠。README 第6節「クエストの入力項目」表に追記済み）。残TBD（カテゴリ自由入力の正規化／編集でのグループ変更可否／候補の検索・ページング／下書きのパーティー未確定／ステータス手動遷移UI）は画面設計md 第9節。
  - **SC-21 アイデア登録・編集 ＝ 完了（2026-07-18）**（モック `mocks/SC-21_アイデア登録編集.html` ＋画面設計 `screens/SC-21_アイデア登録編集.md`）。**モーダル標準**（SC-12「アイデア追加/編集」/SC-01下書き から差し込み）。**必須3項目（件名/価値/本文）を上部固定＋任意項目〔タイムリミット/利害関係者/備考〕を `<details>` 折りたたみ**（投稿ハードルを下げる）。**関連資料＝添付ドロップゾーン（クリック＋D&D・種別アイコン/サイズ/削除・複数可・任意）**。**必須3項目が揃うまで「投稿する」無効**、投稿でチャットグループ自動作成＋投稿XP。下書き保存（本人のみ・付与なし）・ドラッグ移動。残TBD（添付の上限/形式・数上限／編集削除の範囲・履歴／締切後可否／簡易書式流用／画像インラインプレビュー）は画面設計md 第9節。
  - **既存画面の導線を SC-11/SC-21 へ接続（2026-07-18）**: モックはページ遷移で接続（本番は Parallel/Intercept Routes・パネル→モーダルは layoutId）。**SC-10「＋作成」→SC-11**（alert 廃止）／**SC-12「クエスト編集」「パーティー・権限を編集」→SC-11・「＋アイデアを追加」→SC-21**（SC-12 内蔵の簡易プレースホルダ・モーダル＋アイコンJSは撤去）／**SC-01 下書きカード→ kind に応じ SC-11/SC-21**（SC-01 内蔵の簡易 draftModal＋配線は撤去。vote/follow の FLIP デモは残置）。
  - **複数選択コンボボックス部品 `.multiselect` を新設（2026-07-18）**: **オートコンプリート絞り込み＋選択チップ（×で解除）＋Backspaceで直前解除**の複数選択部品。**3モード**＝①複数＋自由入力（`data-free="true"`・候補に無い語を Enter でタグ追加＝`.is-custom` グレー表示）②複数・候補のみ ③**単一選択 `data-single="true"`**（チップにせず入力欄に確定値表示・オートコンプリート付きセレクト）。shared.css（`.multiselect__control/__chip/__chip-remove/__input/__list/__option/__empty`）／shared.js（`[data-multiselect]` を初期化）／style-guide.html（4節フォームに3実演）／デザイン標準.md（フォーム部品節）に反映。**反映済**: SC-11 カテゴリー＝複数＋自由入力／SC-11 クエストグループ＝単一選択／SC-21 利害関係者＝複数＋自由入力。**要件変更2件を確定**: ①**クエストのカテゴリーを複数選択可に**（1クエスト複数カテゴリ許可）②利害関係者も複数可（README 入力項目表・データモデル Quest/Idea・第12節チェックに反映）。クエストグループは要件どおり**1クエスト＝1グループ（単一選択）**を維持。残: 1メッセージ/フィールドの選択上限・自由入力タグの正規化/重複防止・カテゴリのマスタ昇格。
  - **クエストカラー設定を新設＋部品 `.swatches`/`.swatch`（2026-07-18）**: クエスト色は SC-10/SC-01 で既に使用（カード左帯＋アイコンタイル色 `--accent`）していたのに**設定項目もデータモデルも無い綻び**を解消。**プリセットパレット（600レベル10色）から1色選択**するカラースウォッチ部品を shared.css に新設（`role=radiogroup`・選択にチェック・focus-visible リング）、SC-11 に「クエストカラー（必須・既定色あり）」フィールドを追加（**選択でアイコンプレビューのタイル色 `--accent` が即連動**）。style-guide 4節／デザイン標準（カラースウォッチ節・クエストアイコン節）／README（入力項目表・FR-20・データモデル `Quest.color`）／SC-11 mock+doc に反映。残: カスタム色の要否・アイコン画像設定時の色の扱い。
  - **評価に「総評（全体コメント・必須）」を追加（2026-07-18）**: 観点別コメント（任意）とは別に、**アイデア全体への総括コメント**を評価者が入力。**確定（submitted）時は必須**（下書きは空可）＝全5観点＋総評をサーバー検証。**SC-25** に総評 textarea（`#evalOverall`・`.req`・確定バリデーション）を追加、**SC-22 評価結果**に評価者ごとの総評を表示（左アクセント帯 `.eval-overall__item` で観点別コメントと区別・「総評」「観点別コメント」の見出し付き）。README（FR-25・第6節公開範囲の閲覧項目・データモデル `Evaluation.overall_comment`）／SC-25 mock+doc（4.1b・§5/§6・API PUT payload）／SC-22 mock+doc（4.6）に反映。
  - **SC-22 に「議論アクティビティ・グラフ」＋「変更履歴/差分」＋「投票の見直し」を追加（2026-07-18）**: アイデアは投稿→チャット議論→更新→評価の流れを想定。①**議論の活発度グラフ**（`.activity`/`.spark`＝直近14日の日次メッセージ数の棒グラフ＋今週件数/先週比・参加人数・最終投稿＋🔥活発/落ち着き/💤停滞ラベル＋◆アイデア更新マーカー）をチャットセクション先頭に。②**変更履歴モーダル**（ヘッダー「N回更新（履歴を見る）」から）＝版タイムライン（日時/編集者/変更フィールド/メモ）＋**差分**（テキスト=語句差分 `.diff-add`/`.diff-del`、他=旧→新、添付=追加/削除）＋「あなたの投票より後」区切り。③**投票の見直し**＝自分の投票後に更新があると投票パネルに「⚠投票後に更新（差分を見る）」＋「更新前に投票」バッジ、押し直しで解消（投票は有効のまま）。**更新時は投票者＋フォロワーに通知**。**確定した判断**: 投票=有効のまま＋差分導線＋再確定／グラフ=日次棒＋サマリ＋状態＋更新マーカー／差分=全項目＋テキスト語句差分。README（**FR-34 変更履歴・差分／FR-35 議論アクティビティ**・データモデル `IdeaRevision`・`Vote.voted_revision/voted_at`・Notification 種別）／SC-22 mock+doc（4.4/4.4c/4.5/§5/§7/§9）に反映。残TBD（集計窓14日/状態閾値・差分方式〔サーバー語句差分 or スナップショット〕・軽微編集の版/通知・陳腐化通知の強度・評価者にも投票後更新を示すか）。
  - **背景画像の設定をヘッダーのユーザーメニューへ移動（2026-07-18）**: ダッシュボード（SC-01）あいさつ行右上にあった「🖼️背景画像／リセット」を撤去し、**共通ヘッダーのユーザーメニュー**に「🖼️ 背景画像を変更／背景画像をリセット」（`[data-bg-set]`/`[data-bg-reset]`）として移設＝**全認証画面から設定可**。設定操作は **shared.js に集約**（隠しファイル入力を動的生成・localStorage 保存・`#appBg` へ即反映・メニューを閉じる）。**メニュー項目は全8ページの `.usermenu__list` に追加**（設定/ログアウトの間）。SC-01 は設定JSを撤去し**保存済み背景の復元のみ**残置（他ページの復元スクリプトは既存のまま）。README（FR-30・ダッシュボード項目・第12節）／デザイン標準（共通ヘッダー・背景画像）／SC-01 doc（4.11・アクション・TBD）に反映。
  - **ダッシュボードの下書きパネルに「評価の下書き」も表示（2026-07-18）**: 従来のクエスト/アイデア下書きに加え、**評価の下書き**（`Evaluation.status=draft`・SC-25 で下書き保存）も最上段の下書きパネルにまとめて表示。カードは「⭐ 評価」バッジ＋評価対象アイデア件名＋所属クエスト＋**採点進捗（例: 採点 3/5 観点）**、クリックで **SC-25 評価画面**へ（`DRAFT_ROUTE` に 評価→SC-25 を追加）。README（FR-29・FR-25 評価下書き節・ダッシュボード項目・第12節）／SC-01 doc（§3・4.4）に反映。
  - **SC-32 魔法/スキル ＝ 完了（2026-07-19）**（モック `mocks/SC-32_魔法スキル.html` ＋画面設計 `screens/SC-32_魔法スキル.md`）。**ゲーム層の画面**。**SPステータス＝CRTガラス（`.pixel-panel`）**（SP残高大表示＋レベルアップで+1SP＋解放数 n/6）／**魔法カタログ＝可読カード＋ゲーム要素**（効果プレビューに `spell-fx--*` を実適用・ピクセル見出し・`.btn-pixel`）。**初期カタログ6種を確定（初期値・調整可）**: 炎🔥/氷❄️（コモン1SP）・雷⚡/キラキラ✨（標準2SP）・虹🌈/オーラ🌟（レア3SP）。**レア用に `spell-fx--rainbow`（色相回転）／`spell-fx--aura`（脈動）を shared.css に新設**（SC-24 と共通）。解放＝SP消費（デモは confirm→SP減算→解放済み・SP不足で無効）。共通ヘッダーの SP表示リンク先がこれで実体化（全ページ既に `SC-32_魔法スキル.html` を指す）。README（第6節に初期カタログ・第12節チェックを更新）／画面遷移図（既存）に整合。
    - **系統ごとの段階解放（スキルツリー）を追加（2026-07-19）**: カタログを**2系統×3段階**に再構成し、**上位の解放には同系統の下位魔法が解放済みであることを前提**に。**烈火系**: 炎🔥→雷⚡→虹🌈／**静輝系**: 氷❄️→キラキラ✨→オーラ🌟（起点コモンは前提なし）。SC-32 mock（`.spell-line` で系統セクション化・`data-line`/`data-requires`・各カードに前提表示〔✓達成/🔒未達〕・`refresh()` で前提未達→「前提: ◯◯が必要」でボタン無効・解放で上位が解放可に）。初期状態＝炎/雷/氷 解放済み（3/6）でオーラは前提未達デモ。README（FR-33・第6節「系統ごとの段階解放」・データモデル Spell に `line`/`requires`・第12節）／SC-32 doc（4.2/4.3/§2/§5/§6/§9）に反映。残TBD（SP付与量/Lv最終値・解放取消不可・エフェクトの他画面表示・新系統の追加規格）。
  - **SC-31 アバター / 着せ替え ＝ 完了（2026-07-19）**（モック `mocks/SC-31_アバター着せ替え.html` ＋画面設計 `screens/SC-31_アバター着せ替え.md`）。**ゲーム層の画面**。**2カラム＝左 3Dアバタービューア（`.pixel-panel`＝CRTガラス・sticky。本番は three-vrm/R3F・モックは mascot＋装備絵文字の重ねで見立て）＋右 ワードローブ**。**5スロット（頭/顔/体/手持ち/背景・各1点）ごとに「✕外す」＋所有装備＋未所有（グレーアウト＋価格＋🔒ショップで購入）をグリッド表示**。**所有装備クリックで即着せ替え→ビューアに反映**（JS `equipped`/`render()`・同スロット1点に正規化）、**未所有は SC-30 ショップへ誘導**。コイン残高＋「🛒ショップへ」を上部に。レアリティ3段階（`--rarity-*`）。**入手はコイン購入のみ・購入は SC-30 側**（本画面は付け替えのみ）。要件は既定（README 第6節「装備/アイテム」・FR-08）どおりで**新規要件の発見なし**＝README変更なし。残TBD（装備の最終ラインナップ・未所有のプレビュー/試着方式・VRMパーツ制作ガイドライン・3D非対応環境の代替・ビューア操作/コーデ保存の要否）は画面設計md 第9節。
  - **SC-30 ショップ ＝ 完了（2026-07-19）**（モック `mocks/SC-30_ショップ.html` ＋画面設計 `screens/SC-30_ショップ.md`）。**ゲーム層の画面**。**コイン残高ヒーロー（`.pixel-panel`＝CRTガラス）＋レアリティ帯説明＋一覧標準（`.list-toolbar`＝スロット/レアリティ絞り込み・「買えるものだけ」「未所有だけ」トグル・並び替え〔レアリティ/価格昇降/スロット〕・件数・`.list-empty`）＋装備グリッド**。**装備カード＝レアリティ色リング＋スロット/レアリティ＋名称＋価格。未所有=購入ボタン（コイン不足は無効＋あと◆n）／所有済み=「✓所有済み」＋SC-31導線**。**購入＝confirm→コイン消費→所有済み化＋ヘッダーコイン更新＋購入演出**（JS `buy()`・`prefers-reduced-motion`対応）。**装備セット・レアリティは SC-31 と完全一致**、価格は帯（コモン10〜30/標準50〜150/レア300〜800）に沿う初期値を割当。**入手はコイン購入のみ・着せ替えは SC-31**（役割分担: 買う=SC-30／着る=SC-31）。要件は既定（README FR-07・第6節「装備/アイテム」「コイン付与ルール」）どおりで**新規要件の発見なし＝README変更なし**。**SC-31 の購入導線 `SC-30_ショップ.html` はこれで実体化**。残TBD（ラインナップ最終・未所有プレビュー/試着・購入UI粒度〔confirm or 購入モーダル/カート〕・セール/新入荷・URL保持/ページング）は画面設計md 第9節。
  - **SC-41 ランキング ＝ 完了（2026-07-19）**（モック `mocks/SC-41_ランキング.html` ＋画面設計 `screens/SC-41_ランキング.md`）。**ゲーム層の画面**。**全社（テナント内）全メンバーのランキングを全件表示**＝ダッシュボード週間ランキング「すべて見る」の遷移先。**スコア＝期間内の獲得XP＋獲得コイン**（既存の週間ランキングと同定義）。**期間切替（`.rank-tabs`＝今週/先週/今月/通算・クリーンセグメント）で順位が入れ替わる**（JS `ranked(period)`・同点はXP優先→氏名順）。**あなたの順位サマリー（`.card.card-accent`＝N位/全M人＋「▼自分の順位へ」スクロール）＋表彰台TOP3（`.podium`＝2/1/3位を高さ差で・金銀銅）＋全件リスト（`.rank-panel`＝CRTガラスを流用・順位番号追加・`.is-me`ハイライト）**。**SP はランキング対象外**。**SC-01 の「ランキングをすべて見る →」を `SC-41_ランキング.html` に実体化**。要件は既定（FR-10・第6/10節）どおりで**新規要件の発見なし＝README変更なし**。残TBD（ランキング軸の追加要否・週/月起点/TZ・同点タイブレーク確定・大人数のページング/集計方式・会社内セグメント別・プライバシー〔オプトアウト/会社ON/OFF〕・順位変動演出）は画面設計md 第9節。
  - **SC-40 実績 / バッジ ＝ 完了（2026-07-19）**（モック `mocks/SC-40_実績バッジ.html` ＋画面設計 `screens/SC-40_実績バッジ.md`）。**ゲーム層の画面**。**収集サマリー（`.pixel-panel`＝CRTガラス＋達成率バー〔`.xp-bar`流用〕）＋一覧標準（`.list-toolbar`＝カテゴリ/状態絞り込み・件数・`.list-empty`）＋バッジグリッド**。**バッジカード＝ティア色リング（銅/銀/金・ローカル`--tier-*`）＋名称＋説明＋条件＋状態**（獲得済み=✓＋獲得日／未獲得の数値系=進捗バー`cur/max`／シークレット未獲得=🔒`？？？`で内容を伏せる）。**報酬＝ティア連動コイン（決定・2026-07-19。当初「報酬なし」→ユーザー要望で転換）**: 実績獲得で難易度に応じコイン付与＝**ブロンズ◆20/シルバー◆50/ゴールド◆150**（初期値・シークレットもティア準拠・一度きりで乱獲不可＝希少性維持・装備は直接付与せずコインで統一）。カードに報酬額、サマリーに「実績で獲得したコイン合計」を表示。**初期カタログ〔投稿/採用/評価/投票/議論/継続/成長/魔法/装備/シークレット〕を確定（初期値・調整可）**。**README を更新**（FR-09・第6節「実績/バッジ」節新設・コイン付与ルール〔入手経路=評価連動＋実績ボーナスの2つ〕・装備入手文言・概要方針文・データモデル `Achievement`〔`coin_reward`〕/`UserAchievement`・第12節 `[~]実績`〔報酬決定〕`[~]ランキング`）。**SC-01 ショートカットタイルを実体化**（クエスト一覧→SC-10/ショップ→SC-30/アバター→SC-31/実績→SC-40/ランキング→SC-41。通知のみ `#` のまま=SC-02未作成）。残TBD（カタログ最終・シークレット内容・獲得演出/通知粒度・他ユーザー公開/代表バッジ・進捗集計方式）は画面設計md 第9節。
  - **SC-02 通知 ＝ 完了（2026-07-19）**（モック `mocks/SC-02_通知一覧.html` ＋画面設計 `screens/SC-02_通知一覧.md`）。**業務層の画面**（読ませる一覧＝クリーン不透明・ゲーム層ガラスは使わない）。**一覧標準（`.list-toolbar`＝状態〔すべて/未読のみ〕・種別〔メンション/コメント/評価/選定/更新/実績/魔法〕絞り込み＋未読件数＋「すべて既読にする」）＋日付グループ（今日/昨日/それ以前）の通知リスト**。**各通知＝種別アイコン＋本文＋文脈〔クエスト/アイデア〕＋時刻＋未読ドット＋既読/未読トグル**、**行クリックで参照先へ遷移**（メンション/魔法→SC-24／コメント/評価/選定/更新→SC-22／実績→SC-40）。獲得表示（選定=＋XP/コイン・実績=コイン）併記、更新通知は「投票の見直し」タグ（FR-34）。未読数は**ヘッダーのベルバッジと同期**。**種別は README データモデル `Notification` 準拠**（実績獲得・魔法リアクションを README に補記）。**全認証13ページのヘッダーベルを `<button>`→`<a href="SC-02_通知一覧.html">` に変換**（ベルが通知の主要導線に）＋SC-01「すべての通知→」「通知タイル」も SC-02 へ実体化。残TBD（通知のまとめ方/集約・保持期間/上限/自動既読・種別ON/OFF設定・外部通知〔メール等〕・リアルタイム反映・レベルアップを通知に含めるか）は画面設計md 第9節。
  - **トグルスイッチ `.switch` を新設（2026-07-19）**: 即時反映の ON/OFF 設定向け部品（ネイティブ checkbox 装飾・`.switch__track`/`__thumb`/`__text`/`__state`・focus-visible リング・disabled・`prefers-reduced-motion`）。shared.css に追加＋style-guide 4節に実演＋デザイン標準（フォーム部品「トグルスイッチ」節を追記・「今後追加予定」を解消）。**SC-91 会社設定で使用**（MFA必須/投票匿名化/管理者への開示）。
  - **SC-90 クエストグループ管理 ＝ 完了（2026-07-19）**（モック `mocks/SC-90_クエストグループ管理.html` ＋画面設計 `screens/SC-90_クエストグループ管理.md`）。**業務層**。**QG管理者が自分の管理グループ内アカウントを発行/編集/論理削除**。管理グループ切替（`select`）＋一覧標準（検索/状態フィルタ）＋テーブル（氏名/ID/グループ内ロール/状態/最終ログイン/操作）。操作＝**編集・パスワード再設定・無効化（論理削除）⇄再有効化**（無効化してもアイデア/投票/評価/コメントは残す）。発行/編集モーダル（氏名/ID/所属=管理グループ固定/ロール）。**アカウントは管理者発行のみ→初回ログインでパスワード設定**。所属割当（グループ間移動）は SC-91 の領分。要件は既定（FR-17）どおり＝README変更なし。
  - **SC-91 システム管理 ＝ 会社一覧に再設計＋SC-92 会社詳細に分割（2026-07-19・ユーザー要望）**（モック `SC-91_システム管理.html`/`SC-92_会社詳細.html` ＋画面設計 各md）。**業務層・システム管理者のみ**。**分割の意図＝「どの会社をメンテ中か」を明確化**（当初SC-91の1ページ縦積み〔会社一覧＋会社設定＋全アカウント〕は、どのテナントを設定中か分かりづらかった）。
    - **SC-91（会社一覧）**: 会社〔テナント〕一覧テーブル（頭文字タイル＝会社カラー/DB識別子/状態/アカウント数/グループ数/作成日）＋検索/状態フィルタ＋会社作成モーダル。**各行/「管理する→」で `SC-92_会社詳細.html?company=<id>` へ**。
    - **SC-92（会社詳細/設定）**: **上部に「メンテナンス中の会社」文脈バナー（`.ctx`・sticky・会社カラー左帯＋頭文字タイル＋会社名〔大〕＋状態＋DB識別子＋件数＋会社一覧へ戻る）＋パンくず**で対象会社を常時明示。**会社設定＝`.switch`×3**（投票匿名化 `vote_anonymized`／匿名時に管理者へ隠す `hide_voters_from_managers`〔匿名化OFF=記名時は自動無効〕／MFA必須 `mfa_required`）＋DB識別子/プロビジョニング注記。**その会社のアカウント & 所属フル操作**（検索/状態フィルタ＋テーブル＋発行/編集モーダル〔ロール・所属グループ`.multiselect`＝この会社の候補〕）。`?company` は URLSearchParams で特定・不正時は先頭にフォールバック。トグルは即データ反映＋依存無効化。**SC-01 のロール条件付き管理導線（SC-90→SC-91）を実体化**（SC-91→SC-92 は行導線）。
  - **ログインIDとメールアドレスを別項目に分離（2026-07-19・ユーザー要望）**: 従来「ログインID＝メール」と同一視していたのを、**Account に `login_id`（ログイン識別子・一意・非メール可）と `email`（OTP/初回パスワード設定リンク/将来の外部通知の宛先）を別カラムで保持**するよう変更。**SC-00 ログイン**＝ID欄を `type=text`・ラベル「ログインID」に（メール前提を撤去）。**SC-90/SC-92 管理**＝アカウント発行/編集モーダルを「ログインID」＋「メールアドレス」の2項目に・一覧テーブルに両列・検索も両対応。README（Account データモデル・FR-03/FR-03b〔OTPは登録メール宛〕・第8節セキュリティ残存リスク・第2節一元管理項目）／SC-00・SC-90・SC-92 doc に反映。**残存リスク更新**: ログインIDとメールは分離したが OTP 配送がメールのため登録メール侵害は突破の一要因（将来 TOTP 検討）。
  - **会社にアバター（アイコン）＋カラーを追加（2026-07-19・ユーザー要望）**: クエスト（SC-11）と同方式で**会社にも会社カラー（`.swatches`）＋会社アバター/アイコン（アップロード・未設定は「頭文字＋会社カラー」フォールバック＝`.quest-icon` を流用）**を設定可能に。**SC-91**＝一覧の会社名に会社アイコン表示＋会社作成モーダルにアイコン/カラー欄。**SC-92**＝文脈バナーのタイルを `.quest-icon` 化（画像/頭文字・会社カラー左帯）＋「会社プロフィール」カード（アイコン/カラー編集→バナー・アクセントに即反映）。README（**Company データモデルに `color`/`icon_image_path` 追加**・FR-16 に会社カラー/アバター設定を追記）／SC-91・SC-92 doc に反映。実体は MinIO（クエストアイコンと同様）。
  - **SC-92 の所属クエストグループを `.multiselect`（候補のみ・自由入力なし）に修正（2026-07-19）**: 誤った `<select multiple>` マークアップを正しい `.multiselect`（`__control`/`__input`/`__list`＋`.multiselect__option`）へ。候補は shared.js の DOMContentLoaded 初期化より前に同期生成。
  - **style-guide のレイアウト崩れを修正（2026-07-19）**: カラートークン見本のローカル `.swatch`/`.swatches` が、後から shared.css に追加した色選択部品 `.swatch`（28px 円形・`border-radius:50%`）と**名前衝突**して崩れていた（トークン見本の重なり／スウォッチの楕円化）。style-guide の見本クラスを **`.tokens`/`.token`** に改名し衝突解消（4節の色選択デモは shared の `.swatch` をそのまま使用）。
  - **SC-92 文脈バナーの固定位置を修正（2026-07-19）**: バナー `.ctx` が `top:0; z-index:30` で共通ヘッダー（sticky・`--header-h`=56px・z-index:10）と最上部で重なり、ヘッダー内アバターメニューがバナーに被って押せなかった。**`top: var(--header-h)`（ヘッダー直下で固定）＋ z-index を 5（ヘッダーより下）** に変更。SC-92 doc にも反映。
  - **ヘッダーのベル（`<a>`化）のホバー下線を除去（2026-07-19）**: SC-02 対応でベルを `button`→`a` にした際、`a:hover{text-decoration:underline}`（詳細度0,1,1）が効いて 🔔 の下に黒い下線が出ていた。base の `.bell{text-decoration:none}`（0,1,0）では消せないため **`.bell:hover` に `text-decoration:none`（0,2,0）** を追加して打ち消し（shared.css・全画面共通）。
  - **▶ 画面設計フェーズ＝完了（初期スコープ全画面 SC-00〜SC-92）。次フェーズの候補**: ①**データモデル詳細**（`doc/データモデル.md` 等に ER 図・テーブル定義を起こす）→ ②**API設計**（エンドポイント一覧・リクエスト/レスポンス）→ ③**実装フェーズ**（Next.js フロント＋FastAPI＋PostgreSQL＋MinIO の compose/スキャフォールド作成）。**並行して残要件TBD（下記＆各画面設計md 第9/10節）をユーザーと確認**。技術スタックは確定済み。**注: 全画面の相互リンクは実体化済み**（評価=SC-25／SP=SC-32／ベル=SC-02／ショップ⇄きせかえ=SC-30⇄SC-31／ランキング=SC-41／管理=SC-90/91／ダッシュボードのタイル・管理導線）。
  - **▶ 未確定でユーザー確認待ち（次回冒頭で確認推奨）**: ①**全文検索エンジン**＝PGroonga 推奨で記載済み（代替 pg_bigm／将来 Meilisearch・OpenSearch）だが最終確定していない。②全文検索で**添付ファイル名も対象に含めるか**。③各画面の残TBD（下記＆各画面設計md 最終節）。
  - **モックの作り方メモ（重要）**: 新規モックは必ず `shared.css`＋`shared.js` を読み込む。共通コンポーネント（shared.css 昇格済み）＝`.btn`系/`.card`(+`.card-accent`)/`.badge`/`.avatar`/`.quest-icon`/`.poster`/`.table`(+`.table-wrap`)/`.list-toolbar`(+`.list-count`/`.list-empty`)/`.modal`系/`.rank-panel`/`.app-bg`/`.checkbox`/`.combobox`/**`.multiselect`**（複数選択・`[data-multiselect]`／`data-free`自由入力・`data-single`単一選択）/**`.swatches`/`.swatch`**（カラーパレット選択）/**`.switch`**（トグルスイッチ・ON/OFF設定向け）/`.pixel-*`(CRTガラス)/**`.spell-fx--*`**（魔法エフェクト: fire/ice/thunder/sparkle/rainbow/aura）/`.reaction`系/`.mascot` 等。**共通ヘッダー**（`.app-header`＋`.header-actions`〔Lv/コイン/SP＋ベル＋`.usermenu`〕）は全認証画面で同一＝shared.js が `.usermenu` を自動初期化。**背景画像**: 設定（変更/リセット）は**ユーザーメニュー内 `[data-bg-set]`/`[data-bg-reset]`**＝shared.js が集約処理（全画面共通）。各ページは読み込み時に `localStorage('ideaquest_content_bg')` を `#appBg` へ**復元するスクリプトのみ**を持つ（例: 各モック冒頭の savedBg 復元）。**登録・編集・詳細はモーダル標準**（本番=Next.js Parallel/Intercept Routes、パネル→モーダル拡大は framer-motion `layoutId`。モックは遷移 or vanilla JS の FLIP＝SC-01 `flipOpen/flipClose` を参考。ドラッグ移動モーダルは SC-25 参照）。
  - **デザイン標準で今後決める小items**: （済）トグルスイッチ `.switch`／ダークモード要否／ドット絵スプライト素材規格／アイコンセット／コントラスト最終チェック。
- **並行して残る要件TBD（優先順）**:
  1. **XP日次上限の具体値**・コイン付与の確定タイミング（評価締切一括 or 都度再計算）・レベル上限の要否。
  2. 添付ファイルの**上限サイズ・許可形式の具体値**。
  3. テナント数上限・リソース設計、管理DB↔会社DBの同期・配置。
- **その後**: 全画面のモック＋画面設計が固まったら、データモデルの詳細（`doc/データモデル.md` 等）と API 設計を詰め、**実装フェーズ**（Next.js フロント＋FastAPI バックエンド＋PostgreSQL＋MinIO の compose/スキャフォールド作成）へ。技術スタックは確定済み。

## 6. データモデル案（現時点、詳細は README 第9節）

**2層のDBに分かれる**。
- 管理DB（システム共通）: Company（会社/テナント）, Account（ログインID/認証/所属Company/システムロール）, AccountQuestGroup（アカウント×クエストグループ所属）, 運用監査ログ。
- 会社DB（会社ごと物理分離）: User（＋`background_image_path`＝コンテンツ背景画像・任意）/ QuestGroup（クエストグループ）/ QuestGroupMember（User×QuestGroup 多対多）/ Quest（quest_group_id・owner_id・status〔下書き含む〕・`icon_image_path`〔アイコン・任意〕保持）/ QuestMember（パーティー、候補は同一グループ所属者）/ QuestMemberPermission（権限）/ Idea（status〔下書き/公開〕）/ Attachment（パス＋元名・MinIO実体）/ Vote / **Follow（User×Idea アイデアフォロー＝ウォッチ）** / ChatGroup / ChatMessage（添付・メンション）/ Notification（アプリ内通知・フォロー中アイデアの動きも通知）/ Evaluation / EvaluationScore / Item(Equipment) / UserItem / Transaction(Activity)（`quest_id`＝クエスト内ランキング集計用）。
- 添付・各種アップロード画像（背景/クエストアイコン）の実体は MinIO（S3互換オブジェクトストレージ・Dockerコンテナ）に保管。
- **全文検索（FR-31）**: 会社DBの Idea（件名/本文/価値/備考）＋ChatMessage（本文）に日本語全文索引。エンジンは **PGroonga 推奨（要確認）** ／代替 pg_bigm。
- プロビジョニング（確定）: MVPは **docker compose 単一ファイルを手編集**して会社DBを追記（運用コスト最小）。**DB接続情報は .env で管理**。将来は自動化/k8s＋Operatorを検討。
- 未決: テナント数上限・リソース設計、管理DB↔会社DBのUser/クエストグループ所属の同期・配置。
