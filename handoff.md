# 作業引き継ぎ (handoff)

> 次回セッション開始時に **このファイルだけ読めば作業を再開できる** ことを目的とした引き継ぎメモ。
> 読者は「このセッションの記憶が無い次回の自分」。会話ログは参照不可。詳細仕様は必ず `doc/要件定義/README.md`（唯一の要件定義書）と `doc/API設計/` を参照。
> 毎回このファイルは全文を上書きする（履歴は git に任せる）。

---

## 1. 最終更新日時 / ブランチ / 最新コミット

- 最終更新: **2026-08-06 23:50 JST**
- ブランチ: **main**（作業ツリー **クリーン**・`origin/main` と同期済み＝確認済み）
- 最新コミット: **`e7af561`**（handoff: 通しレビュー②完了を記録）
- remote: `https://github.com/t-umekawa-sc/sc-ideaquest-G2.git`
- 直近の主なコミット（新しい順）:
  - `e7af561` handoff: 通しレビュー②完了を記録
  - `05f7eac` 通しレビュー②反映 C-1〜C-5（軽微ポリッシュ）
  - `a49421e` 通しレビュー②反映 B-1/B-2（完了凍結を C.5 に単一正化・§1.7 を代表(非網羅)明記）
  - `f39dbf9` 通しレビュー②反映 A-1〜A-3（門番文言統一・PW再設定参照修正・README §2-D 同期）
  - `59c70ac` D.2 並行編集の並行制御（悲観ロック不使用・後着 409 edit_conflict＝方針A）
  - `d5decd5` API §1.9 Idempotency-Key の保存先/TTL/in-flight/クライアント発行を明記
  - `bc781dc` 署名URL/短TTLの理由を §1.10 に単一正集約＋規約 §2.2⑧ を1語規範化

---

## 2. このプロジェクトのゴール

- **ideaquest**＝社内のアイデア創出をゲーミフィケーション（XP/コイン/レベル/魔法/ランキング）で促す **WEB アプリ**。これから作る。
- **マルチテナント SaaS**（管理DB 1＋会社DB N の DB 分離）。フロント Next.js／バック FastAPI／PostgreSQL／Redis／MinIO／Docker。
- 現在は **実装前の設計フェーズ**。順序＝要件定義 →データモデル →画面設計 →**API設計（いまここ）**→ 実装。

---

## 3. 今回やったこと — 変更ファイルと理由

今回のセッション＝**(前半) ユーザーの D 自己チェック指摘 D-6〜D-8 の反映**＋**(後半) 私(Claude)による A〜D＋README の通しレビュー② を実施・全反映**。変更したのは **ドキュメントのみ**（コードは無い）。

### 前半：D 指摘の反映
- **`doc/API設計/README.md`＋`doc/コーディング規約.md`**（`bc781dc`）: 署名付きDL URL の「短TTL＝直リンク流出耐性」の**理由**を **README §1.10 に単一正として集約**（非公開バケット＋発行時認可＋短TTL・恒久公開URL禁止）。規約 §2.2⑧ は「認可DL＝短TTL署名URL・恒久公開URL禁止」の1語規範化に留めポインタ維持。理由＝説明を規約に複製すると正が2箇所化し drift するため。
- **`doc/API設計/README.md`**（`d5decd5`）: **Idempotency-Key** の仕様を §1.9 に明記＝保存先 Redis `idem:{company_id}:{account_id}:{key}`／TTL 既定24h／in-flight（`SET NX` で確保・後続は 409 `idempotency_in_progress`・完了で再生 `idempotency_replayed`）／別内容は 422 `idempotency_key_reuse`／クライアント発行（1操作1個 UUIDv4・リトライは使い回し）。§1.14/§1.7 も更新。理由＝未記載だった運用を確定。
- **`doc/API設計/D_アイデア・添付・版・投票・フォロー.md`**（`59c70ac`）: アイデア編集/削除の**並行制御**を D.2 に明記＝**悲観ロック/専用version列は使わず**、既存 `UNIQUE(idea_id, revision)` を楽観ロックとして後着を **409 `edit_conflict`**（方針A）。理由＝過剰実装(悲観ロック)の防止と設計意図の明文化。

### 後半：通しレビュー②（A〜D＋README の横断チェック→反映）
Explore エージェント3体（A/B・C/D・README 分担）で洗い出し、誤検出を除外して反映。
- **A-1**（`f39dbf9`・`D_….md`）: D.0 の門番を C.0 と統一＝`quest_members`「行が無い」→「**有効な行（`removed_at IS NULL`）が無い**」。トゥームストーン化(既存 `4053ad4`)未反映の片手落ち解消。
- **A-2**（`f39dbf9`・`A_認証・セッション.md`）: A.7 の管理者PW再設定参照が SoD 改定で陳腐化＝「B.2/B.4」→「**B.2〔system_admin〕/ B.2.1〔会社アカウント管理者〕**」。B.4(QG管理者)は参加選択専任で PW 再設定を持たない旨を明記(2箇所)。
- **A-3**（`f39dbf9`・`README.md`）: **README §2-D サマリを現行 D に同期**（下書き一覧表示・publish アトミック・楽観制御 edit_conflict・完了凍結でフォローは解除のみ・PATCH は内容編集専用）。
- **B-1**（`a49421e`・`C_….md`＋`D_….md`）: **completed 書き込み凍結の canonical を C.5 に確定**（添付を追加＋「例外＝フォローは解除のみ可・新規フォローは409」を C.5 に。D.0 は「全体像は C.5・チャット/評価は E/F・フォロー例外は D.6」ポインタ化）。分散していた凍結記述を一本化。
- **B-2(a)**（`a49421e`・`README.md`）: §1.7 を「**横断的な代表（非網羅）**」と明記。`invalid_state`/`edit_conflict` 等の conflict サブコード・認証系コードは各ドメイン §、**網羅は OpenAPI(SoT)**。README に第2の網羅表は作らない(drift回避)。
- **C-1〜C-5**（`05f7eac`・主に `D_….md`）: 用語「クエスト内権限」→「6権限」統一／D の一覧はカーソル既定明示／添付 `uploaded_by` はユーザーオブジェクトで返す明示／投票日次上限の文言統一（XP付与を伴う初回投票が5回/日・切替/取消/再投票は対象外）／**初版 `revision=1` を「公開処理時に記録」の決定として D.4 に昇格**・D.8 の TBD 除去。

---

## 4. 現在の状態 — 動いているもの / 壊れているもの / テスト

- **リポジトリは `doc/` と `handoff.md` のみ**。アプリのコード・`compose`・テストは**未着手＝存在しない**（`find` でコード/compose 0件を確認済み）。
- **動いているもの**: 静的な設計ドキュメント（Markdown）と**画面モック HTML**のみ。モックの inline JS は `node --check` で構文検証する運用（今回編集した SC-12/SC-01/SC-22 で OK 確認済み）。
- **壊れているもの**: なし（作業ツリー クリーン）。
- **テスト**: 自動テストは無い（コード未着手のため）。ドキュメント整合は今回の**通しレビュー②で人手＋Explore エージェント**により確認（機械的テストではない）。
- **API設計の進捗**: **A/B/C/D ＝詳細確定**（`doc/API設計/` に個別ファイル）。**E〜L ＝未着手**（`README.md` §2 一覧に ⬜ で目次のみ・`180-187` 行）。
- 成果物の所在:
  - `doc/要件定義/README.md`（唯一の要件定義書）
  - `doc/データモデル.md`（管理DB6＋会社DB28テーブル・`system_role` 3値〔system_admin/company_account_admin/general〕）＋`.pdf`（派生・追跡外）
  - `doc/API設計/`（`README.md`＝全体規約＋§2目次／`A_認証・セッション.md`／`B_会社・アカウント・所属.md`／`C_クエスト・パーティー・権限.md`／`D_アイデア・添付・版・投票・フォロー.md`）
  - `doc/コーディング規約.md`（§2 セキュリティ/§3 FastAPI4層・ディレクトリ/§4 フロント構成）
  - `doc/画面設計/`（`screens/` md 19本・`mocks/` html 21本〔一覧 `mocks/index.html` 存在確認済み〕・`デザイン標準.md`・`画面遷移図.md`）
  - `doc/WEBアプリ開発時のセキュリティ対策一覧.md`（OWASP系・規約 §2.2 で義務化）

---

## 5. 詰まっている点 — 失敗したアプローチと理由

- **現時点でブロッカーは無い**。
- **通しレビュー②で Explore が挙げたが「誤検出＝対応不要」と判断した3件**（再提起されても再検討不要）:
  1. 「下書き決定日 8/2 vs 8/6 の食い違い」→ **別の決定**（C＝下書きクエストを一覧表示＝8/2／D＝下書きアイデアを一覧表示＝8/6）で正しい。
  2. 「OTP が Redis か DB か曖昧」→ データモデル **§8-⑨ で Redis 確定済**（曖昧でない）。
  3. 「冪等キーのクロステナント遮断が未担保」→ `company_id` セッション由来＋キースコープで**担保済**。
- **過去に採用→撤回した経緯**（記録）: D の `publish` を当初「ボディなし・`PATCH`→`publish` の2ステップ・非原子は良性」としたが、**部分コミットの穴**（PATCH コミット後の publish 失敗を巻き戻せない）のため撤回し、`content?` を受ける**アトミック単一UoW**に変更（C の publish と対称）。

---

## 6. 決定事項と根拠（採用しなかった案も）

今回セッションで確定した主な設計判断:
- **下書きアイデアを一覧に含める（本人のみ・クリックで SC-21 編集）**。不採用＝ダッシュボード集約のみ（「一覧から作成→戻ると消える」不便）。C の下書きクエスト表示と対称。
- **完了クエストのフォローは「解除のみ」可・新規フォローは 409**。不採用＝双方向切替可（完了後は通知対象イベントが発生せず新規購読は無意味）。UI で「⏸ 完了（凍結）」バッジを出す（SC-22/SC-01 反映済）。
- **`publish` はアトミック**（`content?`・単一UoW・失敗は全ロールバック）。不採用＝2ステップ非原子（部分コミットの穴）。
- **並行編集＝悲観ロック/専用version列を使わず既存 `UNIQUE(idea_id,revision)` で楽観制御・後着 409 `edit_conflict`（方針A）**。不採用＝(i)悲観ロック＝編集はモーダル表示〜保存の「ユーザー思考時間」をまたぐためロック保持はアンチパターン、(ii)自動再試行の last-writer-wins＝silent lost update を招く。版スナップショットが安全網。
- **署名URL/短TTLの理由は §1.10 に単一正集約**（規約はポインタ）。不採用＝コーディング規約に全文複製（drift）。
- **完了凍結の全体像は C.5 に単一正**、各ドメインは自EPの409を再掲。**§1.7 エラーコードは代表(非網羅)＋OpenAPI(SoT)**。不採用＝README にドメイン別網羅表（保守コスト・drift）。
- **初版 `revision=1` を公開処理時に記録**（差分の起点・`voted_revision` 判定の基準を常に存在させるため）。
- ドキュメント運用の原則＝**「設計の正は1箇所・他は参照」**（規約 §2「なぜも併記(必須)」／メモリ [[document-design-rationale]] にも整合）。

（過去の主要決定＝データモデル §8 の TBD①〜⑫決着、SoD＝会社アカウント管理者新設・QG管理者を参加選択専任に縮小〔§8-⑯・system_role 3値〕、認証は Cookie＋Redis 不透明セッション〔JWT 不採用・A.10〕、リアルタイムは WebSocket＋Redis Pub/Sub〔§1.12〕、i18n JA/EN。詳細は各ドキュメント。）

---

## 7. 次にやること — 優先順・具体的

### ★最優先＝③ドメインE（チャット・リアクション・魔法発動）の分割レビュー・詳細確定
段取り「①C/D自己チェック（済）→②通しレビュー（済）→③ドメインE」の③。**ユーザーの GO を待って着手**。進め方＝論点整理→決定案提示→ユーザー承認→反映→**2段コミット（本体→handoff にハッシュ追記）**。

1. **読む（インプット）**:
   - `doc/画面設計/screens/SC-24_アイデアチャット.md` ＋ `doc/画面設計/mocks/SC-24_アイデアチャット.html`
   - `doc/API設計/README.md` の **§1.12（WebSocket 配信）** と **§2-E サマリ（201行付近）**、ドメイン一覧表 E 行（180行）
   - `doc/データモデル.md` の **§5.15 chat_groups・§5.16 chat_messages・§5.17 chat_mentions・§5.18 reactions**（絵文字マスタ `reaction_emojis` の節番号は要確認）。論理削除は `chat_messages.is_deleted`（§で確認済み）
   - `doc/コーディング規約.md` §3.1/§3.4（FastAPI 4層・`tenant/chat` 縦スライス）
2. **成果物**: `doc/API設計/E_チャット・リアクション・魔法発動.md` を**新規作成**（A〜D と同じ「メソッド/パス｜概要｜リクエスト｜レスポンス」4列＋直下注記の形式）。作成後 `README.md` §2 一覧表の **E 行を ✅・リンク化**、§2-E 目次サマリを記述。
3. **詰める論点（当たり）**:
   - チャット取得 `GET /ideas/{id}/chat`（`chat_groups` は idea と 1:1）／投稿 `POST /chat-messages`／本人編集 `PATCH /chat-messages/{id}`・**論理削除** `DELETE`（`is_deleted`）／メンション `chat_mentions`／チャット添付（D.3 と同じ multipart 別API・`attachments.chat_message_id`）
   - 通常リアクション `POST/DELETE /chat-messages/{id}/reactions`（`reaction_emojis` マスタ・複数可・**同一ユーザー×同一絵文字は不可**＝ユニーク制約）
   - 魔法リアクション（**1メッセージ1魔法・各魔法1チャット1回**＝FR-33・`reactions` ユニーク制約・**SP消費/魔法系統前提の計算は G へ委譲**）
   - **D が委譲した2件を E で確定**＝`GET /ideas/{id}/chat-activity`（議論アクティビティ・グラフ＝直近14日の日次メッセージ数＋更新マーカー）と `chat_preview`（`GET /ideas/{id}` が返す直近数件）
   - 書込側 application が **Redis へ event 発行**（`notifications:{user_id}`・`chat:{chat_group_id}`＝ドメインL連携点・README §1.12・WS は配信専用/書込は REST）
   - **完了(completed)凍結**時のチャット投稿/編集/削除/リアクションは 409（**canonical は C.5**・E で再掲）

### その後（順次）
- **ドメインF**（評価／SC-25・SC-22）→ **G**（ゲーミフィケーション：ショップ/装備/魔法/実績/ランキング/XP・コイン・SP／SC-30/31/32/40/41）→ **H**（通知／SC-02）→ **I**（ダッシュボード集約／SC-01）→ **J**（全文検索／SC-12・PGroonga）→ **K**（プロフィール・背景画像）→ **L**（リアルタイム配信 WebSocket／SC-24・SC-02）。
- **全ドメイン確定後＝実装スキャフォールド**（Next.js フロント＋FastAPI＋PostgreSQL＋Redis＋MinIO の `compose`）。ディレクトリ構成は規約 §3.4（バック）・§4.1（フロント）で確定済み。

### 未処理の小キュー（軽微・実装時 or 該当ドメインで整理）
- **SC-22 §7 の想定 API が旧記法**（`PUT /vote` nullable・`PUT /follow {on}`）で確定形（`POST`＋`DELETE`）と表記差＝**未反映**（軽微・E以降 or 実装時に整理）。※SC-21 §7 は今回 D 準拠に更新済み。
- 各画面 §9 の実装寄り TBD（軽微編集の版/通知抑制・締切後可否の細部・添付の会社別上限の具体値 等）＝実装時確定でも可。

---

## 8. 再開に必要な環境情報

- **アプリの起動/テストコマンドは無い**（コード未着手＝`compose`/`package.json`/`pyproject.toml` いずれも未作成・確認済み）。ポート・環境変数も**未定（未確認）**。
- **今使う操作**:
  - `git`（履歴・差分・コミット/プッシュ）。ブランチ `main`・remote `origin`。
  - ドキュメント＝Markdown を読む。画面モック＝`doc/画面設計/mocks/*.html` をブラウザで開く（入口 `doc/画面設計/mocks/index.html`）。
  - モック JS の構文チェック＝inline `<script>` を抽出して `node --check`（今セッションで使用した手法）。
- **技術スタック（確定・未構築）**: フロント Next.js(React/App Router)＋Three.js/R3F＋three-vrm ／ バック Python/FastAPI(4層＝router→application→domain→repository) ／ DB PostgreSQL(管理DB1＋会社DB N) ／ Redis(セッション/OTP/pre-auth/会社コンフィグキャッシュ/冪等キー/Pub-Sub) ／ MinIO(添付・画像) ／ 全て Docker。
- **リポジトリ運用ルール**:
  - `.gitignore` で **`*.pdf` は追跡外**（PDF は Markdown からの派生・Markdown が正）。
  - **コミットは「本体コミット→handoff にハッシュ追記」の2段方式**。コミットメッセージ末尾に `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`。プッシュ先 `origin/main`。
  - ドキュメント方針＝**設計の正は1箇所・他は参照**（drift 回避）／**設計判断はなぜも併記（必須・規約 §2）**。

---

### 自己チェック（このファイルだけで再開できるか）
- ✅ 現在地（②通しレビュー完了・③ドメインE が次）と、E 着手に必要な入力ファイル・作成物・論点をファイル/節レベルで記載。
- ✅ 確定した設計判断と不採用案の理由を記載。
- ✅ コードは無いことと、その事実確認（find 0件）を明記。テストは存在しないと明記。
- ✅ 未確認事項（`reaction_emojis` の節番号・compose のポート/環境変数）は「要確認/未確認」と明記。
- ⚠ 会話ログは参照不可のため、A/B/C/D の**詳細な決定理由は各 `doc/API設計/*.md` を正**とすること（本 handoff は要約）。
