# 作業引き継ぎ (handoff)

> 次回セッション開始時に **このファイルだけ読めば作業を再開できる** ことを目的とした引き継ぎメモ。
> 読者は「このセッションの記憶が無い次回の自分」。会話ログは参照不可。詳細仕様は必ず `doc/要件定義/README.md`（唯一の要件定義書）と `doc/API設計/` を参照。
> 毎回このファイルは全文を上書きする（履歴は git に任せる）。

---

## 1. 最終更新日時 / ブランチ / 最新コミット

- 最終更新: **2026-08-07 JST**
- ブランチ: **main**
- 最新コミット（本文）: **`3b6138d`**（API設計 C/D/E 門番の「グループ非所属も404」文を明快化）。※本 handoff 更新コミットはこの直後（2段方式＝本体→handoff にハッシュ追記のため、本 handoff は自分の未来ハッシュを持てず 1 つ前を指す）。
- remote: `https://github.com/t-umekawa-sc/sc-ideaquest-G2.git`
- **プッシュ状況**: `529caf0`（ドメインE の handoff 追記）まで origin 反映済み。**`3b6138d`（門番文の明快化）＋本 handoff 更新は未プッシュ**（次回 or ユーザー指示でプッシュ）。
- 直近の主なコミット（新しい順）:
  - `3b6138d` API設計 C/D/E: 門番の「グループ非所属も404」文を明快化（意味不変・可読性のみ）
  - `529caf0` handoff: ドメインE 詳細確定を記録
  - `d735017` API設計 ドメインE 詳細確定（チャット・リアクション・魔法発動／SC-24）
  - `22c9f47` 規約: ドキュメント作成規約を導入＋コーディング規約を `doc/規約/` へ集約
  - `148bee3` handoff 全文更新（前回セッション末）

---

## 2. このプロジェクトのゴール

- **ideaquest**＝社内のアイデア創出をゲーミフィケーション（XP/コイン/レベル/魔法/ランキング）で促す **WEB アプリ**。これから作る。
- **マルチテナント SaaS**（管理DB 1＋会社DB N の DB 分離）。フロント Next.js／バック FastAPI／PostgreSQL／Redis／MinIO／Docker。
- 現在は **実装前の設計フェーズ**。順序＝要件定義 →データモデル →画面設計 →**API設計（いまここ）**→ 実装。

---

## 3. 今回やったこと — 変更ファイルと理由

今回のセッション＝**(A) ドキュメント作成規約の導入＋規約類の集約**＋**(B) API設計 ドメインE の分割レビュー・詳細確定**。変更したのは **ドキュメントのみ**（コードは無い）。

### (A) 規約の反映（`22c9f47`・プッシュ済）
- **`doc/規約/ドキュメント作成規約.md`**（他プロジェクトからコピー・汎用）を追加。文書間参照の表記ルール（裸の `§x` 禁止＝文書名接頭辞／同名系は版タグ／区切りは忠実引用／相対リンク）。
- **`CLAUDE.md`（リポジトリ直下・新規）**を作成し同規約を毎セッション自動参照（規約の §自動適用が要求）。版タグ対応表の受け皿も設置（現状＝同名取り違えの恐れなし＝未設置）。
- **`doc/コーディング規約.md` → `doc/規約/コーディング規約.md`** へ `git mv`（規約類を `doc/規約/` に集約）。explicit パス参照を更新（`doc/API設計/README.md`・`doc/要件定義/README.md`・`doc/WEBアプリ…対策一覧.md`・本 handoff）。※過去コミット記述（旧 §3 の `bc781dc` 等）は当時の実パスのため据え置き。
- **適用方針＝折衷**（新規は準拠／既存は触れる機会に順次正規化／網羅一括は設計確定後の最終パス）。→ **A〜E の API設計は house style（README/データモデルの §x を文書名スコープで参照）のままで統一**しており、規約の裸 §禁止の網羅適用は「最終パス」で A〜E 一括実施予定（E だけ先行して体裁を変えない）。

### (B) ドメインE 詳細確定（`d735017`・未プッシュ）
- **`doc/API設計/E_チャット・リアクション・魔法発動.md`（新規）**を A〜D と同じ形式（4列表＋直下注記・なぜ併記）で作成。README §2 一覧の **E 行を ✅+リンク化**・**§2-E サマリを詳細化**・§3 次アクションを **F** に更新。
- **ユーザー選択で確定した3フォーク**:
  1. **未読位置＝`chat_reads` テーブル新設**（データモデル §5.31・§8-⑰・会社DB 28→29）。`notifications.is_read` はベル用でチャット既読を表せず SC-24 §9 が TBD だったため。
  2. **チャット添付＝単一 multipart `POST /chat-messages`**（本文/メンション/添付を単一 UoW）。D の「アイデア添付＝独立サブリソース `POST /ideas/{id}/attachments`」と形が異なる**理由を E.3 に明記**＝**添付のAPI形はエンティティのライフサイクルに従う**（アイデアは長命コンテナで独立増減・メッセージは事前の空コンテナが無い追記単位で添付のみ本文もあり得る＝同時生成が必要）。同一原則の別解で不整合ではない。
  3. **魔法リアクション＝通常と同じ `POST/DELETE /chat-messages/{id}/reactions` に `type` 判別で統合**（`reactions` テーブルが type 一元化と対称）。
- **データモデル**: §5.1 ER に `chat_reads` 追加・§5.31 新設・§8-⑰ 追記。

---

## 4. 現在の状態 — 動いているもの / 壊れているもの / テスト

- **リポジトリは `doc/`・`CLAUDE.md`・`handoff.md` のみ**。アプリのコード・`compose`・テストは**未着手＝存在しない**（`doc/画面設計/mocks/shared.js` はモック用資産）。
- **壊れているもの**: なし（本文コミット時点で作業ツリー クリーン）。
- **テスト**: 自動テストは無い（コード未着手）。ドキュメント整合は人手＋Explore で確認（機械的テストではない）。
- **API設計の進捗**: **A/B/C/D/E ＝詳細確定**（`doc/API設計/` に個別ファイル）。**F〜L ＝未着手**（README §2 一覧に ⬜・目次のみ）。
- 成果物の所在:
  - `CLAUDE.md`（リポジトリ直下・規約自動参照の入口）
  - `doc/要件定義/README.md`（唯一の要件定義書）
  - `doc/データモデル.md`（管理DB6＋**会社DB29テーブル**〔E で `chat_reads` 追加〕・`system_role` 3値）＋`.pdf`（派生・追跡外）
  - `doc/API設計/`（`README.md`＝全体規約＋§2目次／`A_認証・セッション.md`／`B_会社・アカウント・所属.md`／`C_クエスト・パーティー・権限.md`／`D_アイデア・添付・版・投票・フォロー.md`／**`E_チャット・リアクション・魔法発動.md`**）
  - `doc/規約/`（**`ドキュメント作成規約.md`**〔汎用・文書間参照ルール〕／**`コーディング規約.md`**〔§2 セキュリティ/§3 FastAPI4層・ディレクトリ/§4 フロント構成〕）
  - `doc/画面設計/`（`screens/` md・`mocks/` html〔入口 `mocks/index.html`〕・`デザイン標準.md`・`画面遷移図.md`）
  - `doc/WEBアプリ開発時のセキュリティ対策一覧.md`（OWASP系・規約 §2.2 で義務化）

---

## 5. 詰まっている点 — 失敗したアプローチと理由

- **現時点でブロッカーは無い**。
- ドメインE で **Explore が挙げ得た論点のうち確定済で再検討不要**: 未読テーブル不在（→ `chat_reads` 新設で解消）・チャット添付の形（→ 単一 multipart で確定・なぜ E.3）・魔法EP（→ 統合で確定）。
- **過去に採用→撤回**（記録）: D の `publish` を当初 2ステップ非原子としたが部分コミットの穴のため撤回し `content?` を受けるアトミック単一UoW に変更。

---

## 6. 決定事項と根拠（採用しなかった案も）

今回セッションで確定した主な設計判断（ドメインE）:
- **未読＝`chat_reads` 新設**（`UNIQUE(chat_group_id,user_id)`・`last_read_message_id` アンカー・完了後も既読更新可＝読み取り副作用で凍結対象外）。不採用＝(a) メッセージ単位既読テーブル（行数爆発）、(b) Redis のみ（永続要件に反する）。
- **チャット添付＝単一 multipart POST**（アトミック・orphan なし）。不採用＝ステージング先行アップロード（孤児行 GC が必要・過剰）。**D と形が違うのは正当**（E.3 なぜ）。
- **リアクションは通常/魔法を統合EP＋`type` 判別**。魔法は 1メッセージ1魔法・各魔法1チャット1回・早い者勝ち（§5.18 部分ユニーク）・解放判定は `user_spells`（SP消費/系統前提は G）。装飾のみ（XP/評価/投票に非影響）。
- **投稿 XP+5＝各ユーザー日次初回のみ**（上限=チャット10/日）・記帳は G の repo を同一UoW。**完了凍結**は投稿/編集/削除/リアクション（canonical C.5・既読更新は例外で可）。
- **通知発火（mention/idea_comment/follow_comment/magic_reaction）は E が契機提供・配信は H**。**WS event（`chat:{chat_group_id}`／`notifications:{user_id}`）発行は E の application・配信は L**（§1.12・書込は REST）。
- 規約: **設計の正は1箇所・他は参照**／**設計判断はなぜも併記（必須）**（メモリ [[document-design-rationale]]）。ドキュメント作成規約の適用は**折衷**。

（過去の主要決定＝データモデル §8 の TBD 決着、SoD〔会社アカウント管理者新設・§8-⑯〕、認証は Cookie＋Redis 不透明セッション、リアルタイムは WebSocket＋Redis Pub/Sub〔§1.12〕、i18n JA/EN。詳細は各ドキュメント。）

---

## 7. 次にやること — 優先順・具体的

### ★最優先＝④ドメインF（評価）の分割レビュー・詳細確定
段取り「①C/D自己チェック（済）→②通しレビュー（済）→③ドメインE（済・`d735017`）→④ドメインF」。**ユーザーの GO を待って着手**。進め方＝論点整理→決定案提示→ユーザー承認→反映→**2段コミット（本体→handoff にハッシュ追記）**。

1. **読む（インプット）**:
   - `doc/画面設計/screens/SC-25_評価画面.md`＋`SC-22_アイデア詳細.md`（§4.6 右レール＝評価結果）＋各 `mocks/*.html`
   - `doc/API設計/README.md` の **§2-F サマリ**・ドメイン一覧表 F 行、および **§8-⑥（コインは評価締切で一括確定）**
   - `doc/データモデル.md` の **§5.21 evaluations・§5.22 evaluation_scores**（`visibility`・5観点＋観点別コメント＋総評）
   - `doc/規約/コーディング規約.md` §3.1/§3.4
2. **成果物**: `doc/API設計/F_評価.md` を新規作成→ README §2 の F 行 ✅+リンク化・§2-F サマリ記述。
3. **詰める論点（当たり）**: `PUT /ideas/{id}/evaluation`（下書き `draft`/確定 `submitted`）・確定時のみ総評必須・`visibility` 公開範囲・コイン確定は締切一括（§8-⑥）・完了凍結（canonical C.5）・フォロワーへ `follow_evaluation` 通知（H）。

### その後（順次）
- **G**（ゲーミフィケーション）→ **H**（通知）→ **I**（ダッシュボード集約）→ **J**（全文検索・PGroonga）→ **K**（プロフィール・背景画像）→ **L**（リアルタイム配信 WebSocket）。
- **全ドメイン確定後＝実装スキャフォールド**（Next.js＋FastAPI＋PostgreSQL＋Redis＋MinIO の `compose`）。
- **ドキュメント作成規約の網羅適用（最終パス）**＝設計確定後に A〜E ほかの裸 §x を文書名接頭辞へ一括正規化。

### 未処理の小キュー（軽微・実装 or 該当ドメインで整理）
- **SC-22 §7 の想定 API が旧記法**（`PUT /vote`・`PUT /follow`）＝確定形（`POST`+`DELETE`）と表記差・未反映（軽微）。
- **SC-24 §7 の想定 API が旧記法**（`POST /api/ideas/{id}/chat` 等）＝E 確定形（`POST /chat-messages` ほか）と表記差・未反映（軽微・実装時に画面 md を追随）。
- 各画面 §9 の実装寄り TBD（軽微編集の通知抑制・添付の会社別上限の具体値 等）＝実装時確定でも可。

---

## 8. 再開に必要な環境情報

- **アプリの起動/テストコマンドは無い**（コード未着手＝`compose`/`package.json`/`pyproject.toml` 未作成）。ポート・環境変数も未定。
- **今使う操作**:
  - `git`（履歴・差分・コミット/プッシュ）。ブランチ `main`・remote `origin`。
  - ドキュメント＝Markdown を読む。画面モック＝`doc/画面設計/mocks/*.html`（入口 `mocks/index.html`）。
  - モック JS の構文チェック＝inline `<script>` を抽出して `node --check`。
- **技術スタック（確定・未構築）**: フロント Next.js(React/App Router)＋Three.js/R3F＋three-vrm ／ バック Python/FastAPI(4層) ／ DB PostgreSQL(管理DB1＋会社DB N) ／ Redis(セッション/OTP/pre-auth/会社コンフィグキャッシュ/冪等キー/Pub-Sub) ／ MinIO(添付・画像) ／ 全て Docker。
- **リポジトリ運用ルール**:
  - `.gitignore` で **`*.pdf` は追跡外**（Markdown が正・PDFは派生）。
  - **コミットは「本体コミット→handoff にハッシュ追記」の2段方式**。末尾に `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`。プッシュ先 `origin/main`。
  - ドキュメント方針＝**設計の正は1箇所・他は参照**（drift 回避）／**設計判断はなぜも併記（必須）**／文書間参照は `doc/規約/ドキュメント作成規約.md`（`CLAUDE.md` から自動参照）。

---

### 自己チェック（このファイルだけで再開できるか）
- ✅ 現在地（③ドメインE 完了・④ドメインF が次）と、F 着手に必要な入力/成果物/論点をファイル・節レベルで記載。
- ✅ 今回の変更（規約導入＋コーディング規約移動＝`22c9f47`／ドメインE＝`d735017`）とプッシュ状況（E＋本 handoff は未プッシュ）を記載。
- ✅ コードは無いこと・テストは存在しないことを明記。会社DB は 29 テーブル（E で `chat_reads` 追加）。
- ⚠ A〜E の詳細な決定理由は各 `doc/API設計/*.md` を正とすること（本 handoff は要約）。
