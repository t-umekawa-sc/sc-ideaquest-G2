# CLAUDE.md

> リポジトリ直下の本ファイルは Claude Code が毎セッション自動読込する。ここから各種規約を参照させる。

## 各種規約（該当作業時に必読・いずれも同列）

- [doc/規約/ドキュメント作成規約.md](doc/規約/ドキュメント作成規約.md) — 文書間参照の表記ルール（裸の `§x` 禁止＝文書名を接頭辞に付ける／同名系は版タグで区別／区切り記号は忠実引用／可能な限り相対リンク）。**汎用（プロジェクト非依存）**。**ドキュメントの作成・編集時に必読**。
  - 適用方針＝**折衷**: 新規文書は準拠／既存文書は設計上触れる機会に順次正規化／網羅的な一括見直しは設計確定後の最終パスで実施。
- [doc/規約/コーディング規約.md](doc/規約/コーディング規約.md) — レイヤ責務の分離（フロント／バック境界）・i18n・セキュリティ必須遵守・DRY（§2.3）・バックエンド4層（§3.4）・フロント feature 構成（§4.1）・意図的選択（§3.5）。**実装・コードの作成/編集時に必読**。
- [doc/規約/リポジトリ構成規約.md](doc/規約/リポジトリ構成規約.md) — トップレベル構成（`doc/`＝設計・`impl/`＝実装）とファイルの置き場所。**新規ファイル/ディレクトリを作る時に必読**。
- [doc/規約/テスト規約.md](doc/規約/テスト規約.md) — 設計書→テストパターン→テストコードのトレーサビリティ（TC-ID）・テスト階層（unit/int/api/e2e）・red-green 必須（§5.1＝実装前に対象の振る舞いで落ちる red を目視してから green・証跡はコミットメッセージ）・目的列（§1.2）・設計書ファースト（§5.2）。**テストの作成・編集時に必読**。
  - **テスト着手時の鉄則（§5 手順2）＝コードより先に `doc/テスト/<ドメイン>_*.md` に TC 行（`根拠` 列付き）を追加**する。md 無しでテストコードを書かない（無ければ新規作成）。コミット前に **`python3 scripts/check_tc_traceability.py`** で ✅ を確認（TC-ID の md 未記載を検出する再発防止ゲート）。
- [doc/規約/フロントエンド実装フロー規約.md](doc/規約/フロントエンド実装フロー規約.md) — フロント（画面）実装の進め方＝**画面モック先行（全画面をクリッカブル移植・デモデータ）→ 画面群ごとに backend 接続**（2026-08-12 方針転換）。`shared.css` を単一デザインシステムとして採用・段階移行／URL 付きモーダル（Parallel＋Intercept）標準／デモデータ seam（fixtures＝OpenAPI 型）／画面遷移図＝ルートの正／既存接続画面は接続維持でモック整合／**DoD＝モック一致**。**画面の作成/実装・進め方の判断時に必読**。

### プロジェクト固有の対応（ドキュメント作成規約 §1-3）

- 「どのファイルがどの版か」の対応表は本節に置く。
- **現状、同名系で取り違えの恐れがある文書は無いため対応表は未設置**。要件定義・データモデル・各API設計はいずれも単一の正本。必要が生じた時点でここに追記する。

## 設計の正本（該当作業時に参照・自動ロードはしない）

> 全般にかかわる設計の正本。**本節はパス参照のみ**＝毎セッションで全文を読み込むわけではない（自動読込は本 CLAUDE.md 自身だけ）。該当作業に入ったら**着手前に必ず対象を開く**。目的は「どの正本を・いつ読むか」の発見性を上げること（毎セッションのコンテキスト増は数行分のみ）。

- [doc/画面設計/デザイン標準.md](doc/画面設計/デザイン標準.md) — 横断 UI 標準の正本（DataTable §4.5・入力検証 §4.7・モーダル/確認/トースト・複製 等）。**画面/UI の実装・レビュー時に必読**。見本＝`doc/画面設計/mocks/*.html`（`style-guide.html`）。
- [doc/WEBアプリ開発時のセキュリティ対策一覧.md](doc/WEBアプリ開発時のセキュリティ対策一覧.md) — セキュリティ対策の元表（コーディング規約 §2 が参照）。**実装・レビューでのセキュリティ突合時に必読**。
- [doc/実装計画.md](doc/実装計画.md) — 実装順の正本（アカウント→クエスト→アイデア→評価→その他）。**着手対象・進め方の判断時に必読**。
- [impl/README.md](impl/README.md) — **実装現況（画面別の実装/接続状態・backend API 進捗・既知課題）の正本＝スナップショット**。進捗が進むたび追随更新（フロントエンド実装フロー規約 §1.2）。役割分担＝計画順は `doc/実装計画.md`／経緯・次アクションは `handoff.md`。**着手前の現況把握時に必読**。
- [doc/本番デプロイ要件.md](doc/本番デプロイ要件.md) — 本番デプロイ/インフラ/運用要件。**デプロイ・インフラ・本番運用に関わる設計/実装時に必読**。
- [doc/要件定義/README.md](doc/要件定義/README.md) — 機能要件（FR-xx）の正本。**要件（FR）の確認・追跡時に必読**。
- [doc/API設計/README.md](doc/API設計/README.md) — API 横断規約（§1.x＝認可/カーソル §1.8・DataTable クエリ契約 §1.8.1・Idempotency §1.9・画像 §1.10 等）。各ドメインは `doc/API設計/{A..L}_*.md`。**API の設計/実装時に必読**。
- [doc/ゲーム感フェーズ_進め方.md](doc/ゲーム感フェーズ_進め方.md) — **現行フェーズ（ゲーム感向上）の進め方の正本**＝非同期パイプライン運用／`feature/game-feel` 自走 push・main は承認後／受入とゲーム実装の並行 QA スタックの作法（workers 込みフル起動・本番ビルド・push後 `--build`・pytest ワーカ停止/cwd=impl）。受入台帳＝`doc/テスト/ゲーム感受入.md`。**ゲーム感フェーズの作業/QA の進め方判断時に必読**。

> 上記が参照するデータモデル [doc/データモデル.md](doc/データモデル.md)・動線 [doc/画面設計/画面遷移図.md](doc/画面設計/画面遷移図.md)・各画面 `doc/画面設計/screens/SC-xx_*.md` も同格の正本（該当作業時に開く）。
